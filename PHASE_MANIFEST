# Chip Measurement — Phase C: Documentation sync (PERIPHERY)
#
# ACCEPTANCE CONTRACT (written before Phase C implementation edits)
# C1 GIVEN the measurement layer is implemented, WHEN conventions are read,
#    THEN one Analytics section lists exactly the 13 custom events, states that
#    analytics parameters contain no PII, and requires new surfaces to log at
#    the same service-layer state-change boundary where possible.
# C2 GIVEN the canonical launch ledger, WHEN measurement readiness is checked,
#    THEN a VERIFIED measurement row records Crashlytics receipt, the 13-event
#    logged journey, the clean iPhone 17 Pro build, and the no-deployment scope.
# C3 GIVEN PrivacyInfo.xcprivacy and launch rows L11/L12, WHEN Firebase manifests
#    are cross-checked, THEN the app manifest is changed only if the approved
#    data-use matrix requires it. SDK-provided Crashlytics declarations are noted,
#    while app-wide analytics/privacy-label declarations remain gated on L11 and
#    must not be guessed; L11/L12 therefore do not close with this chip.
# C4 GIVEN the Phase C diff, WHEN documentation checks run, THEN the conventions
#    event list exactly matches AnalyticsEvents.swift, the checklist status is
#    internally consistent, PrivacyInfo.xcprivacy remains valid, git diff check
#    passes, and project.pbxproj remains byte-diff clean from HEAD.
# C5 GIVEN C1-C4 evidence, WHEN a fresh-context validator runs the Phase C
#    acceptance list, THEN it reports PASS before the phase is committed. Nothing
#    deploys and no remote state is mutated.
#
# EDIT SITES
# PHASE_MANIFEST
# peezy-conventions-v2.md
# LAUNCH_CHECKLIST.md
# SESSION_NOTES.md (session's final file write)
#
# CONDITIONAL EDIT SITE
# Peezy 4.0/PrivacyInfo.xcprivacy (only with an approved collected-data matrix)
#
# READ SITES
# peezy-chip-measurement.md
# peezy-execution-protocol.md
# CLAUDE.md
# Peezy 4.0/MainInterface/Models/AnalyticsEvents.swift
# Peezy 4.0/PrivacyInfo.xcprivacy
# Firebase 12.7.0 Crashlytics/Resources/PrivacyInfo.xcprivacy
# LAUNCH_CHECKLIST.md rows L11/L12
# Peezy 4.0.xcodeproj/project.pbxproj (read-only; protected)

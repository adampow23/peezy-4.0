# Post-Test Cleanup — Phase E: ISP screen (PERIPHERY)
#
# ACCEPTANCE CONTRACT (locked before implementation)
# E1.1 GIVEN the successful ISP comparison state has at least three plans,
#      WHEN it renders, THEN exactly the first three editorially curated plans
#      by ISPPlan.sortOrder appear as full-width ComparisonCardView cards.
# E1.2 GIVEN the three curated plan cards render on the target iPhone 17 Pro,
#      THEN all three fit without scrolling, or the third card clearly peeks;
#      comparison is vertical and no horizontal carousel/swipe UI exists.
# E1.3 GIVEN more than three providers are returned, THEN a visible
#      "See other providers" affordance appears below the three cards and gives
#      access to every remaining provider without adding more comparison cards
#      to the main screen.
# E1.4 GIVEN fewer than three valid plans are returned, THEN the success state
#      does not render a misleading partial comparison and the existing error
#      surface offers retry.
# E1.5 GIVEN a curated or additional provider is chosen, THEN it uses the
#      existing in-app provider handoff, records the chosen plan, and preserves
#      the existing completion gate.
#
# EDIT SITES
# PHASE_MANIFEST
# PHASE_E_REPORT.md (new)
# Peezy 4.0/MainInterface/Views/ComparisonCardView.swift
# Peezy 4.0/Tasks/Task Cards/SetupInternetFlow.swift
#
# READ SITES
# Peezy 4.0/MainInterface/Models/ISPPlanService.swift
# Peezy 4.0/MainInterface/Models/ComparisonCardModel.swift
# Peezy 4.0/Tasks/Task Cards/MoversComparisonView.swift
# peezy-chip-post-test-cleanup.md
# peezy-execution-protocol.md
# peezy-conventions-v2.md
# peezy-v1-architecture.md
# CLAUDE.md
# SESSION_NOTES.md (session-final write only)

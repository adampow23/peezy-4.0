# Chip Measurement — Phase A: Crashlytics (PERIPHERY)
#
# ACCEPTANCE CONTRACT (written before Phase A implementation edits)
# A1 GIVEN the existing firebase-ios-sdk package reference, WHEN the app target's
#    package products are inspected, THEN FirebaseCrashlytics is available to the
#    app without introducing a second Firebase package or editing project.pbxproj.
# A2 GIVEN Firebase configuration succeeds, WHEN the app launches, THEN
#    Crashlytics initializes alongside Firebase with no user-visible behavior change.
# A3 GIVEN assessment save, identity save, or workflow submission fails at an
#    existing caught-and-swallowed boundary, WHEN the error is handled, THEN the
#    same behavior is preserved and Crashlytics records the non-fatal error.
# A4 GIVEN a forced local test crash, WHEN the built app is run, THEN Crashlytics
#    console evidence or an API read-back proves the crash report arrived.
# A5 GIVEN the Phase A diff, WHEN the exact iPhone 17 Pro build, acceptance checks,
#    fresh-context validator, and diff review run, THEN A1-A4 receive an
#    evidence-backed PASS/FAIL; no deployment occurs and project.pbxproj is unchanged.
#
# NEEDS-CLARIFICATION
# - The app target currently has no FirebaseCrashlytics product dependency. The
#   chip explicitly requires STOP if adding that module requires project.pbxproj;
#   no source implementation proceeds unless read-only inspection proves otherwise.
#
# EDIT SITES (only after A1 is unblocked)
# PHASE_MANIFEST
# SESSION_NOTES.md (blocked-session closeout only)
# Peezy 4.0/MainInterface/Models/PeezyV1App.swift
# Peezy 4.0/Assessment/AssessmentModels/AssessmentCoordinator.swift
# Peezy 4.0/MainInterface/Models/IdentityService.swift
# Peezy 4.0/MainInterface/Models/WorkflowService.swift
#
# READ SITES
# peezy-conventions-v2.md
# peezy-execution-protocol.md
# peezy-chip-measurement.md
# CLAUDE.md
# Peezy 4.0.xcodeproj/project.pbxproj (read-only; protected)
# Peezy 4.0.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
# Peezy 4.0/MainInterface/Models/PeezyV1App.swift
# Peezy 4.0/Assessment/AssessmentModels/AssessmentDataManager.swift
# Peezy 4.0/Assessment/AssessmentModels/AssessmentCoordinator.swift
# Peezy 4.0/MainInterface/Models/IdentityService.swift
# Peezy 4.0/MainInterface/Models/WorkflowService.swift

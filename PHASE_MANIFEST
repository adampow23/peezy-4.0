# Spec 02 — Phase C manifest (seed-file reweights + reseed).
# The PreToolUse hook denies Write/Edit on any repo path not listed here.
#
# Read-site files this phase: functions/taskCatalogData.json,
# functions/seedTaskCatalog.js (read-only — deploy mechanics).
# Deploy: `cd functions && node seedTaskCatalog.js` — catalog reseed is the
# ONLY deploy in Spec 02 (no functions, no rules).
tasks/todo.md
functions/taskCatalogData.json

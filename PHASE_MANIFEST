# Chip Estimate Integrity — Phase A: Hidden-goods factor (CORE)
#
# ACCEPTANCE CONTRACT (written before Phase A implementation edits)
# A1 GIVEN scan-derived inventory cube and a bedroom answer, WHEN MoveScopeFactory
#    builds cube scope, THEN it adds exactly the LOCKED-pending-calibration hidden
#    goods factor (1BR 80, 2BR 140, 3BR 220, 4BR+ 300 cu ft) once.
# A2 GIVEN no moveable scan inventory, WHEN bedroom fallback supplies cube, THEN
#    the existing fallback midpoint is unchanged because that table already
#    includes hidden goods; storage contribution remains independent.
# A3 GIVEN otherwise-identical scan scopes before/after the factor, WHEN pricing
#    runs, THEN both range bounds move upward and the estimate contains the exact
#    locked disclosure: "Includes what scans can't see — closets, cabinets, drawers."
# A4 GIVEN scan and fallback estimates, WHEN disclosures are mapped to a mover
#    comparison card, THEN the hidden-goods line is visible for scan estimates
#    only and remains part of the durable estimate disclosures.
# A5 GIVEN the Phase A diff, WHEN standalone CORE tests and a clean simulator
#    build run, THEN both pass; a fresh-context validator grades A1-A4 PASS and
#    the phase report walks every hub-file diff before commit. No deploy occurs.
#
# EDIT SITES
# PHASE_MANIFEST
# Peezy 4.0/MainInterface/Models/PricingConstants.swift
# Peezy 4.0/MainInterface/Models/PricingEngine.swift
# Peezy 4.0/MainInterface/Models/MoveScopeFactory.swift
# Peezy 4.0/MainInterface/Models/MoversVendorQuote.swift
# Tests/PricingEngineTests.swift
# Peezy 4.0Tests/EstimateIntegrityTests.swift (new)
# SESSION_NOTES.md (session final write only)
#
# READ SITES
# peezy-chip-estimate-integrity.md
# peezy-execution-protocol.md
# peezy-conventions-v2.md
# CLAUDE.md
# peezy-v1-architecture.md
# Peezy 4.0/Inventory/Models/InventoryModels.swift
# Peezy 4.0/MainInterface/Models/ComparisonCardModel.swift
# Peezy 4.0/MainInterface/Models/Vendor.swift
# Peezy 4.0/MainInterface/Views/ComparisonCardView.swift

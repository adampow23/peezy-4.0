# Spec 07 Phase D — documentation sync (PERIPHERY)
#
# ACCEPTANCE CONTRACT (written before edits)
# D1 GIVEN the completed Spec 07 implementation and live evidence, WHEN the v1
#    architecture is read, THEN section 8 describes the 46-entry seeded provider
#    directory, exact-citation resolver invariant, high-confidence write-through
#    cache/admin-review boundary, and the five Firestore-backed ISP cards with
#    pending-affiliate provider fallback and no address-level serviceability claim.
# D2 GIVEN the 45-row catalog remains unchanged, WHEN the catalog sheet is read,
#    THEN its source count and SETUP_INTERNET row reflect the shipped Spec 07 flow
#    without implying that affiliate URLs or a serviceability API are live.
# D3 GIVEN the approved deployed rules and scoped operations, WHEN conventions and
#    CLAUDE.md are read, THEN they record entries-shaped userKnowledge, authenticated
#    read/backend-only providerDirectory and ispPlans, the URL-citation invariant,
#    exact seed counts, sanctioned deploy scope, and current accessibility counts.
# D4 GIVEN protocol section 6, WHEN SESSION_NOTES is read, THEN an append-only Spec
#    07 block records spec gaps, surprises, missing/misleading guidance, concrete
#    doc edits, and open ownership for affiliate URLs (Adam) plus admin review of
#    source:resolved directory entries.
# D5 GIVEN the Phase D diff, WHEN a fresh-context validator and final local checks
#    run, THEN D1-D4 receive evidence-backed PASS/FAIL, markdown changes pass
#    git diff --check, all three data/resolver validators pass, and the exact iPhone
#    17 Pro simulator build succeeds without any additional deploy or seed.
#
# NEEDS-CLARIFICATION (bounded, reversible interpretations)
# - The catalog sheet is a planning artifact but its 56-task source header is now
#   objectively stale; update the header/net-result note to the verified 45-row
#   post-Spec-06 catalog while leaving functions/taskCatalogData.json untouched.
# - "Last action" in protocol section 6 is treated as the final repository edit:
#   append SESSION_NOTES after the other doc edits, then validate and commit it so
#   the required artifact is independently checked and retained in history.
#
# EDIT SITES
# PHASE_MANIFEST
# peezy-v1-architecture.md
# peezy-v1-catalog-sheet.md
# peezy-conventions-v2.md
# CLAUDE.md
# SESSION_NOTES.md
#
# READ SITES
# peezy-build-spec-07.md Phase D and Files Summary
# peezy-execution-protocol.md section 6
# functions/providerDirectoryData.json
# functions/seedProviderDirectory.js
# functions/resolveProvider.js
# functions/ispPlansData.json
# functions/seedIspPlans.js
# firestore.rules
# Peezy 4.0/MainInterface/Models/UserKnowledgeService.swift
# Peezy 4.0/MainInterface/Models/ProviderDirectoryService.swift
# Peezy 4.0/MainInterface/Models/ISPPlanService.swift
# Peezy 4.0/Tasks/Task Cards/SetupInternetFlow.swift

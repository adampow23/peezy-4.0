# Post-Test Cleanup — Phase C: One question per screen (PERIPHERY)
#
# ACCEPTANCE CONTRACT (locked before implementation)
# C1.1 GIVEN the mover-refinement questionnaire, WHEN it renders, THEN exactly
#      one answer control is shown per screen and navigation preserves every
#      existing conditional field and the final comparison gate.
# C1.2 GIVEN any other scrolling multi-field form surfaced inside a task flow,
#      WHEN Phase C closes, THEN every qualifying form in the locked inventory
#      is converted to one question/field per screen with native back/cancel
#      behavior preserved for nested sheets.
# C1.3 GIVEN the complete ScrollView/Form inventory, THEN every excluded screen
#      is cited with a concrete reason (list/review/comparison/checklist or a
#      single logical input), and no qualifying multi-field form remains.
# C1.4 GIVEN a converted flow is exited and resumed, THEN its recorded answer
#      values still participate in the outer flow's existing persistence model;
#      no new nested surface can outlive its parent.
#
# QUALIFYING INVENTORY
# Peezy 4.0/Tasks/Task Cards/MoveRefinementView.swift
# Peezy 4.0/Tasks/Task Cards/MoveCheckInView.swift
# Peezy 4.0/Tasks/Task Cards/BoxReturnView.swift
# Peezy 4.0/Tasks/Task Cards/SuppliesKitView.swift (Customize sheet)
# Peezy 4.0/Inventory/Views/InventoryRoomReviewView.swift (Add Item sheet)
#
# EDIT SITES
# PHASE_MANIFEST
# PHASE_C_REPORT.md (new)
# Peezy 4.0/Tasks/Task Cards/MoveRefinementView.swift
# Peezy 4.0/Tasks/Task Cards/MoveCheckInView.swift
# Peezy 4.0/Tasks/Task Cards/BoxReturnView.swift
# Peezy 4.0/Tasks/Task Cards/SuppliesKitView.swift
# Peezy 4.0/Inventory/Views/InventoryRoomReviewView.swift
# Peezy 4.0/MainInterface/Models/PeezyV1App.swift (DEBUG fixtures only, if needed)
#
# READ SITES
# peezy-chip-post-test-cleanup.md
# peezy-execution-protocol.md
# peezy-conventions-v2.md
# peezy-v1-architecture.md
# CLAUDE.md
# Peezy 4.0/Tasks/Task Cards/MoversBookingReviewView.swift
# Peezy 4.0/Tasks/Task Cards/MoversComparisonView.swift
# Peezy 4.0/Tasks/Task Cards/PackingReadinessView.swift
# Peezy 4.0/Tasks/Task Cards/SetupInternetFlow.swift
# Peezy 4.0/Tasks/Task Card Components/QuoteSelectionFlow.swift
# Peezy 4.0/Tasks/Task Card Components/TaskFlowBusinessSearchCard.swift
# Peezy 4.0/Tasks/Task Card Components/TaskFlowConfirmAddressCard.swift
# Peezy 4.0/Inventory/Views/InventoryRoomHubView.swift
# SESSION_NOTES.md (session-final write only)

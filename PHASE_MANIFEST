# Chip Estimate Integrity — Phase G0: processInventory model migration (CORE)
#
# ACCEPTANCE CONTRACT (locked before migration implementation edits)
# M1 GIVEN production inventory scans fail because the deployed Anthropic model
#    is retired, WHEN this migration ships, THEN `processInventory` requests
#    exactly `claude-sonnet-4-6`, matching the Spec 07 resolver. The inventory
#    vision prompt, parsing, normalization, persistence, and callable contract
#    remain byte-identical.
# M2 GIVEN the model migration, WHEN focused verification runs, THEN a unit guard
#    rejects the retired identifier and requires the new identifier, Node syntax
#    passes, and the walked diff contains no behavior change beyond model/config.
# M3 GIVEN deployment authorization, WHEN shipping, THEN only
#    `functions:processInventory` is deployed. No rules deploy, other function
#    deploy, catalog reseed, or flow reseed occurs.
# M4 GIVEN the deployed migration, WHEN a bounded signed test-bot scan runs, THEN
#    it completes on the new model and its exact Firestore session and Storage
#    prefix are deleted and independently verified absent.
#
# EDIT SITES
# PHASE_MANIFEST
# functions/processInventory.js
# functions/tests/processInventoryModel.test.js
#
# READ SITES
# peezy-chip-estimate-integrity.md
# peezy-execution-protocol.md
# peezy-conventions-v2.md
# functions/resolveProvider.js
# functions/peezyBrain.js
# functions/index.js
# firebase.json

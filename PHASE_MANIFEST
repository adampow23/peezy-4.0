# Spec 03 Phase B — TaskStage persisted (model + decode + write path, no UI)
# EDIT SITES
tasks/todo.md
Peezy 4.0/Tasks/Store/PeezyCardFirestoreMapper.swift
Peezy 4.0/MainInterface/Models/TaskActionService.swift
# READ SITES (audited, not edited)
# Peezy 4.0/MainInterface/Models/TaskStage.swift — enum created Phase A, unchanged
# Peezy 4.0/MainInterface/Models/PeezyCard.swift — stage field created Phase A
# Peezy 4.0/MainInterface/Models/PeezyHomeViewModel.swift — write-pattern reference (markTaskCompleted et al.)
# Peezy 4.0/Tasks/Store/TasksStore.swift — listener path exercising the decode

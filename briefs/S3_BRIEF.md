# SLICE S3 — scheduler/migration/deletion/outbound integration

Job: When S3 is done, S2's deletion authority is obeyed on every path and the reset client drives its own cleanup. Every `ACCOUNT_DELETION_FENCE_WRITERS_V1` path that exists calls the shared fence inside its committing transaction — `functions/index.js`'s three inline writers (requestConcierge, submitTaskFlow, submitSupportMessage, whose support notification is awaited only after its committed record), `functions/spawnTasks.js`, `functions/dispositionTriggers.js`, new `functions/notificationIntents.js`, `functions/processInventory.js` at process success, process error, and `onInventoryRoomWritten`, `functions/researchTask.js`, `functions/peezyChat.js`, `functions/packageInventory.js`, `functions/submitCheckIn.js` plus `functions/submitCheckInCore.js`, `functions/entitlement.js`, `functions/validateSubscription.js` — and the seven remaining `USER_OUTBOUND_PROVIDERS_V1` sends (support email and SMS in `functions/notifySupport.js`, inventory email, check-in SMS, one Anthropic `messages.create` each in processInventory, researchTask, and peezyChat) run under `withOutboundLease`. `functions/accountDeletionFence.js` gains the C3 global-scheduler cleanup step inside the application sweep, the complete C6 registries with `taskDisposition.js`'s fence-writer entry recorded pending, the literal Ed25519 trust anchor in `PROVIDER_EVIDENCE_TRUST_ANCHOR_V1`, the `phase2LegacyCreateBlocker` handler, and the source scans that the C5 tests `timeouts do not exceed 300 seconds` and `active exports contain no dynamic server log sink` assert. The four import-safe `functions/scripts/*.js` exist as pure cores behind exact CLI arming guards: `purgeLegacyResolvedProviders.js` (two-pass non-seeded purge), `purgeLegacyDeletedAccounts.js` (audit-default historical migration over the 23-source registry, apply-only checkpoint and candidates, three Auth checks, two confirmation passes), `sealAccountDeletionProviderEvidence.js` (once-only sealer carrying the same trust anchor; refuses nonzero matches, backlog, nonfinite retention), `migrateOversizeEvents.js` (MIG-EVENT-V1); `functions/accountDeletionProviderEvidenceV1.json` stays absent, so Build A's `PROVIDER_EVIDENCE_NOT_ACTIVATED` behavior is the proven state. `functions/package.json` pins `@google-cloud/firestore` 7.11.6 as an exact direct dependency with a lock regenerated under Node 24 that resolves every C7 version unchanged. `firestore.rules` and `storage.rules` carry the deletion boundaries — owner denial while DELETING/DATA_DELETED/AUTH_GUARDING/ACCOUNT_DELETED, descendant and `userKnowledge` writes only under marker absence, Storage create/update only for an existing root with no marker, the `fcmTokens` create/update grammar with read/list denial, denial of `phase2System`, both work-row collections, candidates, `outboundLeases`, `legacyResetMigrations`, quarantine, archive, and refusal rows, and the §5 cleanup predicates over the reconciled `taskReset` marker — each with its `firestoreRules.test.js` case; `firestore.indexes.json` equals the C7 result tuple. `functions/dispositionTriggers.js` implements the scheduler contract (D1–D9, B1, D11 observables), `notificationIntents.js` the intent records and `claimTaskIntent`, `migrateOversizeEvents.js` D10 — each only after the contract states it (Decision 1). On the client, `ResetOperationRegistry` in `TaskPlanService.swift` gains `drive`: the `reset_progress`/`reset_final`, `committed_operation_inspection`, and `legacy_reset_reconciliation` DTOs and their transport, `ResetLocalCleanupAuthorityV1` constructed only from a validated `pending`/`awaiting_local_reset` inspection, inspect-before-each-callback with committed short-circuit and zero ephemeral notification on replay, the `recoverEpoch` actions, and the §6.5 legacy-migration rows; `RetakeAssessmentCoordinator.swift`'s closure slice is replaced by authority-taking closures (assessment and knowledge as per-document exact-marker transactions through the Firestore-runtime seam, dose only through `DailyDoseEngine.resetForRetake(authority:)`), the obsolete operation store leaves production construction, and the recorded same-process trace is exactly `resetDispatch,inspect,deleteAssessments,inspect,deleteUserKnowledge,inspect,resetDose,inspect,finalize,notification`; `DailyDoseLocalStore` gains epoch-r cleanup and the exact 0→1 bridge.

Why: S2 built the authority; this slice makes every writer and every outbound send obey it, so a deleted user's data cannot be recreated or re-sent once the marker exists, and it gives the reset client the generation-fenced cleanup the reconciled marker was written for. It is for the user whose deletion must be complete on every path, and for S4, whose recovery UI and deletion orchestration consume these fences, rules, and receipts.

Boundaries:
- Files you own, S3 sole Phase 2 writer — because the ordered-owner ledger gives them to S3 alone and no other slice edits them; in each existing provider file, only its fence call and, where the outbound registry lists one, its lease call: `functions/notifySupport.js` (lease only; it has no Firestore write to fence), `functions/researchTask.js` (fence and lease only; research links stay S9's), `functions/peezyChat.js`, `functions/packageInventory.js`, `functions/submitCheckIn.js`, `functions/submitCheckInCore.js`, `functions/entitlement.js`, `functions/validateSubscription.js`; `functions/processInventory.js` only at process success, process error, `onInventoryRoomWritten`, and the Anthropic call sites — because it is a Build 25 WIP file authorized at those sites and nowhere else; `functions/dispositionTriggers.js`; new `functions/notificationIntents.js`; the four new `functions/scripts/*.js`; `functions/package.json`, `functions/package-lock.json`; `storage.rules`; `firestore.indexes.json`; `functions/accountDeletionProviderEvidenceV1.json` in name only, because it is deliberately absent at S3 close (Decision 3).
- Files you own, S3 integrates — because S2 created the core or an earlier owner pass precedes S3's final integration, and S3 re-runs every earlier test unchanged: `functions/accountDeletionFence.js` and `functions/index.js` (S2's core; the three inline writers' fences, support notification after its committed record, the blocker export per Decision 4; every other export line byte-identical); `functions/taskPlan.js` (S2's core; the intent-claim follow-on and the `inspectLegacyTaskReset` action only, Decisions 1 and 5); `firestore.rules` (S1's owner pass first; S3 is final integrator); `functions/spawnTasks.js` (S1's file; its fence call was moved to S3 by the S2-brief decision; fence call only).
- Files you own, client reset driver — because the owner moved the drive reducer, cleanup authority, the three cleanup callbacks, the `recoverEpoch` actions, and `DailyDoseLocalStore` cleanup from S1 to S3 when S1's I6 was cut to reserve/bind, and because the contract makes S3 the client consumer of the reset, inspection, and reconciliation wires: `Peezy 4.0/MainInterface/Models/TaskPlanService.swift` (`ResetOperationRegistry.drive` and the reset/inspection/reconciliation DTOs and transport; S1's envelope, reserve/bind, and account-deletion transport are unchanged); `Peezy 4.0/MainInterface/Models/RetakeAssessmentCoordinator.swift` (the closure slice, `retake()`'s drive switch, production construction); `Peezy 4.0/MainInterface/Models/DailyDoseEngine.swift` (`DailyDoseLocalStore` cleanup and bridge, `resetForRetake(authority:)` cleanup).
- Extend-only test files — because tests are the reviewer and S1/S2 families are closed: `functions/tests/accountDeletionFence.test.js`, `functions/tests/taskPlan.test.js`, `functions/tests/dispositionTriggers.test.js`, `functions/rules-tests/firestoreRules.test.js`, `Peezy 4.0Tests/DurableStoreRecoveryTests.swift` (the contract puts every client decoder and the coordinator family there); `functions/tests/support/fakeFirestore.js` and `scripts/test-emulator.sh`, because S2 created them as shared support and S3's families need them (node and rules modes gain S3's files). New `functions/tests/notificationIntents.test.js` is the only new test file — because the frozen Node envelope already names it. `TaskPlanDispositionTests.swift`, `TaskSupersessionTests.swift`, and `FirebaseEmulatorTestSupport.swift` are read-only — because no S3 family is named for them.
- Read-only — because each has a frozen other owner: `functions/resolveProvider.js` and `functions/supportAdmin.js` (S2 sole writer; S3 verifies registry coverage without editing); `functions/getWorkflowQualifying.js`; `functions/taskDisposition.js` and `functions/taskInteraction.js` (S6; `taskDisposition.js`'s fence-writer entry stays recorded pending); `functions/validateResolveProvider.js` and every other deployment-inactive script (Decision 8); every Swift production file not named above (the Settings `deleteAccount()` hunk is S4/S7's, `PeezyHomeViewModel`'s dose keys S4's, `InventorySessionManager.swift`'s `pendingNarration` S4's); `Peezy 4.0.xcodeproj/project.pbxproj` and every other C7-pinned file byte-identical; `PHASE2_CONTRACT.md` changes only by owner-approved amendment.
- Mirror, don't invent — because the contract states the registries, wires, marker, pins, rules boundaries, and index tuple verbatim: S3 copies them and adds no member, code, path, or branch. Where the contract carries only a one-line resolution row, S3 codes nothing until the contract states the behavior (Decision 1).
- The trust anchor is owner input — because the manifest requires a reviewed literal and forbids placeholders: S3 asks for the base64url public key and its SHA-256 at the sealer increment, inserts the identical literal into `accountDeletionFence.js` and the sealer in one commit, generates no key, and creates no evidence artifact (Decisions 2–3).
- No production, no deploy, no configuration mutation, no arming — because the execution gate stops before deploy and the preamble forbids production: emulator project `demo-peezy-phase1` only; script tests call the pure cores and the argv guards, never an armed `--apply`; `phase2LegacyCreateBlocker` is never deployed and never exported by default (Decision 4); Storage stays adapter-backed because the emulator has no versions, soft delete, holds, or metageneration; the lock is regenerated offline with node@24's npm and diffed against C7 before commit.
- No new writes outside `ACCOUNT_DELETION_FENCE_WRITERS_V1` — because every registered write must read every owner root and require `accountDeletion` absent: the static check runs over every registry file that exists, and an entry whose file does not exist yet is recorded pending, never invented.
- Second-model diff review — because S3 is a deletion slice: the slice is not closed until the review named in Done has run and every finding is fixed or dispositioned in the ledger.
(Files you own for this slice: the lists above. Everything else is read-only — because two writers on shared deletion state is how we got 36 defects.)

Done:
- These tests exist, were run red against a deliberately broken build, then green (offline Node envelope under node@24; emulator-backed subsets and rules under `scripts/test-emulator.sh node|rules`; Swift suites under `scripts/test-emulator.sh swift`): `functions/tests/accountDeletionFence.test.js` — `timeouts do not exceed 300 seconds`; `active exports contain no dynamic server log sink`; `sealer refuses nonzero matches, backlog, and nonfinite retention`; the retained-copy cases of `Storage 0/1/100/101 retained-copy and guard boundary`; `Firestore 0/1/100/101 and two universal empty sweeps` and the external-family equal/+1 page and restart cases re-run unchanged and extended with the global-cleanup step; outbound lease/payload/call-graph for every C6.2 caller; registry, global-cleanup (100/101 quarantine rows, cursor-map member removal, `ACCOUNT_DELETION_GLOBAL_PATH_MALFORMED`), retained-copy, and export-graph cases; provider-cache purge CLI guards and two-pass core; policy-check 1/12/13, generation, break-glass; package/lock/config/evidence digests; historical-migration CLI guards, checkpoint, candidates, three Auth checks, and two-pass confirmation; the blocker's exact throw (Decision 4); trust-anchor literal parity between fence and sealer. `functions/rules-tests/firestoreRules.test.js` — owner denial while DELETING/DATA_DELETED/AUTH_GUARDING/ACCOUNT_DELETED; `fcmTokens` grammar and read/list denial; `phase2System` docs, work rows, candidates, `outboundLeases`, `legacyResetMigrations`, quarantine, archive, refusal denial; equal/+1 access budgets; late Storage and the existing-root/no-marker Storage predicates (Decision 6); §5 cleanup marker/generation/bridge cases; other/anonymous denial. `functions/tests/dispositionTriggers.test.js` — D1–D11, B1, MIG-EVENT-V1 codec/oracle/migration; `functions/tests/notificationIntents.test.js` — intent records and `claimTaskIntent`; `functions/tests/taskPlan.test.js` — intent-claim follow-on and `inspectLegacyTaskReset` (all four contingent on Decision 1, run red then green only after the amendment). `Peezy 4.0Tests/DurableStoreRecoveryTests.swift` — coordinator (D30): exact call trace, committed short-circuit with zero notification, generation-fence fixtures (device A paused before each inspection/callback while B finalizes at epoch r), callback error then committed/pending inspection, every §6.5 migration phase/edge and the `RESET_MIGRATION_*` local surfaces, `recoverEpoch` actionable-epoch rules; the receipt/inspection/reconciliation decoder mirrors against the same fixtures the Node tests freeze; daily-dose local fixtures (stale-writer CAS, empty floor, exact bridge order and crash, malformed preservation).
- These contract rows are closed (server half; client halves remain S4's): `Firestore user root and fence-covered descendants`; `Storage current, noncurrent, soft-deleted, held, copied, late-upload objects`; `Local/server quarantine` (server rows); `Provider cache and Firestore local cache` (purge CLI); `Notifications, FCM token, installation identity` (rules boundaries); `Release and server logs` (server sink test); `Gmail, Twilio, Anthropic, other legacy provider destinations`; `External indexed families`; `Backup/export destinations`. `Auth record and retained Auth copies` gains the trust anchor. `Local durable stores and deletion intent/journals` gains the client reset driver's cleanup.
- No new writes outside the fence-writer registry (static check over every existing C6.1 file), plus these S3 static gates: C7 pins re-verified (`recursive-delete.js` `2a17d8fb…`, `firestore_client_config.json` `2ed7a904…`, firebase-admin 13.6.0, `@google-cloud/firestore` 7.11.6 now direct, `@google-cloud/storage` 7.18.0, google-gax 4.6.1, google-auth-library 9.15.1, Node 24); `firestore.indexes.json` = 5,872 bytes sha256 `a6de8daf…` built from the frozen base `a7a432ec…` plus the eight appends; coordinator closure-slice preimage `a7cf81ca…` matched before the edit and the following line now `            postNotification: {`; `PeezySettingsView.swift`, `project.pbxproj`, `GoogleService-Info.plist`, `Peezy-4-0-Info.plist` byte-identical; `node --check` on every touched `.js` and all four scripts; zero `onObjectFinalized`/`firebase-functions/v2/storage` reachability; every pre-existing `functions/index.js` export line byte-identical except the blocker export (Decision 4); `changeTaskPlan` 540 s and every participating function ≤ 300 s; zero production `Firestore.firestore()` in S3-owned Swift files; `rg` zero `console.*` sinks in the S3-owned server files.
- Close-out review checkpoint (workflow rule 4): after the last increment is green and pushed, the close-out commit is tagged in the ledger as the S3 head. The second-model diff review receives `git diff 1a48e0f..<S3 head>` limited to the paths in Boundaries (so the brief, allowlist, and ledger commits sit outside the diff and are supplied separately), this brief, `PHASE2_CONTRACT.md` at its close-out hash, the manifest v9 and spec v5 hashes, the ledger's S3 section, and the red/green outputs above; it reviews diff-scoped and read-only (Decision 9). Every finding is fixed or dispositioned with a rationale in the ledger before S3 is reported closed.
- `/swiftui-pro` and `/swift-concurrency-pro` run once, as a close-out review pass over the three S3-owned Swift files; their findings are candidates dispositioned in the ledger, not instructions.
- LESSONS.md (`tasks/lessons.md`) updated.

## Decisions (owner; recommendation on each)

1. Contract coverage. The contract states the deletion/outbound half of S3 (C2.3, C2.8, C3, C6, C7, Reconciled 9–11) but carries the rest only as C8 rows or scattered mentions (C2.8's ROUTE_CLAIM row, C3's Build A gate over "the historical apply path", C5's "historical migration", the C6.3 and C7 names), so under workflow rule 2 those parts cannot be briefed or coded from it: the scheduler contract (manifest §2.2–2.5, lines 44–283, ≈47 KB), MIG-EVENT-V1 (§3, lines 284–467, ≈27 KB), notification intents and `claimTaskIntent` (spec v5 §3.3 lines 705–775 ≈16 KB for the intent, route-inbox, and persisted-route shapes; §5.2 lines 1265–1311 ≈20 KB for the scheduler/wake contract; and the command's own definitions at §3.3:664, §4.2:906, §4.3:1045, and §7:1434), the historical account migration (§11.4, lines 1710–1782, ≈20 KB), the provider-purge and sealer CLIs (§11:1439 ≈4 KB, §11.2:1652–1656 ≈4 KB), and the client legacy migration with its `inspectLegacyTaskReset` action (§6.5, lines 701–765, ≈21 KB; the action is not in C2.8 and not implemented). This is also why the brief runs past one page. Recommendation: Reconciled 12 as S3's first increment after your approval — verbatim extraction with source lines, the Reconciled 10 pattern, into new C9 (scheduler), C10 (event migration), C11 (intents), and additions to C2.8/C3/C7 — and order S3 so contract-complete work (fence and lease integration, rules, indexes, package pins, the client reset driver) runs first while the amendment is reviewed. Not recommended: deferring those parts to a slice after S4, because `dispositionTriggers.js` and `notificationIntents.js` are registry writers and the rules/index refreeze would stay open across the diff-reviewed pair.
2. Trust-anchor handoff. Recommendation: you supply the base64url public key and its SHA-256 when S3 reaches the sealer increment (after fence/lease integration); one commit inserts the identical literal into `accountDeletionFence.js` and the sealer, a test asserts the digest of the decoded key equals the literal in both, and the sealer's fixtures sign with an injected test key.
3. Evidence artifact. The ownership row makes S3 sole writer of `functions/accountDeletionProviderEvidenceV1.json`, but the manifest lets only the sealer create it from a signed external bundle under separate operational authorization. Recommendation: the artifact is absent at S3 close; its creation is an owner-run sealer invocation (Build B) outside any slice, and S3 proves Build A.
4. `phase2LegacyCreateBlocker`. A deployed `beforeUserCreated` export that always throws blocks every sign-up if it ships by accident. Recommendation: the handler lives in `accountDeletionFence.js` with a named test for its exact throw and event/project/tenant acceptance (closing contract Open 5), and `index.js` exports it only when `PHASE2_LEGACY_CREATE_BLOCKER=armed` is present at module load; absent means no export, so an ordinary deploy cannot block creation.
5. `taskPlan.js` follow-on ownership. Spec v5's ledger gives S3 the intent-claim/task-route follow-on in `taskPlan.js`; S2's close-out left the ROUTE_CLAIM/HANDOFF/WORKFLOW inspection identity sources to S5/S6/S7. Recommendation: S3 owns `claimTaskIntent` (with `notificationIntents.js`) and `inspectLegacyTaskReset`, contingent on Decision 1, and the ROUTE_CLAIM entry of `INSPECTION_IDENTITY_SOURCES` moves to S3 with it, since S3 then writes the `INTENT_CLAIM` records that row inspects; the HANDOFF, HANDOFF_CANCEL, and WORKFLOW identity sources stay with S5/S6/S7.
6. Storage Rules tests (contract Open 4). Recommendation: the existing-root/no-marker and owner predicates get cases in `firestoreRules.test.js`, whose run already includes the Storage emulator; no new test file; close Open 4 in the Reconciled 12 amendment.
7. `firestore.indexes.json`. The frozen base is not today's file: it drops the two deployed `tasks` composites (`status,next_trigger.kind[,next_trigger.at]`) in favor of `…next_trigger.fired…` composites plus a `thresholdProjection` composite, and keeps `events.processingState`. Recommendation: S3 writes the exact C7 result and the scheduler query changes in the same increment (Decision 1); the live index change is recorded as an owner deploy action for S7's pre-ship gate.
8. `functions/validateResolveProvider.js` still injects the `cache` dependency S2 removed. It is deployment-inactive and in no owner list. Recommendation: leave it unedited and record it in the close-out.
9. Diff-review packet. Recommendation: Sol via `/codex-consensus:codex-review`, read-only, one persistent thread, diff-scoped as stated in Done; report to `~/Downloads/peezy-reports/S3_DIFF_REVIEW.md` and, after your read, committed at the repo root as `S3_DIFF_REVIEW.md` (the V9 pattern).

# SLICE S5 — identity / provider conformance

Job: When S5 is done, every seam S4 proved against fakes has its one real conformer, and the handoff store exists. `GoogleIdentityAuthority` (`Peezy 4.0/Auth/GoogleIdentityAuthority.swift`) is the sole location of any direct `GIDSignIn.sharedInstance` call and owns the C9.7.14 slot machine (`idle | signingIn(generation, awaitingCallback|callbackHandled) | disconnecting(generation, expectedProviderUID) | signingOut(generation)`), with client ID, presenter, tokens, credentials and SDK errors confined to one authority-owned MainActor operation and only a UID or a fixed outcome crossing to the actor. `NotificationIdentityAuthority` (`Peezy 4.0/MainInterface/Models/NotificationIdentityAuthority.swift`) is the sole location of any `UNUserNotificationCenter`/`Messaging` deletion call and performs the C2.2 notification purge in its frozen order (unregister → remove delivered/pending → badge zero → `Messaging.deleteData` → `Installations.delete`). `HandoffSessionStore` and `HandoffSession` (`Peezy 4.0/Tasks/Handoff/`) own the handoff durable target and its quarantine sibling under `fileKind:"HANDOFF_AUTHORITY_V1"` (L3463): the four-member payload, the ordinary and reconciliation-cancel record grammar and phases, the C9.7.9 signed-auth reconstruction, the L1999 credential-revision CAS, the C9.7.7 expanded regime with its 32/96/64/128 ceilings and lineage projection, and the store's `DurableStoreRecovering` and `HandoffAccountDeletionPurging` conformances; it is the sole Keychain accessor through a real `InstallationIdentityProviding` implementing C9.7.10 exactly. `WorkflowService` gains its two conformances and nothing else. Real auth epochs replace `TransitionalFirebaseAuthAuthority` on every S5-authorized path, alongside the `AppleCredentialStateChecking` and `CurrentFirebaseUIDProviding` conformers and the C2.4 auth-transition purge hook. `ContextualOutcome` and `TaskExternalActionButton` render the P1-H surface and the C9.3.12 routes; the flow files carry the P1-B attempt-generation CAS and the A3-5 read-only restoration preflight; the legacy mutation fence is proved. Nothing is mounted: S7 constructs the eleven singletons, publishes the barrier and the recovery surface, wires the delegates, and owns the route envelope with its refresh lease.

Why: Deletion, recovery and identity are only as true as their least-real conformer, and today every one of them is a test double — the promise "your account is gone from this device" is proved against fakes. S5 is the slice that makes the S4 machine load-bearing, and the handoff store it adds is the readiness gate every other store's operations already depend on.

Boundaries:

- Files you own, new (C10.2 L4092–L4095, L4158–L4159; every file added to `PHASE_MANIFEST` before its first write): `Peezy 4.0/Auth/GoogleIdentityAuthority.swift`, `Peezy 4.0/MainInterface/Models/NotificationIdentityAuthority.swift`, `Peezy 4.0/Tasks/Handoff/HandoffSession.swift`, `Peezy 4.0/Tasks/Handoff/HandoffSessionStore.swift`, `Peezy 4.0/Tasks/Handoff/TaskExternalActionButton.swift`, `Peezy 4.0/Tasks/Handoff/ContextualOutcome.swift` — because the contract creates them under S5.
- Files you own, existing, whole scope (C10.2 L4096–L4103, L4105–L4110): `ProviderActionCard.swift`, `ProviderDirectoryService.swift`, `TaskFlowRouter.swift`, `TaskActionService.swift`, `SupportChatService.swift`, `SupportChatView.swift`, `FlowProgressSession.swift`, `FlowEngineView.swift`, `HandleAutoInsuranceFlow.swift`, `HandleHomeInsuranceFlow.swift`, `FindCleanersFlow.swift`, `SellItemsFlow.swift`, `RemoveItemsFlow.swift`, `SuppliesKitView.swift` — each with scope `—` in its row, narrowed only by the §11 clauses at L4164 and by L4165 (a preexisting `Firestore.firestore()` path is broadened only for the mechanical runtime substitution and the operation-registration guard, never a product-behavior edit).
- Files you own, existing, exact scope only: `FlowExitControl.swift` (L4104, L4187 — the exact-attempt lease and barrier, which is its own scope and unrelated to the route refresh lease; the scene follow-on is S8's); `AuthViewModel.swift` (L4160 — only the injected Apple checker/observer, routing every existing Google SDK operation through the one `GoogleIdentityAuthority`, the auth-transition purge hook, fixed diagnostics, and the Keychain/auth/readiness/provider conformances shared with `HandoffSessionStore.swift`; no authorization-code or token persistence, no auth UI change, no sign-in product-semantics redesign); `WorkflowService.swift` (L4185, S4-CD5 — the `WorkflowAccountDeletionPurging` and `DurableStoreRecovering` conformances and nothing else, widened only by Decision 2).
- Test files you own: three new, `HandoffSessionTests.swift`, `ContextualOutcomeTests.swift`, `LegacyTaskMutationFenceTests.swift` (C10.4 L4262–L4264), plus the two existing classes you own and integrate, `PeezyNudgeAnswerTests.swift` (L4265, L4196) and `ConversationFlowTests.swift` (L4266, L4192) — because L4284 forbids any extra Swift test file or suite and L4285 forbids any broad glob or additional test file. You contribute to, and do not own, `AppRootAuthRaceTests.swift` (L4208) and `TaskDispositionSurfaceTests.swift` (L4194). You re-run and never edit `functions/rules-tests/firestoreRules.test.js` (L4188); the C2.7 rerun on real conformers needs an authorized harness, which is Decision 5.
- Read-only — because each has a frozen other owner: `DurableStoreReadiness.swift` (the seams are S1's and S4's; S5 writes conformers, never declarations, L3903/L4183); `DurableStoreRecoveryCoordinator.swift`, `LocalPrivacyPurgeCoordinator.swift`, `DurableStoreRecoveryView.swift`, `TaskDispositionSurface.swift`, `TasksStore.swift`, `PeezyHomeViewModel.swift` (S4's); `PeezyV1App.swift`, `AppRouteInbox.swift`, `TaskRoute.swift`, both plists (S7 mounts, declares `Phase2ProductionRuntime`, and adds the collection flags); `PeezySettingsView.swift`, `AssessmentDataManager.swift`, `DailyDoseEngine.swift` (S1's); `PEEZY_STATE.md` (C10.5 L4300/L4310 list it unchanged and excluded); every `functions/` file and `firestore.rules` (S2's and S3's; L4180 and L4188 make S5 a verifier).
- Settled, not a decision: the route store's `DurableStoreRecovering` conformer is S7's (L3924) and the route envelope is `AppRouteInbox`'s to read, modify and replace (L1931). Its `refreshLease` and `refreshNamespace` are members of the RECEIVED record (L1940–L1943), so the P1-P/A3-7 60-second lease — its persistence, overlap refusal, expiry and late-completion handling — is S7's with `TaskRouteTests` (L4268). S5's share of that row is only the L1999 credential-revision CAS in `HandoffSessionStore` and the durable namespace/revision change the lease resumes on. S5 proves its half against a scripted route owner exactly as S4 did.
- Mirror, don't invent — because the contract states most shapes: the Google slot union and its four operations' outcome unions, the frozen callback scheme, the notification purge order, the handoff payload's four members, the two recovery keys, the candidate classes and their counts, the reconstruction table, the expanded ceilings and the lineage projection, the C9.7.10 Keychain lookup identity and its four result unions, the C9.3.12 precedence, the P1-H surface facts and exits, and the fence's captured-generation tuple. Where it does **not** — the handoff record grammar, the workflow store's schema and physical targets, and both stores' file names — the gap is closed by amendment at I0 before any code, never by invention (Decisions 1–3).
- No production, no deploy, no runtime mount — because S7 is the sole mount: emulator project `demo-peezy-phase1` only; the Google, Apple, notification and Keychain SDKs are reached only through the authorities and their spies; the L4172 gate stays at zero production `Firestore.firestore()` outside `LocalPrivacyPurgeCoordinator.swift` and the one `PeezyV1App.swift` initialization line, and S5 clears every remaining occurrence in the files it owns.
- Second-model diff review — because S5 completes the deletion path: the slice is not closed until the review named in Done has run with the S3/S4 pattern (three rounds, then one owner-scoped round on post-cap fixes, then confirming rounds until APPROVE) and every finding is fixed or dispositioned in the ledger.

(Files you own for this slice: the lists above. Everything else is read-only — because two writers on shared deletion state is how we got 36 defects.)

## Sequence

- **I0** Amendments S5-CD1..CDn written from the brief gate's anchors, fresh-context verified, registered (hash in `STATUS.md` and the ledger — **not** `PEEZY_STATE.md`, which C10.5 L4300/L4310 exclude); one Sol re-gate round on the amended brief. No S5 code until the amendments land, because three of them supply schemas the contract does not currently state.
- **I1** `HandoffSession.swift`: the value types only — the four-member payload, the ordinary and reconciliation-cancel records under the S5-CD grammar, their phases and transition table, the two C9.7.6 identity keys, the C9.7.9 reconstruction inputs, the canonical empty payload (L3593). Tests: the grammar cross-product and both keys in `HandoffSessionTests`.
- **I2** `HandoffSessionStore.swift` part 1: the store over the S5-CD target and quarantine names — envelope encode/decode at the 524,288 cap with the base+190 reserve (L3456, L3463), atomic replace, the C9.7.9 preserve-or-reconstruct table (L3737–L3743), the L1999 credential-revision CAS, the quarantine sequence, and `HandoffAccountDeletionPurging` with its fresh bounded postcondition reread (C2.2 L57). Tests: reconstruction rows, CAS, crash/tamper/quarantine (P1-N's handoff suite).
- **I3** `HandoffSessionStore.swift` part 2: `InstallationIdentityProviding` for real — the C9.7.10 lookup identity (`com.peezy.phase2.installation` / `installation-id-v1`, `kSecAttrSynchronizable:false`, `AfterFirstUnlockThisDeviceOnly`, no access group), the four result unions with no raw bytes or `OSStatus`, and the five files-present × Keychain-state rows including rekey-before-empty-target and never-overwrite-a-valid-item. Tests: every row and both crash boundaries.
- **I4** `HandoffSessionStore.swift` part 3: `DurableStoreRecovering` — the C9.7.3 order over its two files with the Keychain authority winning first (L3536), `foreign_installation` and `foreign_resolution_required` under the complete predicate, the handoff-specific `discard_quarantine` (last-file → empty-target-first, L3565/L3593), the receipt-mismatch loop over the HANDOFF and HANDOFF_CANCEL families, and the single attempt slot. Tests: the classification family and each action's CAS.
- **I5** The C9.7.7 expanded regime: entry only through item recovery or quarantine-only recovery, the four ceilings, the lineage identity and its one-for-one replacement, the projection computed before any mutating await and again before write, the marker-free return to normal, and the fresh-begin refusal with zero server call or opener. Tests: the ceiling boundaries, the invalid 32+96 example, the re-entry path.
- **I6** The A3-5 read-only typed restoration preflight and `FlowExitControl.swift`'s own exact-attempt lease and barrier (L4187): the preflight performs zero flow lease or write, and the cancel, both undo branches, and reopen take their fixed branches. The route refresh lease is **not** here — it is S7's (see Boundaries). Tests: the A3-5 zero-write cases and the preflight drift/relaunch fixtures.
- **I7** `GoogleIdentityAuthority.swift`: the slot machine (L3927), the four operations with their exact outcome unions (L3915), the frozen callback scheme (L3929), the confinement rule (L3928, L3943), the `handle(url)` same-operation rule (L3939), `disconnect` and `signOutAll` (L3940–L3941), and the C2.5 provider disposition the coordinator's `AccountDeletionProviderContextProviding` reads (L3938). Tests: every outcome; the slot transitions including a stale callback and a second-slot refusal; the sole-call-site scan in `AppRootAuthRaceTests`.
- **I8** `NotificationIdentityAuthority.swift`: the C2.2 L57 purge order, the C2.4 L79 registration authority (`clear` plus a matching signed-auth/startup-discovery generation) and the tap no-op during a nonclear gate finishing exactly once, and `NotificationIdentityPurging`. Tests: the pinned Messaging/Installations API-order fixture and the deletion-call-site scan in `AppRootAuthRaceTests`; the C4 L330 row's "pending/delivered zero, badge zero, Messaging/FID deleted".
- **I9** The identity conformers on the S5 paths: the `AuthAuthorityProviding` conformer replacing `TransitionalFirebaseAuthAuthority`, with `committed` legal only after the same UID/epoch CAS durably wrote exactly `expected.credentialRevision + 1` (C9.7.14 L3935) and `definitivelyDeleted` only under the exact C9.7.14 rule (L3936: the provider still holds the exact cached user for `expected.uid`, the epoch is not replaced, and a forced token refresh fails with Firebase's exact user-not-found; everything else is `notProven`, and a generic `signedOut` is never deletion proof — C2.6 L101 restates the same three conjuncts); the `AppleCredentialStateChecking` conformer wrapping only `getCredentialState` (L3914) and the C2.5 L97 behavior it drives — `revoked|notFound` for still-matching authority signs out and resumes only an already-named deletion, `authorized` is a no-op, every other state emits the fixed `APPLE_CREDENTIAL_STATE_UNRESOLVED` — plus the Apple disposition rule (L3937: one valid `apple.com` entry → `manual_required`; a complete list without → `not_required`; absent or ambiguous → `manual_required`); the C2.4 L80 auth-transition hook, so every SIGNED_OUT transition and direct A→B switch enters `loading` and creates or resumes the crash-durable all-accounts purge, claiming no remote deletion or revocation and blocking with Retry on provider or offline failure; and the `CurrentFirebaseUIDProviding` conformer (L3911) that S7's runtime singleton will consume, in `AuthViewModel.swift` under its L4160 scope. Tests: both refusal rules, every Apple outcome and disposition, the transition hook, and the `AppRootAuthRaceTests` drift cases.
- **I10** `ContextualOutcome.swift` and `TaskExternalActionButton.swift`: the P1-H six facts and two exits, the C9.3.12 routing consumers, the three returned-session branches (exact match, rotated epoch with `AUTH_EPOCH_CHANGED`, other installation with "Continue on the other device" and "Restart here"), the L2005/L2011 pre-presentation recheck set, and the server-authored action label (L3796/L3814). Tests: `ContextualOutcomeTests` plus the caller cases in `TaskDispositionSurfaceTests`.
- **I11** `WorkflowService.swift`'s two conformances over the S5-CD workflow store schema; the flow and provider files' register obligations — P1-B's instance + attempt-generation + write-revision callable CAS with its atomic proof clear and content revisions (L4795); P1-A's policy-ineligible Supplies gate in `SuppliesKitView.swift` (L4793); A2-4's waiting display from immutable policy or exact provider-directory evidence, with the same-trigger owned USER_ACTION fallback and no opaque visible identifiers (L4818); P1-K's `clearPackingPlan`, `packing1_` fingerprint, marker↔operation projection, one-step progress and terminal/error/adoption/replay (L4804); P1-L's five field actions with their exact alternative envelopes, revisions, typed receipts and conflicts, and replay/adoption (L4805) — and the L4172 runtime substitution across every S5-owned file.
- **I12** `LegacyTaskMutationFenceTests.swift`: the generic, server and local-adoption fence matrix and the fractional `REPLACE_QUOTES` replay (L4800, L4264, L4824). The S5 integration of `PeezyNudgeAnswerTests` (legacy claim/reset/non-adoption) and, because L4196 names no contributor after S5, its final full-class review over S1's spawn expectation/token cases. The S5 integration of `ConversationFlowTests` — A3-9's six-value workflow disposition consumption with its transaction-write presence rule, persisted string equality, and workflow trusted-WAIT/fallback consumption (L4831, L4266) — whose final full-class review follows S6 (L4192) and is not S5's.
- **Close-out** The C2.7 two-device and A→B families re-run on the four real conformers through the Decision-5 harness (L4209); the full envelope including `ConversationFlowTests` and `PeezyNudgeAnswerTests` in the green run; the diff review; `STATUS.md`, `HANDOFF.md`, `tasks/lessons.md`; the reviewed head tagged in the ledger.

Done:

- These tests exist, were run red against a deliberately broken build, then green on the emulator (`scripts/test-emulator.sh swift`, the C10.8 positions with `ConversationFlowTests` at 1, `HandoffSessionTests` at 8, `ContextualOutcomeTests` at 9, and `LegacyTaskMutationFenceTests` and `PeezyNudgeAnswerTests` after the manifest's `DurableStoreRecoveryTests` insertion): `HandoffSessionTests` — ordinary and reconciliation restoration-preflight durability, the crash/loss/auth-rotation/takeover matrix, the reconstruction table, the expanded-regime ceilings and the invalid 32+96 projection, and the L1999 credential-revision CAS; `ContextualOutcomeTests` — the P1-H facts and exits and the three returned-session branches; `LegacyTaskMutationFenceTests` — the fence matrix and the fractional `REPLACE_QUOTES` replay; `ConversationFlowTests` — the six-value workflow disposition consumption; `PeezyNudgeAnswerTests` — the legacy claim/reset/non-adoption cases, with its final full-class review recorded; `AppRootAuthRaceTests` — the Google callback-slot fixtures, the two call-site scans, and the pinned Messaging/Installations API order; and the C2.7 families re-run on the four real conformers.
- These contract rows are closed in full: `Keychain/provider credentials`, `Notifications, FCM token, installation identity`, `Two-device crash-mid-purge and guarding handoff` (on real conformers), P1-O, P1-H, P1-A (its Supplies gate is S5's last contribution; the other paths are complete S2 work), A2-1, A2-4, A3-5, A3-9, and the C9.7.9/C9.7.10 rows. P1-K and P1-L close in full if only their S5 legs remain at close; the ledger states which, rather than assuming. These close only their S5 half: P1-N's handoff and workflow suites — its closure names four stores including `AppRouteInbox`, which is S7's conformer (L4807, L3924); P1-P and A3-7's `HandoffSessionStore` credential-revision CAS, the lease itself being S7's; P1-B's `FlowProgressSession`/TaskAction/FlowEngine/FlowExit half (the Movers model pair at L4111–L4112 and `MoversChainCoordinatorTests` at L4267 are S6's); P1-G's generic, server and local-adoption fence matrix (the booking/readiness/packing views and the H54_REOPENED cases are S6's contribution to S5's class, L4195); and A3-2's fractional `REPLACE_QUOTES` replay leg. P1-B, P1-G and A3-2 close in full after S6, and S5 performs the final full-class review of `LegacyTaskMutationFenceTests` and `ConversationFlowTests` then, not now.
- Static gates: the sole-`GIDSignIn.sharedInstance` and sole-`UNUserNotificationCenter`/`Messaging` deletion call-site scans pass (D29, L344/L4842); zero production `Firestore.firestore()` in every S5-owned file (L4172); `project.pbxproj`, both plists, `PEEZY_STATE.md` and every `functions/` file untouched; no Swift test file or suite added beyond the three named (L4284), and no broad glob or additional test file (L4285).
- No new writes outside the fence-writer registry (static check passes).
- `tasks/lessons.md` updated; `STATUS.md` and `HANDOFF.md` rewritten at close.

## Decisions (owner)

The instrument rule the gate confirmed: when S5 must touch a file another slice owns, the lawful shape is an exact C10.3 ordered-writer row on the L4183/L4185 precedent, never a new C10.2 entry, because L4220 keeps the frozen owner and a duplicate entry installs a second writer.

**Mandatory before any S5 code** — the contract does not currently state a shape the slice must build:

1. **The handoff record grammar.** The contract fixes the canonical empty payload (L3593), the two recovery identity keys (L3613–L3614) and the summary "stored/requester namespaces, immutable request/hash, PREPARED→APPLYING phases" (L4808) — but no `HandoffSessionRecordV1`, no exact member sets, no per-phase required/forbidden table, no transition table, no sort order, and no request/receipt mapping. Recommendation: an amendment (S5-CD) enumerating both record unions and all of the above, drafted from the S3 reset-row precedent, before I1.
2. **The workflow durable store's schema and physical targets.** `WorkflowService` must conform to `DurableStoreRecovering` (L3924), but the contract fixes only `fileKind:"WORKFLOW_REQUESTS_V2"` with cap 16,777,216 (L3466) and the counted universe (L3624, L3644) — never the target or quarantine file names, protection class, payload, active-record grammar, phases, empty state or sort. Recommendation: an amendment (S5-CD) supplying them, plus the C10.3 row widened from "the two conformances and nothing else" to authorize exactly the implementation those conformances and their purge postconditions need.
3. **The handoff store's durable file names.** The contract fixes the route store's literally (L1922–L1923) and names the handoff store only by its `fileKind`. Recommendation: an amendment fixing `PeezyHandoffAuthority-v1.json` and `PeezyHandoffAuthority-v1.quarantine-v1.json`, Application Support, protection `completeUntilFirstUserAuthentication`, matching every other durable file.
4. **The `inspectCommittedOperation` transports S5's two recovery conformers need.** C9.7.8 requires the `receipt_mismatch` reconcile to run through `inspectCommittedOperation`; the typed callable DTOs live in `TaskPlanService.swift`, and C10.3 L4182 gives S5 "tests only" there. S4 resolved the identical conflict with S4-CD2. Three families, not two: HANDOFF and HANDOFF_CANCEL for `HandoffSessionStore`, **and WORKFLOW** for `WorkflowService`, whose `receipt_mismatch` snapshot carries `store:"workflow"` with `availableActions:["reconcile"]` (L3493, L3540, L3571, L3713). ROUTE_CLAIM stays S7's. Recommendation: extend the writer sequence to "… → S5 (the HANDOFF, HANDOFF_CANCEL and WORKFLOW `inspectCommittedOperation` transports only)".
5. **The C2.7 real-conformer rerun harness.** L4209 requires S5's close-out to re-run those families on the real conformers "unchanged", but the families construct `RecordingPurgeOwners` fakes and `DurableStoreRecoveryTests.swift` is S4's (L4271), so an unchanged rerun cannot substitute real owners. Recommendation: a C10.3 S4→S5 exact-scope test contribution authorizing a real-owner injection path in that file, naming the construction and injection concretely; the alternative is to authorize the same unchanged vectors inside a named S5-owned suite.
6. **The Home dose keys (carried from the S4 gate, finding 9).** C9.5.16 L2842 requires every `PeezyHomeViewModel` dose read and write to go through `DailyDoseLocalStore`; Home still uses the three v0 keys. The production file is S4's (L4091) and `DailyDoseEngine.swift` is S1's, so this needs an ordered-writer row and cannot be left as a ledger divergence — the contract already settles the behavior, and no later slice has authority over `PeezyHomeViewModel`. Recommendation: an amendment adding "S4 owns the file → S5 routes the three dose reads and writes through `DailyDoseLocalStore` under the v2 epoch/revision CAS, and nothing else", with the pinned assertions in `PeezyNudgeAnswerTests` re-pointed to the v2 state. If the owner prefers a different later writer, name it — but some writer must be named.

**Optional, owner's call, default is no edit:**

7. **The assessment geocode seam (carried from the S4 gate, finding 20).** `AssessmentDataManager.computeDistanceAndInterstate()` applies its result to the shared data manager before the completion's UID and load-generation guard can run, so a stale completion's fields can survive into a replacement flow. This is a scope expansion, not an obligation: P1-R's literal closure (L4811) names only `AppRootView.swift`, `PeezyMainContainer.swift` and `AppRootAuthRaceTests.swift`, and the file is S1's at L4131. Default: no edit, the residual risk tracked as a ledger line. If the owner does want it closed, the instrument is a C10.3 row — "S1 owns the file → S5 changes `computeDistanceAndInterstate()` to return an operation-local value, the caller applying it after its guard, and nothing else" — with a named S1-scope reviewer.
8. **`PEEZY_STATE.md`.** C10.5 L4300 and L4310 list it unchanged and excluded, but `CLAUDE.md` makes it the canonical project-state document that outranks everything else, and the S3 and S4 closes both recorded slice state there — including this session's S4 close, disclosed as a boundary exception in the ledger. The two instructions conflict for a file that is documentation rather than an implementation path. Recommendation: the owner rules once, either exempting the project-state document from C10.5's exclusion or directing that slice state live only in `STATUS.md` and the ledger. S5 writes nothing there until then.

**Ledger interpretation, no amendment:**

9. **Where the real auth-epoch authority lives.** No contract row names `TransitionalFirebaseAuthAuthority`; L4160 authorizes the auth conformances in `AuthViewModel.swift` "with `HandoffSessionStore.swift`", and `TaskPlanDispositionTests.swift` (not S5's) pins `TransitionalFirebaseAuthAuthority.legacyAuthEpochUUID`. Recommendation: the real conformer lands in `HandoffSessionStore.swift` (it owns the durable epoch and the credential revision the L3935 CAS increments), `AuthViewModel.swift` injects it and supplies `CurrentFirebaseUIDProviding`, and the transitional type stays declared so the S1 pin keeps compiling.

## Register candidates carried into this slice

The two interpretations S4 recorded that S5 inherits: a UID-scope purge journal with no intent is unmatched authority and blocks (`LOCAL_PRIVACY_PURGE_FAILED`); and the C9.7.8 same-process drive rows retain their first-response behavior without installing provenance. Both are ledger lines, not amendments.

## Verification already performed on this brief

A six-lens fresh-context pass (contradictions, unsupported claims, omissions, test envelope, boundaries, decisions), each candidate finding adversarially verified by an independent refuter: 62 agents, 56 candidates, 19 survived and were applied — most consequentially, the ownership premises of two carried decisions were false and their instruments wrong. The Codex brief gate then returned ITERATE with nine BLOCKERs and one HIGH, all folded in here: the route refresh lease reassigned to S7, the two missing schemas and the missing harness raised as mandatory amendments, P1-N and P1-A rescoped, the four uncovered register rows given Sequence items and falsifiers, the C2.5 and `CurrentFirebaseUIDProviding` obligations spelled out in I9, the `PEEZY_STATE.md` write removed from I0, the Home-dose decision's nonconforming alternative deleted, and the two non-decisions demoted.

# SLICE S1 — seams/reset stamps/runtime consumers + TaskPlanService transport

Job: When S1 is done, new `Peezy 4.0/Tasks/Durable/DurableStoreReadiness.swift` exists and declares, with no file I/O, `StartupBarrier`, store/state vocabulary, `RouteIngressReceiving`, `RouteIngressDiagnosticReporting`, `AuthAuthorityProviding`, `CurrentFirebaseUIDProviding`, `AccountDeletionProviderContextProviding`, `AccountDeletionCompletionPresenting`, `AppleCredentialStateChecking`, `GoogleIdentityControlling`, `FirestoreLocalCachePurging`, `NotificationIdentityPurging`, `ClientTelemetryPrivacyPurging`, `RoomCaptureArtifactPurging`, `LocalPrivacyPurgeCoordinating`, `ResetEpochAuthorityProviding`, `ResetEpochConflictRecovering`, `InstallationIdentityProviding`, `AccountDeletionRemoteProviding`, and the four durable-store purge seams `RouteAccountDeletionPurging`, `HandoffAccountDeletionPurging`, `ResetAccountDeletionPurging`, `WorkflowAccountDeletionPurging`. `ResetOperationRegistry` inside `TaskPlanService.swift` is the sole actor and sole reader/writer of `PeezyTaskPlanReset-v2.json`: exact envelope payload, auth-scoped `reserve`/`bind`/`drive` singleflights, deterministic `(uid,expectedEpoch)` point path with no alias collection query, `ResetEpochAuthorityProviding.current(uid:expectedAuth:)`, and `recoverEpoch(recoveryStateDigest:expectedTaskGenerationEpoch:expectedPhase:action:)`. The same file owns the typed DTO/transport conformance `AccountDeletionRemoteProviding.perform(_ request: AccountDeletionRequestV1) async throws -> AccountDeletionRemoteResultV1` (requests only `discover|begin|resume|finalize(uid,operationId,proofNonce)`). `RetakeAssessmentCoordinator.retake()` calls registry reserve as its first asynchronous operation and its three cleanup closures accept `ResetLocalCleanupAuthorityV1`. `AssessmentDataManager.saveAssessment()`, `UserKnowledgeService.merge()`, and `DailyDoseEngine.resetForRetake(authority:)` stamp/verify `task_generation_epoch` in root transactions, and `DailyDoseLocalStore` inside `DailyDoseEngine.swift` is the sole accessor of `peezy.{uid}.dailyDose.v2`. Every S1-owned direct `Firestore.firestore()` acquisition is replaced by `FirestoreRuntimeOwner`, capturing `FirestoreRuntimeGeneration` and registering/unregistering operations. The first-pass `firestore.rules` marker-shape/stamp predicates land with their `functions/rules-tests/firestoreRules.test.js` cases.

Why: This gives S2–S7 the closed seams, one reset-envelope owner, epoch-stamped data, and a single Firestore-runtime chokepoint that later slices purge and gate through, so account deletion can be proven complete rather than asserted. It is for the user whose "someone in your corner" promise depends on deletion being legally defensible, and for the later slice owners who would otherwise inherit two writers on shared deletion state.

Boundaries:
- Files you own, S1 core/reset — because §12.2 lists them as S1 rows and §5/§6.6/§8 put the registry, transport, coordinator, and stamps there:
  - new `Peezy 4.0/Tasks/Durable/DurableStoreReadiness.swift`
  - `Peezy 4.0/MainInterface/Models/RetakeAssessmentCoordinator.swift`
  - `Peezy 4.0/Menu/PeezySettingsView.swift` (sole authorized hunk: the `deleteAccount()` function slice)
  - `Peezy 4.0/Assessment/AssessmentModels/AssessmentDataManager.swift`
  - `Peezy 4.0/MainInterface/Models/UserKnowledgeService.swift`
  - `Peezy 4.0/MainInterface/Models/DailyDoseEngine.swift`
  - already-authorized `Peezy 4.0/MainInterface/Models/TaskPlanService.swift`
- Files you own, S1 Firestore-runtime/gate closure — because the edit is the mechanical runtime-provider substitution and operation-registration/cancellation guard, no product behavior edit; `PaywallGateView.swift` only at its `redeemGiftCode` call site; `InventoryStorageService.swift` additionally only for §11's media-transfer/buffer registry, cancellation, and generation guards:
  - `Peezy 4.0/Assessment/AssessmentViews/Onboarding/GeneratingView.swift`; `Peezy 4.0/Inventory/Services/InventoryStorageService.swift`
  - `Peezy 4.0/MainInterface/Models/BoxReturnService.swift`; `Peezy 4.0/MainInterface/Models/CheckInService.swift`; `Peezy 4.0/MainInterface/Models/ISPPlanService.swift`
  - `Peezy 4.0/MainInterface/Models/IdentityService.swift`; `Peezy 4.0/MainInterface/Models/PeezyStackViewModel.swift`; `Peezy 4.0/MainInterface/Models/SubscriptionManager.swift`
  - `Peezy 4.0/MainInterface/Models/Vendor.swift`; `Peezy 4.0/MainInterface/Views/Paywall/PaywallGateView.swift`
  - `Peezy 4.0/Tasks/FlowEngine/FlowDefinition.swift`; `Peezy 4.0/Tasks/FlowEngine/InAppTaskFlows.swift`; `Peezy 4.0/Tasks/FlowEngine/MoveAnswersStore.swift`; `Peezy 4.0/Tasks/Task Cards/ScanInventoryFlow.swift`
- `Peezy 4.0/Inventory/Models/InventorySessionManager.swift`: migrate exactly its six `Firestore.firestore()` occurrences to `FirestoreRuntimeOwner`, nothing else — because it is a baseline-`d321547` WIP file; S4 alone later changes `pendingNarration`, and no other byte is authorized for Phase 2.
- `firestore.rules` and `functions/rules-tests/firestoreRules.test.js`: S1's owner pass is first — because S3 then integrates the account-deletion/system/query boundaries and performs the final full-file review; do not write those boundaries.
- The other three `d321547` files — `functions/processInventory.js`, `Peezy 4.0/Inventory/Views/InventoryCameraView.swift`, `Peezy 4.0/Inventory/Services/NarrationService.swift` — are read-only for S1 — because they are authorized only at S3/S4 call sites (`onInventoryRoomWritten` fence/Anthropic lease, `pendingNarrationTranscript`, `NarrationService.start`).
- Preserve the C7 pins you own — because any outside-slice byte change, retake-slice change, or a remaining direct `httpsCallable("deleteAccount")` is fatal:
  - `Peezy 4.0/Menu/PeezySettingsView.swift` pre-edit whole file `419055c9eb0e756daf70f68aeb37d5761888460a65f4c129243fe3f28a4c34d5`
  - `retakeAssessment()` lines 662–679 `348ec7db63aeecb76e7012d4fac91e41b60df78562a32b9528a5e40553a5a2e2` (unchanged after patch)
  - `deleteAccount()` lines 681–712 `8f86efa8523960588cfe8a17d6351645cba50bb14f4258a77a4dc8ccf98a5bbf`; file minus that slice `ef856d3bf2f89b79d4340e5b4afe8e3844b13bb6167854486d5919d83b37e4cc` (identical after patch)
  - `RetakeAssessmentCoordinator.swift` closure slice (`deleteAssessments: { userId in` … `resetDose` `},`) preimage `a7cf81cab9bfa9c49e5dba2c12c618bbfe309818284156f9594b0be127515e93`; next line must be `            operationStore: store,` (verified before the edit)
- Stop and ask before touching Settings outside `deleteAccount()` — because the contract's C7 says file-minus-slice is identical after patch, manifest §12.2 instead normalizes "one exact runtime-provider line" before hashing, and `PeezySettingsView.swift:833` holds a `Firestore.firestore()` outside the slice; that is a scope question only the owner can settle.
- Do not declare a second `FirestoreRuntimeOwner`, `StartupBarrier` instance, or any of the eleven `Phase2ProductionRuntime` statics — because `FirestoreRuntimeOwner` is the §11 type in S4's `LocalPrivacyPurgeCoordinator.swift` and the statics are S7's; S1 codes consumers against the type and seams, not a runtime.
- `Peezy 4.0Tests/DurableStoreRecoveryTests.swift` is added by S4 per manifest §12.3 yet holds S1's named closures (D14/D23/D24/D30/B2), and spec v5 gives `TaskPlanDispositionTests.swift` (D15) to S2 and `TaskRouteTests.swift` (B4) to S7, so S1 owns no test file as written — because the workflow makes tests the reviewer, confirm who creates that file before S1 starts.
- No new writes outside `ACCOUNT_DELETION_FENCE_WRITERS_V1` — because every registered write must read every owner root and require `accountDeletion` absent; S1 owns no `functions/*.js` path, so a fence-writer edit is out of scope unless a frozen §8.3 S1 row names it (none named in v9 or the contract).
- No deploy, configuration mutation, or production read/write/send; destructive tests only on the Firebase emulator — because the execution gate stops before deploy and the standard preamble forbids production.
(Files you own for this slice: the lists above. Everything else is read-only — because two writers on shared deletion state is how we got 36 defects.)

Done:
- These tests exist, were run red against a deliberately broken build, then green on the emulator: `Peezy 4.0Tests/DurableStoreRecoveryTests.swift` — reset envelope (D14/D24/B2); readiness (D23/D28); coordinator (D30); `UID-interpolated preference keys are registry-complete`; offline A/B cache. `Peezy 4.0Tests/TaskPlanDispositionTests.swift` — D15 (spec v5 §:1807 has S2 create this file; it does not exist yet). `functions/rules-tests/firestoreRules.test.js` (`firebase emulators:exec --only firestore,storage`) — S1 reset-stamp/marker predicates: none named in contract. `AccountDeletionRemoteProviding` DTO/transport: none named in contract. `ResetEpochConflictRecovering`: none named in contract beyond readiness (D23/D28).
- These contract rows are closed: none closed by S1 alone. S1 contributes to `Local durable stores and deletion intent/journals` (reset owner `ResetOperationRegistry`, four durable-store purge seams); `Provider cache and Firestore local cache` (Firestore acquired only through `FirestoreRuntimeOwner`); `UserDefaults/preferences` (`DailyDoseLocalStore` sole accessor of `peezy.{uid}.dailyDose.v2`); `Auth record and retained Auth copies` (C2.3 transport). `firestore.rules` first pass: none named in contract.
- No new writes outside the fence-writer registry (static check passes), plus these S1 static gates:
  - `rg` over S1-owned files shows zero production `Firestore.firestore()`
  - `InventorySessionManager.swift` diff is exactly six substitutions and no other byte
  - the C7 hashes above re-verified after patch (`retakeAssessment()` slice unchanged; file minus `deleteAccount()` identical)
- LESSONS.md updated.

## Sources

- Title / slice name: PHASE2_WORKFLOW_v2.md "Sequence" item 2; template from "Brief template (one per slice)".
- Job: manifest §8:971 (protocol list, no file I/O); §5:476 (`ResetOperationRegistry` sole owner, no collection query); §5:482 (`ResetEpochAuthorityProviding.current`); §5:551 (`ResetEpochConflictRecovering`, `recoverEpoch(...)`); §8:995 and §8:1005 (TaskPlanService DTO/transport); §8.9:1048 and contract C2.3:63 (`perform` shape and request union); §6.6:768/772 (retake reserve-first, `ResetLocalCleanupAuthorityV1` closures); §5:539/543/545 (stamps, `resetForRetake(authority:)`, `DailyDoseLocalStore`); §12.2:1866 (runtime substitution, generation capture); §5:543/563 (rules predicates).
- Why: PHASE2_WORKFLOW_v2.md standard preamble line 17 (brand promise, legally defensible); template line 42 (36 defects).
- Boundary "S1 core/reset": manifest §12.2:1857; `TaskPlanService.swift` "already-authorized" §8:995; sole `deleteAccount()` hunk §6.6:770.
- Boundary "S1 Firestore-runtime/gate closure": §12.2:1858 (file list); §12.2:1866 (mechanical substitution, `PaywallGateView.swift` `redeemGiftCode` limit, `InventoryStorageService.swift` extra scope).
- Boundary InventorySessionManager six occurrences: §12.2:1850, §12.2:1864, §13:2088; contract C1:13 and C6.1:164 note.
- Boundary firestore.rules first pass: §12.2:1846.
- Boundary other three d321547 files read-only: §12.2:1864; contract C6.1:164 note.
- Boundary C7 pins: contract C7:287 (PeezySettingsView four hashes), C7:288 (RetakeAssessmentCoordinator preimage and next line); fatal conditions §6.6:770/772.
- Boundary Settings scope question: contract C7:287 ("identical after patch") vs manifest §12.2:1866 ("normalizing its one exact runtime-provider line"); §6.6:770 ("any outside-slice byte change ... is fatal"); working-tree line 833 observed this session.
- Boundary no second runtime/static: §8:995/997 (eleven statics, single `FirestoreRuntimeOwner` type), §11:1529 (type definition), §12.2:1859 (S4 file "containing the Firestore runtime"), §12.2:1868 (S7 mount).
- Boundary DurableStoreRecoveryTests ownership: §12.3:1876 ("Add S4 ..."); §13:2051/2058/2059/2065/2069 (D14/D23/D24/D30/B2 closures); contract C5:150; PHASE2_WORKFLOW_v2.md rule 3. Spec v5:1807 ("S2 owns new `Peezy 4.0Tests/TaskPlanDispositionTests.swift`"), :1811 ("S7 owns new `Peezy 4.0Tests/TaskRouteTests.swift`").
- Boundary fence-writer registry: contract C6.1:162-164; manifest §12.2:1853 ("already-frozen S1/S2/S3/S9 owner").
- Boundary no deploy/emulator only: §13:2088; PHASE2_WORKFLOW_v2.md preamble line 25.
- Boundary everything else read-only: PHASE2_WORKFLOW_v2.md template line 42.
- Done falsifiers: contract C5:150 (DurableStoreRecoveryTests families and named test), C5:156 (TaskPlanDispositionTests D15), C5:152 (firestoreRules.test.js run command), C4:135 (offline A/B cache), C2.7:104 and §12.3:1876 ("every client decoder" in DurableStoreRecoveryTests, no named test → "none named in contract"); §5:563 (rules fixtures named only in the manifest).
- Done §11.5 rows: contract C4:132 (`Local durable stores and deletion intent/journals`), C4:135 (`Provider cache and Firestore local cache`), C4:136 (`UserDefaults/preferences`), C4:131 (`Auth record and retained Auth copies`); contributions from C2.2:58 (eight owners incl. `ResetOperationRegistry`), C2.4:75 (Firestore acquisition gated), C6.6:270 (`DailyDoseLocalStore`), C2.3:63.
- Done static checks: PHASE2_WORKFLOW_v2.md template line 47; §12.2:1866 (`rg` gate); §12.2:1850 (six substitutions); contract C7:287-288.
- Done LESSONS.md: PHASE2_WORKFLOW_v2.md template line 48 and preamble line 26.

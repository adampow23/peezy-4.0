# SLICE S2 — workflow/server implementation

Job: When S2 is done, `functions/index.js` exports a replaced `deleteAccount` (`onCall`, `us-central1`, `timeoutSeconds:60`) whose request is exactly the closed union S1's transport already sends, `{schemaVersion:1,action:"discover"|"begin"|"resume"|"finalize",uid,operationId,proofNonce}`, and whose every response is exactly one of the four C2.3 wires (`account_deletion_discovery/absent`, `account_deletion_data_final`, `account_deletion_auth_guarding`, `account_deletion_account_deleted`; every value copied from the validated root, times as canonical UTC millisecond RFC 3339 strings, exactly the member sets S1 decodes) or exactly one thrown `HttpsError` whose `details` is `{schemaVersion:1,reason:"REQUEST_INVALID",field}` | `{schemaVersion:1,reason:"AUTH_REQUIRED"}` | `{schemaVersion:1,reason:"DELETION_CAPABILITY_INVALID"}` | `{schemaVersion:1,reason:"DELETION_RETRY_REQUIRED"}`; it also exports `reconcileAccountDeletionStorage` and `reconcileAccountDeletionAuth` with their exact C3 `onSchedule` options and no Storage-finalizer export. New import-safe `functions/accountDeletionFence.js` owns the root branch grammar DELETING-sweeping → DELETING-guarding → DATA_DELETED → AUTH_GUARDING → ACCOUNT_DELETED, capability enrollment (1…64 sorted, `authenticatedOverflow` at 64, same-ID/different-proof invalid), the bounded application sweep (`listCollectionIds` three-tuple, `recursiveDelete` per collection, C6.7 external families, two consecutive empty sweeps → guarding, `outboundLeases` reserved), both work rows and both reconciler state documents with their lease/heartbeat/cursor/backoff fences, the Storage guard (metadata tuple, `versions:true`/`softDeleted:true` tuples, string-generation preconditions) and Firestore-version guard, the Auth reducer (3-second `getUser`, one 10-second `deleteUser`, guarding-after-deadline residual checks), the `authResidualChecks` partition validator, the `ACCOUNT_DELETION_FENCE_WRITERS_V1` export with the shared root-fence assertion, and the `USER_OUTBOUND_LEASE` transaction helper (Decision 6). `functions/taskPlan.js` writes the reconciled `taskReset` marker (Reconciled 9: spec v5 §697 shape plus §5:543 terminal invariants) and the canonical per-epoch `rso1_` record/tombstone with `reset_progress`/`reset_final` receipts; adds `reconcileLegacyTaskReset`, the server-only `users/{uid}/legacyResetMigrations/{rlm1_…}` record created in the same root-fenced transaction, the `PHASE2_RESET_PROTOCOL_MODE` (`compat`|`phase2_required`) cutoff and legacy/Phase-2 bridge under which the shipped client's `resetAllTasks`/`finalizeTaskReset` bytes are unchanged; the read-only `inspectCommittedOperation` six-row projection table; the closed `changeTaskPlan` discriminator; and the root fence on every committing branch. `functions/resolveProvider.js` loses `cacheResolved` and admits only `source:"seeded"` rows; `functions/supportAdmin.js` sends the exact C6.8 support-reply multicast under the lease and fences its four writing tails; `functions/getWorkflowQualifying.js` `submitWorkflowAnswers` gains only the fence call.

Why: S3 (scheduler/migration/deletion/outbound integration) and S4 (the client deletion state machine) can only be proven against a server whose wires, marker, and errors are exact, so this slice replaces the callable that swallows Storage errors with the authority the promise is checked against. It is for the user whose data must be provably gone, and for the S3/S4 owners who consume these bytes.

Boundaries:
- Files you own, S2 server core — because §12.2:1843–1845 and §8:1005 give creation to S2 and S3 only extends:
  - new `functions/accountDeletionFence.js` (S3 later adds outbound-lease ownership of the remaining callers, complete registries, global-scheduler/retained-copy/historical-migration integration, final whole-file review)
  - `functions/index.js`: only the `deleteAccount` replacement, the two reconciler exports, and their imports; `exports.changeTaskPlan = changeTaskPlan` and every pre-existing export line byte-identical; no fence edit to requestConcierge/submitTaskFlow/submitSupportMessage (S3)
  - new `functions/tests/accountDeletionFence.test.js` — S2 creates it holding only S2's families; S3 extends
- Files you own, S2 taskPlan — because §12.2:1833 and spec v5 §8.1:1754/§8.3:1778 name S2 first and S3 adds only the intent claim: `functions/taskPlan.js`; `functions/tests/taskPlan.test.js` (first pass)
- Files you own, sole writer — because §12.2:1848 makes S2 sole writer and S3 verifies without editing: `functions/resolveProvider.js` (cache removal, seeded-only directory, Anthropic lease call); `functions/supportAdmin.js` (C6.8 exact multicast, lease, fence on the adminGetThread/adminReplySupport/adminMarkSeen/adminSetThreadStatus writing tails)
- `functions/getWorkflowQualifying.js`: the fence call inside `submitWorkflowAnswers`' existing root transaction and nothing else — because §12.2:1853 gives each file's fence call to its frozen owner (spec v5 §8.1:1754 = S2) and no other edit is authorized under Decision 1.
- Extend-only test files — because the workflow makes tests the reviewer and S1's families are closed: `Peezy 4.0Tests/TaskPlanDispositionTests.swift` (S1 created; S2 adds only its D15 server-path parity family, never touches S1's two tests); `scripts/test-emulator.sh` (S1 created; S2 adds only a `node` mode for the emulator-backed Node subset). `DurableStoreRecoveryTests.swift`, `TaskSupersessionTests.swift`, `FirebaseEmulatorTestSupport.swift`: read-only for S2 (no S2 family is named for them under Decision 1).
- Read-only for S2 — because each has a frozen other owner: `firestore.rules`, `storage.rules`, `functions/rules-tests/firestoreRules.test.js` (S1 pass done; S3 integrates the deletion boundaries, §12.2:1846); `functions/package.json`, `functions/package-lock.json`, the four `functions/scripts/*.js`, `functions/accountDeletionProviderEvidenceV1.json` (S3 sole writer, §12.2:1847; `@google-cloud/firestore` 7.11.6 already resolves transitively and both C7 module hashes match, so S2 asserts them without editing the manifest); `functions/notifySupport.js` and the §12.2:1849 list (S3); `functions/dispositionTriggers.js`, `functions/notificationIntents.js` (S3, §12.2:1834); `functions/spawnTasks.js`, `functions/taskInteraction.js` (Decision 2); `functions/taskDisposition.js`, `functions/dispositionContract.js`, and every spec v5 §8.2:1763 client path (Decision 1); every Swift production file (`TaskPlanService.swift` is S1's sole DTO writer, spec v5 §8.3:1779; the client reset driver is S3's, tasks/todo.md:421).
- Mirror, don't invent — because C2.3, C3, C6.8, C7, and Reconciled 9 state the request map, four wires, error union, marker, FCM surface, and pins verbatim: S2 copies them and adds no member, code, or branch. The `inspectCommittedOperation`, `reconcileLegacyTaskReset`, and reset-receipt unions are not yet in the contract (Decision 3); S2 codes them only after the contract states them.
- `DELETION_RETRY_REQUIRED` is thrown, never returned — because S1's transport maps it only from `HttpsError.details` and treats a returned map without `kind` as protocol ambiguity that retains durable bytes (Decision 5).
- Legacy client compatibility — because S1's I6 condition ("with S1 landed and S3 absent the user-visible retake path is identical to today") extends to the server: under `compat` the shipped `resetAllTasks {action,operationId,reason}` / `finalizeTaskReset` requests and `{reset,deletedCount,replayed}` responses are byte-identical; a `taskPlan.test.js` case proves it.
- No production, no deploy, no configuration mutation — because §13:2088 stops before deploy and the preamble forbids production: emulator project `demo-peezy-phase1` only; `PHASE2_RESET_PROTOCOL_MODE` is set only in the test environment; the reconcilers are never scheduled; Storage fixtures use an injected bucket adapter because the Storage emulator has no versions, soft delete, holds, or metageneration (Decision 8).
- No new writes outside `ACCOUNT_DELETION_FENCE_WRITERS_V1` — because every registered write must read every owner root and require `accountDeletion` absent; the static check runs over files that exist, and the registry entries for `functions/taskDisposition.js` and `functions/notificationIntents.js` are recorded pending in the close-out (Decision 1).
(Files you own for this slice: the lists above. Everything else is read-only — because two writers on shared deletion state is how we got 36 defects.)

Done:
- These tests exist, were run red against a deliberately broken build, then green: `functions/tests/accountDeletionFence.test.js` (S2 creates; Node 24; offline matrix plus an emulator-backed subset under `firebase emulators:exec --only firestore,auth,storage`, skipped when `FIRESTORE_EMULATOR_HOST` is unset) — C2.7 server set: exact discover/begin/resume/finalize request and wire maps; decoder mirrors; enrollment and overflow; sweeping mutation versus guarding no-write; finalize `replayed:false`/`true`/deleted replay; transport/unknown retry only; signed-out member replay; two-device/lost-response roots; residual guard exact/equal/+1; no completed result or presentation authority before ACCOUNT_DELETED. C4/C5 named: `Firestore 0/1/100/101 and two universal empty sweeps`; `Storage 0/1/100/101 retained-copy and guard boundary` (S2 writes the 0/1/100/101 and guard-boundary cases; S3 appends retained-copy cases, Decision 11); `Auth destination partition is completely falsifiable`; both reconciler state/work fences and takeover at every boundary; historical-migration core entry (fresh user-not-found DATA_DELETED→AUTH_GUARDING creation; migration-only capability); provider-cache graph (`cacheResolved` absent, seeded-only `loadDirectory()`); zero-finalizer export graph; C6.8 multicast exactness (payload, `limit(501)`, 19-code union, two deletion codes); lease helper core (create/delete/prune/expiry/≤64/marker-fenced) with its two S2 callers. Not S2's: `timeouts do not exceed 300 seconds`, `active exports contain no dynamic server log sink`, `sealer refuses nonzero matches, backlog, and nonfinite retention`, package/lock/config/evidence digests (S3). `functions/tests/taskPlan.test.js` (S2 first pass) — every `inspectCommittedOperation` request/path/kind/state/fingerprint/identity/replay/error branch and action-registry/export coverage; MIG-RESET-V1: C7 legacy fingerprint literal `reset1_862fe3…`, parity fixture `phase2-rule-owner` + `20000000-0000-4000-8000-000000000001` → `rlm1_0a4d53a4…`/`rlmreq1_0a4d53a4…`, alias never recomputed, `OPERATION_REUSED`/`LEGACY_RESET_ALIAS_OCCUPIED` collision reducer, replay changes only `replayed`; Reconciled 9 marker: every member missing/surplus/wrong-state, `awaiting_local_reset` ⇒ `targetIndex:4`, no `pageAfterPath`/`lease`, `createdAt <= awaitingLocalResetAt <= updatedAt`, marker present iff a record is active except inside finalize; compat-mode legacy request/response byte parity; closed discriminator rejects unknown actions before any lookup; root fence on every committing branch. `Peezy 4.0Tests/TaskPlanDispositionTests.swift` (extend-only) — D15 server-path parity: Swift `TaskCanonicalV1` reproduces `rso1_`, `rlm1_`, `rlmreq1_`, and `reset1_862fe3…` from the same fixtures the Node tests freeze. D12/D13, D18, D25–D27: not S2's under Decision 1.
- These contract rows are closed: none closed by S2 alone. S2 supplies the server half of `Firestore user root and fence-covered descendants` (sweep core; fence calls in S2-owned writers), `Storage current, noncurrent, soft-deleted, held, copied, late-upload objects` (Storage reconciler and guard), `Auth record and retained Auth copies` (exact finalize wires; Auth reconciler; partition validator), `Provider cache and Firestore local cache` (`resolveProvider.js`), `Notifications, FCM token, installation identity` (C6.8 surface), `Two-device crash-mid-purge and guarding handoff` (two-device/lost-response roots), and `Local durable stores and deletion intent/journals` (the reconciled marker S3's cleanup callbacks and rules predicates reread).
- No new writes outside the fence-writer registry (static check passes over existing files), plus these S2 static gates: zero `onObjectFinalized`/`firebase-functions/v2/storage` reachability from `functions/index.js`; every pre-existing `functions/index.js` export line byte-identical; C7 pins re-verified (`recursive-delete.js` `2a17d8fb…`, `firestore_client_config.json` `2ed7a904…`, 5000/1000, `Commit`/`BatchWrite` 60000, firebase-admin 13.6.0, `@google-cloud/storage` 7.18.0, google-gax 4.6.1, google-auth-library 9.15.1); `deleteAccount` 60 s and `changeTaskPlan` 540 s; `node --check` on every touched `.js`.
- LESSONS.md (`tasks/lessons.md`) updated.

## Sources

- Title / slice name: PHASE2_WORKFLOW_v2.md "Sequence" item 3; manifest §12.2:1870 ordered ledger; template from "Brief template (one per slice)".
- Job: manifest §8:1005 (S2 owns the callable's JS server implementation); §11:1435 (closed request union, absent wire, enrollment/overflow, begin creates DELETING-sweeping plus its work row, resume continues only for an exact member); §11:1437 (literal error union); §11.3:1664–1668 (branch grammar, storage work row, Storage reconciler options/state/lease/cursor); §11.3:1670–1682 (guarding retry, page/backoff rules, data-final after both guards); §11.3:1694–1704 (auth work row, AUTH_GUARDING/ACCOUNT_DELETED, Auth reconciler, pending/guarding reducers, historical entry); §11:1483/1485 (`listCollectionIds` tuple, `recursiveDelete`, external families); §11:1497 (two empty sweeps, finalize preconditions); §11:1499 (zero Storage finalizer); §11:1439 (`cacheResolved` deleted, seeded-only directory); §11:1417/1419 (support-reply FCM surface, `limit(501)`); §11:1441 (`accountDeletionFence.js` owns marker validation, registry export, lease helper; `taskPlan.js` creates `legacyResetMigrations` in the root-fenced transaction); §12.2:1833 (taskPlan.js legacy reconciliation, cutoff, schema, bridge, inspection table, closed discriminator; `changeTaskPlan` export byte-identical); §6.2:624 (`reconcileLegacyTaskReset` request); §6.3:681 (permanent path); §6.4:697 (`PHASE2_RESET_PROTOCOL_MODE`); §7:830/843 (inspection request/result); contract C2.3:63–68, C3:112–120, C6.8:279, Reconciled 9:351; spec v5 §697 (marker and equality projection), §4.4:1068–1073 (`rso1_` record, `STALE_STATE`/`RESET_ACTIVE`), §4.4:1071 (per-epoch fingerprint); S1 transport `Peezy 4.0/MainInterface/Models/TaskPlanService.swift:205–233` (request payload), `:299` (`timePattern`), `:317–355` (exact four-wire decode), `:357–370` (thrown-details mapping).
- Why: PHASE2_WORKFLOW_v2.md preamble line 17; template line 42; PEEZY_STATE.md H42 (current callable's swallowed Storage errors).
- Boundaries: §12.2:1843–1845 (S2 → S3 rows), :1844 (index.js split), :1848 (sole writer), :1853 (fence call by frozen owner), :1846/:1847/:1849/:1834 (other owners); spec v5 §8.1:1754, §8.3:1778/1779/1783/1785, §8.4:1807, §8.2:1763; contract C6.1:163–165, C7:285–287; §13:2088 and preamble line 25 (no deploy/production); tasks/todo.md:421 (I6 condition); spec v5 §9.1:1870 (node@24 v24.19.0), §9.3:1956–1988 (offline Node envelope), §12.3:1876 (insertion before `accountabilityLadder.test.js`; 24-file envelope); §11.2:1656 (trust-anchor constants).
- Done falsifiers: contract C2.7:107 (server set), C4:130–132/136/138/144 (named tests), C5:150/155/157, C6.8:279; §11.3:1698 (`Auth destination partition is completely falsifiable`); §11:1551 and §11.3:1678/1682/1704 (server and reconciler fixtures); C7:294–295 (fingerprint literal, parity fixture); C8 D15:318, MIG-RESET-V1:339; §12.3:1876 (inspection branch coverage).
- Done §11.5 rows: C4:130, 131, 132, 133, 136, 138, 144.
- Done static checks: PHASE2_WORKFLOW_v2.md template line 47; §11:1499; §12.2:1833; C7:285–287; C6.3:171–175 (deadlines).
- Done LESSONS.md: template line 48; preamble line 26.

# PHASE2 AMENDMENT 1 — Owner adjudication of P1-A through P1-U (adopt into PHASE2_EXECUTABLE_SPEC_v5, then re-review)

Authority: this document is the owner's coherent amendment set required by the read-only review verdict. It amends the adopted plan (PHASE2_PLAN.md) and, where noted, the locked INSTITUTION_FLOWS_MASTER_v3.md via its own amendment register. The spec team applies these, produces the revised executable spec, and stops for the new read-only pass at zero P0/P1. No worktree until that pass and explicit adoption of the resulting hash.

## A. Authority amendments — ADOPT VERBATIM as the spec proposed
- **P1-A** adopted: the four client paths (WorkflowService, RentTruckFlow, SetupInternetFlow, SuppliesKitView), owner/order, payload/receipt lifecycle, conditional trigger, Supplies policy-ineligible gate, tests.
- **P1-B** adopted: the complete H56 contract replacement (task_instance_id, generation, flowWriteRevision, callable-validated policy flow writes, direct client writes rules-denied), FlowProgressSession.swift, both Movers model paths and their test path, ordered integrations.
- **P1-C** adopted: all-owner `taskGenerationEpoch` fence, root `activeMoveEventId` authority with its closed producer matrix, strict owner projection, marker-based packing clear. Rationale: a policy-only fence leaves the legacy population able to resurrect deleted tasks after reset — the fence must cover every owner create.
- **P1-D** adopted: stored contradiction is terminal `none` only; `present` lives in the resolver receipt; `unknown` is absence plus deterministic failure.
- **P1-E** adopted: selector sources are exactly TASK / IDENTITY / USER_KNOWLEDGE. The plan's assessment/roster/evidence selectors named authorities that do not exist in the repository; any later source requires its own frozen path, version authority, privacy projection, and tests.

## B. Master-conflict adjudications — owner decisions (both amend the locked master via its register; neither changes a product decision, both scope execution to capabilities that exist)
- **P1-H — WAIT/support surface.** Decision: **amend the master, minimal form**, not build support state. Add to INSTITUTION_FLOWS_MASTER_v3.md's amendment register (A12): *"A6-activation note (Phase 2 execution): until the support/operations phase ships a backed SLA, the WAITING surface keeps all six visible facts and exactly two one-tap exits; 'It's stuck' opens Chat pre-filled with the named office and task context and preserves the owned disposition — it does not write SUPPORT_ACTIVE or show a reply-by promise (a promise nobody is behind would violate A1-5 evidence honesty, which outranks the A6 surface). The second failed cycle still changes channel: the waiting policy's declared alternate channel with its pre-scripted nudge template is mandatory in every waiting_transition (spec: TRY_CHANNEL is required, with template ID), never a third identical wait."* Spec change: `waiting_transitions` gains a required `second_cycle_channel {transition_code, nudge_template_id}`; first renewal same-channel is permitted once only.
- **P1-J — urgent ordering.** Decision: **rank-optional hybrid + master note.** Threshold policy/evidence gains an optional closed `consequence_class` (server-authored enum, validated); the consolidated view sorts rank-first when present, else `threshold_at` then IDs. Master A12 note: *"Consequence ordering activates per-policy as Phase 3 supplies consequence classes; deadline order is the interim, and is itself a consequence proxy (nearest irrecoverable date first)."* No invented ranks.

## C. Executable closures — ADOPT the reviewer's fix; owner picks where a choice was offered
- **P1-F** adopted: authorize `SpawnService.swift` + its four call sites; bind spawn request/token identity to captured generation epoch and source instance; freeze the four staleness results.
- **P1-G** decision: **fence option 1 — captured generation/instance on every legacy mutation surface.** Rationale: nonaliasing regenerated IDs would break identity expectations across the app; passive regenerated legacy rows would silently degrade users who reset. Authorize every affected caller/server/rules path; test delayed update/delete/H54/receipt replay across reset.
- **P1-I** adopted: server-owned indexed threshold projection + an independent threshold scan creating threshold-cause attention/wake/intent with replay, race, routing, and offline tests. (This is what makes the master's immediate-recovery rule executable.)
- **P1-K** adopted: freeze the complete packing-clear callable contract (action, request, durable operation record, marker↔operation transitions, fingerprint, progress/final receipts, replay/adoption/error map, loss-after-every-page and terminal-adoption tests).
- **P1-L** adopted: name all five policy H56/content actions in the closed unions with exact maps, revisions, fingerprints, receipts, replay; notes/quotes get an explicit last-write-wins-by-revision concurrency rule with the losing write returned as a typed conflict, tested.
- **P1-M** adopted: freeze the root marker map; **one casing everywhere (lowercase)** with an explicit operation↔marker↔receipt equality mapping; fixture every phase/replay.
- **P1-N** adopted: complete outer envelopes for all four durable files (installation/auth/session), signed-out shape, SHA-256 with exact preimage and excluded-member rule (sidecar), quarantine name/behavior, atomic coverage.
- **P1-O** adopted: reconciliation-cancel record/phase carrying both namespaces, immutable canonical request bytes/hash, evidence, retry/application states, terminal cleanup; crash/lost-response tests for auth rotation and takeover.
- **P1-P** adopted: CAS against the captured dispatch namespace; deterministic `AUTH_REQUIRED` atomically returns the record to RECEIVED and resumes on a valid durable current namespace; both named tests.
- **P1-Q** adopted: `TasksStore` namespace/readiness/revision qualified by UID + listener token; every callback guarded; late-A and stop/start-reuse tests.
- **P1-R** decision: **authorize `AppRootView.swift`** and UID/generation-guard it directly (S4 ordered follow-on). Contorting the fix inside already-authorized paths would leave the race's source untouched.
- **P1-S** adopted: conservative aggregate budget for the 20-child terminal transaction — sum of canonical encoded sizes plus a frozen per-document index estimate must stay ≤ 8 MiB (20% headroom under the documented 10 MiB limit) computed before mutation; over-budget fails the command with a typed error (no partial materialization); maximum-size boundary tests. Redesign only if the budget cannot hold the real catalog's worst case — prove with the fixture first.
- **P1-T** adopted: a separate Firestore/task canonical domain with finite-double emission (negative-zero and exponent/decimal normalization, JS/Swift parity fixtures); policy canonicalization keeps its stricter integer domain.
- **P1-U** decision: **current-code-preserving option.** Every a2 replacement carries normalized subject, institution ID, supersedes, and `spawnedFrom.id`; §2.2/§4.4 declare the explicit H54 exception; validator and fixtures align. Rationale: preserves Phase 1's shipped behavior and keeps provenance uniform for Phase 3's consumers.

## D. Process
1. Apply A–C to the executable spec; update its §8.6 register so every P1 ID maps to its adopted resolution.
2. Produce the master's A12 additions from B as a one-line-each register edit (text above is normative); the master file edit itself ships with this amendment, not with Phase 3.
3. New read-only adversarial pass on the revised spec. STOP at any P0/P1; report all findings together. Zero P0/P1 → present the new spec hash for owner adoption; only then does the S1 worktree exist.
4. Inventory additions that fit the frozen model append to the annex per the adopted plan; they are not new review rounds.

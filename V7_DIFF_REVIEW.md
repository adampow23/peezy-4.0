# V7 DIFF REVIEW — PHASE2_REPLACEMENT_MANIFEST_v7.md (diff-scoped, read-only)

Artifact: `PHASE2_REPLACEMENT_MANIFEST_v7.md` · sha256 `d7c83fccc577264702e3b26905ab9e82d5fa36a33c9b1a87f6b59d32e5a0351a` (verified before review) · 535,128 bytes · 1,894 lines · committed as `6bfb6e8` ("Add PHASE2_REPLACEMENT_MANIFEST_v7 (sha256 d7c83fcc)", manifest file only, matching the v6 commit shape).
Preimage: `PHASE2_REPLACEMENT_MANIFEST_v6.md` at `76e98c5` (1,826 lines). Diff: 68 hunks; 69 lines carry `[v7: …]` markers; every marker ID is one of the 38 finding IDs.
Instruction: `V7_INSTRUCTION.md` (untracked). Reviews: `~/Downloads/peezy-reports/V6_PASS_A.md`; `V6_PASS_B.md` was **not present** in `~/Downloads/peezy-reports/` — the only copy is `V6_PASS_B.md.rtf` at the repo root, converted with `textutil -convert txt` into the session scratchpad and used as Pass B. Findings in both are treated as data.
Frozen spec: `docs/plans/PHASE2_EXECUTABLE_SPEC_v5.md` sha256 `e02ef938…` (verified).
Scope statement: no file in the repository was edited; no worktree, tests, xcodebuild, node tests, or Firebase calls. The only write is this report. Implementation files were opened read-only to test feasibility of v7's static gates.

Result summary: 29 CLOSED · 2 NOT CLOSED · 7 REGRESSED. Eight new P1 (all textual contradictions or unreachable transitions, each with anchors) and six P2. Under §13 item 7 ("Any P0/P1 reopens this manifest") v7 is not ready.

---

## 1. Per-finding verification (38 IDs)

Legend: CLOSED = fix present as V7_INSTRUCTION specified and no new contradiction with adjacent frozen text. NOT CLOSED = instruction not (fully) applied. REGRESSED = fix applied but it contradicts adjacent frozen or v7 text. New findings are cross-referenced as V7-nn (section 5).

| ID | Result | v7 §:line | Verification |
|---|---|---|---|
| A-1 | CLOSED | §2.2:142; §2.5:280 | `MIG-TRIGGER-V1` accepts exactly `dateAfterAt`, `dateAfterPath`, `eventTaskAfterPath` and/or legacy lease `{runId,acquiredAt,expiresAt}`, deletes both in the acquisition transaction, refuses any other unknown key with zero write; fixtures list each residue shape alone, both, neither, foreign key alongside residue. §2.2:59 initialization reads the trigger-state document without key validation, so residue keys do not block initialization. |
| A-2 | NOT CLOSED | §2.2:142/280 vs §2.2:59, §2.2:61, §3.1:308 | The legacy lease is removed only "on first Phase 2 acquisition". Two frozen steps that precede first acquisition still refuse it at the same literal path `phase1System/dispositionTriggerLease` (§2.2:61): initialization "requires the lease absent" and "nonabsent lease … blocks rollout" (§2.2:59); MIG-EVENT acquisition "A present malformed scheduler lease fails closed" (§3.1:308). A crashed Phase-1 lease therefore blocks rollout before the migration branch can run. → V7-07 (P1). |
| A-3 | CLOSED | §12.2:1661, 1668, 1674; §13:1883, 1892 | Recount independently reproduced: 56 unique frozen + 30 unique additions = 86 (section 2). The four already-frozen paths are named as not repeated (L1668). "18 newly authorized direct-caller paths" (L1672) reproduced: exactly 18 of the 30 additions contain `Firestore.firestore()`. |
| A-4 | REGRESSED | §12.2:1643, 1657, 1664, 1665, 1670, 1672; §13:1881 | The four WIP paths are struck and per-file relocation is stated as instructed. But unchanged frozen text still binds those files: (a) §11:1281 lists "process success/error plus `onInventoryRoomWritten` in `functions/processInventory.js`" in `ACCOUNT_DELETION_FENCE_WRITERS_V1`, §11:1283 lists "every Anthropic call in `functions/processInventory.js`" in `USER_OUTBOUND_PROVIDERS_V1` ("dominated by the shared lease helper"), and §12.2:1659 says every registry path's owner "adds that file's shared-fence call" — while §12.2:1643/1657 forbid editing it; (b) §11:1331/1333 require `InventorySessionManager`, `InventoryCameraView.pendingNarrationTranscript`, `InventorySessionManager.pendingNarration` to "hold only" actor-issued lease handles and `NarrationService.start` to require an actor lease — property/API changes inside files that "receive no Phase 2 edit" (L1670) and are "verification-only" (L1672); (c) §12.2:1672's `rg` gate requires zero production `Firestore.firestore()` outside two files, but `Peezy 4.0/Inventory/Models/InventorySessionManager.swift` holds six occurrences at HEAD and is now outside the authorized union (36 production files scanned; it is the only one outside). → V7-05 (P1). |
| A-5 | NOT CLOSED (partial) | §12.2:1641, 1642, 1674, 1676 relabelled; §12.2:1643 residual | `taskDisposition.js`/`notificationIntents.js` are "new" in the S2/S3 bullets, `AppRootAuthRaceTests.swift` and `HandoffSessionStore.swift` are "new" (L1674/1676). The S2/S3-shared bullet (L1643, one of the three lines Pass A cited) still opens "existing `functions/index.js`, …, `functions/taskDisposition.js`, …, `functions/notificationIntents.js`, …". Same-section label conflict. → V7-10 (P2). |
| A-6 | REGRESSED | §1:30; §2.1:38 (closed part) vs §12.1:1629–1633; §2.5:276 | Line-number references into the master are replaced by section anchors plus quoted sentences; all three quoted strings are verbatim in the master at HEAD (§A2 line 51; §8.5 line 434), and the pinned sha `945902…` equals `git show 76e98c5:docs/plans/files/INSTITUTION_FLOWS_MASTER_v3.md`. But the frozen spec pins the same file as "with A12-11/A12-12, SHA-256 `80e6dbd3…`" (spec v5 L10 and §9.5 L2098 = the current worktree file), and v7 §12.1:1629 still says "A12-1 through A12-12 change no locked decision" with A12-13 appended after them — A12-11/A12-12 do not exist at the pinned HEAD baseline (HEAD master ends at A12-10; worktree lines 188–189 add them). §2.5:276 still says "replace item 9 in full" of `PEEZY_STATE_REGEN_SPEC.md`; at HEAD that file has items 1–8 only (item 9 exists only in the worktree, line 55). Two incompatible pins for one authority file. → V7-06 (P1). |
| A-7 | CLOSED | §12.2:1666, 1674; §8:995 | Literal new paths `Peezy 4.0/Auth/GoogleIdentityAuthority.swift` and `Peezy 4.0/MainInterface/Models/NotificationIdentityAuthority.swift` declared; both scans assigned to `AppRootAuthRaceTests`. Feasibility: tree `GIDSignIn` call sites are `AuthViewModel.swift:128,137` and `PeezyV1App.swift:173`, both authorized for exactly that routing/forwarding; no `UNUserNotificationCenter`/`Messaging` deletion call exists in the tree (`SupportChatView.swift:326` is `setBadgeCount(0)`, not a deletion), so both static scans are satisfiable. |
| A-8 | CLOSED | §11:1257; §13:1881 | `data:{thread:"support"}` retained beside the category; routing fixture asserts both; removal is a named deferral gated on a client-version floor. §8:999's "spoofed legacy `thread:"support"` key without the fixed category" rule remains consistent (new clients key on category). No residual "no `data` member" sentence in v7. |
| A-9 | CLOSED | §12.4:1716 | `firestore.rules:141-143` is `allow read, write: if isOwner(userId)`; v7 now says Phase 2 "changes the current owner-readable baseline to deny client read/list". |
| A-10 | CLOSED | §12.4:1720 | `storage.rules:4-6` is owner-auth only; v7 now says existing-root/no-marker predicates are newly added, not baseline. |
| A-11 | CLOSED | §6.3:681 | "forbidden from the union … placement clarification rather than removal". |
| A-12 | CLOSED | §12.1:1625 | "pre-ship checklist" defined as the named review record in the §12.1 evidence packet; no new path. |
| A-13 | CLOSED | §11.1:1430 | Release blocked if SIL/link-map cannot exclude a `#Preview` sink; the four files stay unauthorized. |
| A-14 | CLOSED | §12.2:1643 | "plus existing `functions/rules-tests/firestoreRules.test.js`". |
| B-01 | CLOSED | §8:981, 991; §11.3:1482 | Exact `account_deletion_queued` snapshot with the v6 L1454 copy and `availableActions:["retry"]`; stated not a member of `account_deletion_recovery_unavailable` (L991); §11.3 references it by name; decode-rejection fixture named (L1482). |
| B-02 | REGRESSED | §8:977; §11.3:1506, 1512 | `authGuarding(…)`/`accountDeleted(…)` branches with exact wire maps added; second-device/lost-response fixture named. Two contradictions: (i) L977 "A prepared-intent reducer … `authGuarding` … enters `auth_finalize_dispatched`", but §11:1311 makes `auth_finalize_dispatched` require all eight acks and prepared requires `acks:[]` (§11:1307 also: only after eight owners plus both barriers). (ii) Neither L977 nor L1506 states the member/overflow predicate for the new branches while §11:1307 says "Member/overflow predicates remain exact" and §11:1275 requires auth for discover/begin and membership for resume; at `AUTH_GUARDING` the Auth user is already absent, so a never-enrolled second device can satisfy neither predicate and the L977 "second device … leave `prepared`" fixture is unsatisfiable for it. → V7-02, V7-03 (P1). |
| B-03 | REGRESSED | §8:991; §11:1307, 1311, 1355 | The `definitivelyDeleted → local_detaching(auth_deleted)` completion path is removed; `auth_deleted` legal only after `ACCOUNT_DELETED`; §11.3 supersession honored. But L991 now retains a capability-invalid + `definitivelyDeleted` intent "until an exact B-02 … branch", which a non-member cannot obtain (L1275), and §8:989 keeps every other UID at `ACCOUNT_DELETION_BUSY` while a `prepared` target exists — an unbounded device-wide block with no named exit, contradicting L991's own "no ALREADY→recover gap and no registry-full blocked state exist". → V7-03 (P1). |
| B-04 | CLOSED | §11:1307 | Finalize precondition names "absent `accountDeletionWork` data-work row"; creates/adopts `accountDeletionAuthWork`; its absence is not a precondition. Consistent with §11.3:1499. |
| B-05 | REGRESSED | §8:983, 985, 989; §11:1311, 1355; §11.3:1498, 1516; §12.2:1672; §11.5:1613 | Option A is implemented as instructed: gate union gains `guarding(uid,authGuardAfter)`; loading/active/blocked stay global; guarding forbids only the deleted UID; `ACCOUNT_DELETION_GUARDING` presentation with `authGuardAfter` and the exact copy; completion only at `ACCOUNT_DELETED`; different-UID `startDeletion` no longer BUSY (Option-B handoff); all four instructed fixtures at L1516. Contradiction: the only client verb at `auth_finalize_dispatched` is finalize (§8:991), finalize "against DATA_DELETED/AUTH_GUARDING returns retry" (§11.3:1512; §11:1275; §11.3:1499), and the `AUTH_GUARDING` branch is encoded only "On discover, begin, or resume" (§8:977; §11.3:1506). The deleting device therefore never stores `authGuardAfter`, never enters `guarding`, and never projects `ACCOUNT_DELETION_GUARDING`; L1355/L1498's "exact `AUTH_GUARDING` stores `authGuardAfter`" and the L1516 relaunch fixture presuppose an unreachable state. → V7-01 (P1). Also P2: §8:983's post-transition list `clear|active|blocked` and the §8:999/§8:1008 gate-state enumerations omit `guarding` (V7-11); no crash fixture at the Option-B unlink boundary (V7-09, section 4). |
| B-06 | CLOSED (P2 clarification) | §11.4:1585 | Ownership partitioned: reconcilers close Storage/Firestore-version guards; migration owns only its historical fresh user-not-found `DATA_DELETED→AUTH_GUARDING`; Auth reconciler alone closes `AUTH_GUARDING→ACCOUNT_DELETED`. Disjoint by row state with §11.3:1509/1513. |
| B-07 | CLOSED | §11.4:1575; §12.4:1714 | Extractor accepts `qev1_`, `qevu1_`, `qev2_`; UID from `users/{uid}/events/{eventId}` path only; runtime scrub unchanged; foreign family blocks, body differences do not. |
| B-08 | CLOSED | §11.4:1587 | 100-per-transaction document-ID-ordered pages, reread-before-delete, drift returns to reducing, checkpoint deleted last after `limit(1)` zero proof; fixtures 0/1/100/101/500 and crash after every page boundary. |
| B-09 | CLOSED, one P2 | §11.2:1502; §12.3:1684; §11.5:1600 | Firebase branch covers `auth_user`; the five other kinds use the §11.2 backup-destination pattern (absence + retention elapsed, bound into signed evidence); uncheckable kinds removed with a stated reason; named test `Auth destination partition is completely falsifiable`. Residual ambiguity: `authResidualChecks` still has exactly two branch shapes and must "form an exact partition of every accepted Auth-destination ordinal"; the absence-pattern check has no branch shape, so it is consistent only if absence-pattern destinations are not accepted ordinals — but L1502 says "its accepted finite `retentionSeconds`". → V7-12 (P2). |
| B-10 | CLOSED | §2.2:132; §2.4:196 | `refusalCapacity` = 100 (contiguous, admission 200) or 200 (catch-up, admission 400); consistent with §2.2:136 `thresholdAdmissionCapacity = catchUp ? 400 : 200`. |
| B-11 | CLOSED | §5:478, 480 | Conformance sentences for `DurableStoreRecoveryCoordinator`→`ResetOperationRegistry` and `LocalPrivacyPurgeCoordinator`→`DailyDoseLocalStore`; sole-owner claims stand. |
| B-12 | CLOSED, one P2 | §8:995 | Eleven-member static order including `roomCaptureArtifactOwner` and `firestoreRuntimeOwner`; prohibition and `ObjectIdentifier` fixtures cover eleven. Type name `FirestoreRuntimeOwner` appears only at L995; §11 names the thing only as "the Firestore runtime" inside `LocalPrivacyPurgeCoordinator.swift` (L1665) and `FirestoreRuntimeGeneration`. → V7-14 (P2 naming). |
| B-13 | REGRESSED | §12.3:1684; §11.5:1610 vs §11:1255 | Named test `timeouts do not exceed 300 seconds` "source-scanning every deployed function `timeoutSeconds` … failing above 300". The claim it falsifies (§11:1255) covers only "participating" functions. Committed `changeTaskPlan` is `timeoutSeconds: 540` (`functions/taskPlan.js:812`), is not in `USER_OUTBOUND_PROVIDERS_V1`, and no v7 or spec v5 clause pins or changes it (v7 pins only 270 at L52/L1478/L1508; the scheduler's current 540 at `dispositionTriggers.js:477` is superseded by L52). The fixture as worded fails against the pinned-unchanged tree. → V7-08 (P1, narrow). |
| B-14 | CLOSED | §12.3:1686; §11.5:1606 | Named source-scan test. Tree check: every UID-interpolated preference key currently in production Swift is in the ten-key registry (`RetakeAssessmentCoordinator.swift:14`; `PeezyHomeViewModel.swift:991–1011`; `InventoryCameraView.swift:54,61`; `DailyDoseEngine.swift:55–57` via prefix), so the scan is satisfiable without touching a WIP file. |
| B-15 | CLOSED | §11.1:1428; §12.3:1684, 1686; §11.5:1609 | Release-call-graph + adversarial `NSError` fixtures → `DurableStoreRecoveryTests`; active-export walk → `accountDeletionFence.test.js`; no new file. |
| B-16 | CLOSED | §11.2:1470; §12.3:1684 | `sealer refuses nonzero matches, backlog, and nonfinite retention`. |
| B-17 | CLOSED (P2 clarification) | §11.1:1424; §11.5:1608 | Sentence added as instructed; "collection is disabled per §11.1" rests on `FirebaseCrashlyticsCollectionEnabled = false` at L1416 (Sessions/MetricKit ride on Crashlytics collection). |
| B-18 | CLOSED | §12.3:1686; §11.5:1604 | `Firebase Auth keychain item is absent after terminal detach`. |
| B-19 | CLOSED | §11.1:1426; §11.5:1615 | Persisted OS unified-log entries excluded from the promise. |
| B-20 | CLOSED, one P2 | §7:965 | Over-cap mapping stated; but it writes `present:true` while the §7 schema members are `targetPresent` / `quarantinePresent` (L889–890). → V7-13 (P2 naming). |
| B-21 | REGRESSED | §11.3:1482 vs §11:1275, 1307 | The finding's target sentence, "For a valid DELETING, … a distinct nonmember at count below 64 is appended" (v7 L1275), is unchanged. Instead the no-write sentence was moved from DELETING-guarding to DELETING-sweeping: L1482 now says that during DELETING-sweeping discover/begin/resume "changes no capability, marker, guard, work, or local-intent byte". That contradicts L1275 (append during DELETING) and L1307 ("Two consecutive empty application sweeps perform only §11.3's DELETING-sweeping→DELETING-guarding transaction and return the exact queued retry branch" — those sweeps and that transition are byte changes performed by the same verbs). The guarding no-write rule that B-21 relied on no longer exists. → V7-04 (P1). |
| B-22 | CLOSED | §11:1247 | Tombstone retains UID doc ID + capability-proof hashes; stated as an explicit product-promise limit. |
| B-23 | CLOSED | §2.4:178 | Fingerprint preimage intentionally has no `domain`; no byte change. |
| B-24 | CLOSED | §8:969 | `AppRouteInbox` named sole conformer of `RouteAccountDeletionPurging`. |

---

## 2. §12.2 authorized client path recount (from the literal list)

Method: backticked `.swift`/`.plist`/`.pbxproj` paths in frozen spec v5 §8.2 rows (L1760–1770) unioned with backticked `Peezy 4.0/…` paths in the four v7 addition bullets (L1662–1666).

| Quantity | Count |
|---|---|
| Frozen §8.2 entries / unique | 58 / 56 |
| Frozen duplicates | `Peezy 4.0/MainInterface/Views/PeezyMainContainer.swift` (S4 + S7), `Peezy 4.0/Tasks/FlowEngine/FlowExitControl.swift` (S5 + S8) |
| v7 addition entries / unique | 30 / 30 |
| Additions already in frozen §8.2 | 0 |
| Union | **86** (matches L1661 "exactly 30" and L1674 "86") |
| Additions containing `Firestore.firestore()` | 18 (matches L1672) |
| Production files with `Firestore.firestore()` | 36; 35 inside the union; 1 outside: `Peezy 4.0/Inventory/Models/InventorySessionManager.swift` (see V7-05c) |

Bare basenames in §12.2 (`GoogleIdentityAuthority.swift`, `NotificationIdentityAuthority.swift` at L1666; the four frozen names at L1668) are references to full paths already counted, not extra entries. v6's 91 → v7's 86 = 91 − 4 (A-3 duplicates) − 3 (A-4 client WIP) + 2 (A-7 new files).

---

## 3. §13 gate items 1–6, independent rerun

| Item | Result | Evidence |
|---|---|---|
| 1 ID set / path closure | PASS | Table has 36 rows, first column unique, set equality with D1–D15, D18–D32, B1–B4, MIG-EVENT-V1, MIG-RESET-V1; no slash/comma/range IDs; every row cites one section; all 21 distinct backticked paths occur in spec v5 §8.1/§8.2/§8.4/§9 or v7 §12.1–§12.3; D32 → spec §9.4 only. |
| 2 schema/crash fixtures | PASS with 2 gaps (document level; no fixture code exists) | New branches with named missing/surplus/unknown fixtures: `MIG-TRIGGER-V1` (L142), `account_deletion_queued` (L1482), `authGuarding`/`accountDeleted` (L977), `authGuardAfter?` payload branches (L1311), paged cleanup (L1587). Gaps: no crash fixture between the Option-B unlink and the new prepared persist (V7-09); no named decode fixture for the `ACCOUNT_DELETION_GUARDING` presentation map at L983. |
| 3 codec/decoder pins | PASS | `@google-cloud/firestore` 7.11.6 (`functions/package-lock.json:1412-1414`), `@google-cloud/storage` 7.18.0 (`:1463-1465`); `firestore_client_config.json` sha256 `2ed7a904…` equals L298/L697, Commit/BatchWrite `timeout_millis` 60000; recomputed `rlm1_0a4d53a438873090f69e4255426310b0feb4cfd6` and `rlmreq1_0a4d53a4…827ad8e1` from `{account_uid,legacy_operation_id}` sorted-key compact JSON (L620) and `reset1_862fe3fc…f04e94a1` from the committed `taskPlan.js:166` derivation `{kind:"reset",reason:"retake_assessment"}` (L573) — all exact; `migrationUUID` zero in spec v5 and only in the gate text of v7 (L1887); UUIDv5 only in the gate text; `taskPlanOperations/{migrationId}` only as decoy/gate (L622, L774, L1887). |
| 4 §7.4 registry | PASS | Spec v5 §7.4 fenced block (L1533–1742) is 4,432 bytes / `a7a432ec…`, equals its own `JSON.stringify(v,null,2)+"\n"` normalization, 3 indexes / 28 overrides, `workflowSubmissions` wildcard present; appending the eight §12.4 objects yields 4 indexes / 35 overrides / 5,872 bytes / `a6de8daf…` — both equal L1690/L1712. |
| 5 mechanical counts | PASS | Client paths 86 = L1674 (v6's 91 failure is cleared); Swift test files 21 → 22 (spec §8.4 has 21; + `DurableStoreRecoveryTests.swift`); `-only-testing` 22 → 23 and set `E` 22 → 23 with the same insertion point (spec L1893–1914, L1929); Node `--test` 23 → 24 (spec §9.3 L12–34 + `accountDeletionFence.test.js`); 25 unique Node/rules files with `firestoreRules.test.js` — all equal §12.3. |
| 6 grep bans | PASS | Master timing clauses present at HEAD 51/434 and worktree 51/436; v7 cites anchors and quoted text, not lines; `PROTOTYPE_MISMATCH` only in the withdrawal sentence (L202); policy-bearing `planChangeHistory[ordinary].evidence_ids` forbidden (L1047), policy-absent positive path exactly once (L1093) plus gate text; eviction mentions are the permitted no-eviction assertions and the frozen AppRouteInbox rule (L557, L561, L794, L993, L1005); no wall-clock guarantee outside the disclaimers (L40, L1633, L1874). |

---

## 4. Item 5 — crash fixture at the Option-B guarding-intent unlink

**Absent → V7-09 (P2).** §8:989 sequences three durable steps for a different-UID `startDeletion` during guarding: "atomically CAS-unlinks/fsyncs only that local guarding intent, reclassifies the gate for the new signed UID, and persists the new UID's ordinary prepared intent before its first await." The only Option-B fixtures are §11.3:1516 (handoff instead of BUSY; relaunch *with* the first guarding intent present), §11.3:1498, and the §11.5:1613 row; §11:1311's "guarding relaunch" and §11:1355's generic "every … unlink … crash is total" name no kill between the unlink and the new prepared persist, nor the recovery from that window (no intent on disk, gate reclassified, retired UID recoverable only from server root/work per L989).

---

## 5. New findings

Rule applied: a P1 is a contradiction between two normative sentences or an unreachable/unfalsifiable requirement, each with anchors. No rewrites proposed.

### P1

- **V7-01 · B-05 · §8:991 + §11:1275 + §11.3:1499/1512 vs §8:977 + §11.3:1506 + §11:1355 + §11.3:1498** · The guarding gate is unreachable on the deleting device. At `auth_finalize_dispatched` "member authority calls finalize" (L991; "Retry … selects the sole verb from durable state"); finalize at `AUTH_GUARDING` "return[s] only retry" (L1275, L1512) and `DELETION_RETRY_REQUIRED` "regardless of … canonical user-not-found" (L1499); the `AUTH_GUARDING` branch exists only "On discover, begin, or resume" (L977, L1506). Hence `authGuardAfter` is never stored, `guarding(uid,authGuardAfter)` never occurs, `ACCOUNT_DELETION_GUARDING` is never projected, and the L1516 "relaunch with the first guarding intent and exact `authGuardAfter`" fixture presupposes a state no transition produces. Item 1 of the instruction is therefore not achieved in effect.
- **V7-02 · B-02 · §8:977 vs §11:1311, §11:1307** · "A prepared-intent reducer receiving either exact branch atomically leaves `prepared`: `authGuarding` … enters `auth_finalize_dispatched`" contradicts "Auth_finalize_dispatched … requires all eight acks" (L1311) and "Only after the client has durably purged/acked all eight local owners, the preference barrier, and the client-telemetry barrier does it enter auth_finalize_dispatched" (L1307); `prepared` requires `acks:[]`. The `accountDeleted` sentence routes through the purge; the `authGuarding` sentence does not.
- **V7-03 · B-02/B-03 · §8:991 + §11:1275 + §8:989** · Stranded non-member. On capability-invalid + `definitivelyDeleted`, L991 "retains the intent and may advance only after an exact B-02 … branch". A device never enrolled before the Auth user was deleted cannot obtain that branch: discover/begin "require authentication and exact `request.auth.uid === request.uid`" and resume "only continues when the recomputed capability is an exact member" (L1275); §11.3:1506 adds no predicate exception and L1307 says "Member/overflow predicates remain exact". Meanwhile L989 returns `ACCOUNT_DELETION_BUSY` to every other UID "While a durable deletion target exists in `prepared|…`" and keeps "every ordinary nondeletion operation … globally gated regardless of UID". Result: an unbounded device-wide block with no named exit — the same defect class Item 1 was decided to remove — and a contradiction with L991's "no ALREADY→recover gap and no registry-full blocked state exist". v6 had an exit here (the wrong one B-03 removed); v7 removed it without replacement.
- **V7-04 · B-21 · §11.3:1482 vs §11:1275, §11:1307** · "During exact DELETING-sweeping, discover/begin/resume … changes no capability, marker, guard, work, or local-intent byte" contradicts L1275 ("For a valid DELETING … a distinct nonmember at count below 64 is appended") and L1307 (the two empty application sweeps and the DELETING-sweeping→DELETING-guarding transaction are performed by those verbs). Under L1482 as written the sweeping phase can never advance. The B-21 target sentence itself is unchanged.
- **V7-05 · A-4 · §12.2:1643/1657/1670/1672 vs §11:1281, 1283, 1331, 1333; §12.2:1659** · Three contradictions from striking the WIP files without amending the frozen sentences that bind them: (a) `functions/processInventory.js` remains a member of `ACCOUNT_DELETION_FENCE_WRITERS_V1` (L1281: "process success/error plus `onInventoryRoomWritten`") and `USER_OUTBOUND_PROVIDERS_V1` (L1283: "every Anthropic call … dominated by the shared lease helper"), and L1659 requires each registry path's owner to add "that file's shared-fence call"; L1643/L1657 forbid any edit. The "active-export adapter" of L1670 is not a construct L1281/L1283's static analysis recognises as dominating writes inside the file. (b) L1331/L1333 require `InventorySessionManager`, `InventoryCameraView.pendingNarrationTranscript`, `InventorySessionManager.pendingNarration` to hold only actor-issued lease handles and `NarrationService.start` to require an actor lease — type/API changes in files that "receive no Phase 2 edit". (c) L1672's `rg` gate ("zero production `Firestore.firestore()` occurrence outside `LocalPrivacyPurgeCoordinator.swift` and the one initialization line in `PeezyV1App.swift`") is unsatisfiable: `InventorySessionManager.swift` has six occurrences at HEAD and is "verification-only". Rule 7 (design change → decision packet) applied to this item and was not invoked.
- **V7-06 · A-6 · §1:30 vs spec v5 L10/L2098, §12.1:1629–1633, §2.5:276** · Two pins for one file: v7 pins the master "as read at HEAD" = `945902…` (verified), while the frozen spec pins "with A12-11/A12-12, SHA-256 `80e6dbd3…`" (= the current worktree file; spec §9.5 L2098 "166,983"). v7 §12.1 then instructs replacing the A12 preamble with "A12-1 through A12-12 change no locked decision" and appending A12-13 — A12-11/A12-12 are absent at the pinned HEAD (HEAD master ends at A12-10). §2.5:276 "replace item 9 in full" targets a `PEEZY_STATE_REGEN_SPEC.md` item that exists only in the worktree (HEAD has items 1–8). Applying v7 at either baseline trips §1.5.
- **V7-07 · A-2 · §2.2:142 vs §2.2:59, §2.2:61, §3.1:308** · The legacy lease at `phase1System/dispositionTriggerLease` is migrated only "on first Phase 2 acquisition", but initialization (schedule disabled) "requires the lease absent" and a "nonabsent lease … blocks rollout" (L59), and MIG-EVENT acquisition "A present malformed scheduler lease fails closed" (L308). Both precede first acquisition, so a crashed Phase-1 lease still blocks with no clause authorizing its removal.
- **V7-08 · B-13 · §12.3:1684 vs §11:1255 and `functions/taskPlan.js:812`** · The named test fails "every deployed function `timeoutSeconds` … above 300", but the claim being falsified (L1255) is scoped to "participating" functions, and `changeTaskPlan` is deployed at 540 with no v7/spec clause pinning or changing it. The fixture cannot pass against the tree v7 otherwise leaves unchanged; either the test scope or the timeout is unspecified.

### P2

- **V7-09 · Item 5 · §8:989** · No crash fixture between the Option-B guarding-intent unlink and the new UID's prepared-intent persist (section 4).
- **V7-10 · A-5 · §12.2:1643** · "existing …`functions/taskDisposition.js`… `functions/notificationIntents.js`" remains in the S2/S3-shared bullet while L1641/L1642 in the same section label them new.
- **V7-11 · B-05 · §8:983, 999, 1008** · Closed enumerations predating `guarding`: "before changing to `clear|active|blocked`" (L983); support-tap opener states "loading, blocked, active-same-UID, active-other-UID, active-with-nil-current-UID" (L999) and "deletion gate is loading/blocked/active" (L1008) omit guarding-same/other-UID, unlike L1672 which was updated.
- **V7-12 · B-09 · §11.2:1502** · Absence-plus-retention check has no `authResidualChecks` branch shape; the unchanged partition sentence requires every accepted ordinal in some check; consistent only if such destinations are not accepted ordinals, which "its accepted finite `retentionSeconds`" contradicts.
- **V7-13 · B-20 · §7:965 vs §7:889–890** · `present:true` is not a schema member; the members are `targetPresent` / `quarantinePresent`.
- **V7-14 · B-12 · §8:995** · `FirestoreRuntimeOwner` is a type name introduced only here; §11 never declares an actor by that name (only "the Firestore runtime" in `LocalPrivacyPurgeCoordinator.swift`, L1665, and `FirestoreRuntimeGeneration`).

---

## 6. Verified with no finding

- Every `[v7: …]` marker line lies inside the section the resolution claims; §11.1 (L1412–1431) holds B-15/B-17/B-19/A-13; §11.2 (L1432–1471) holds B-16; §11.5 is a new subsection at L1593–1617 with 15 rows, every cell citing a section, the six formerly unassigned cells naming Item 5 tests, the Sessions/MetricKit cell reading exactly as instructed, and the B-19 exclusion sentence at L1615.
- Rule 5 of the instruction holds: no new Swift test file or suite; every new fixture is placed in `DurableStoreRecoveryTests`, `AppRootAuthRaceTests`, or `accountDeletionFence.test.js`.
- Rule 4 holds for this review: implementation files were read only; `git status` shows no new modification from this session (the v7 commit was requested).
- A-6 quoted anchors are byte-verbatim at HEAD (three strings, 758/80/89 chars).
- §12.4 A-9/A-10 baselines match `firestore.rules` and `storage.rules` at HEAD.

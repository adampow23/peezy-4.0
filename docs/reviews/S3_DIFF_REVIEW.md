# S3 diff review (Sol, gpt-5.6-sol, read-only; thread 01a07794-2dc9-7b20-8f18-173e18f7e372)

Packet: ~/Downloads/peezy-reports/S3_GATE/ (base 1a48e0f, code head eb0cc81, close-out db4da6d, Swift-pass fix 1092ea7; contract fb6a8bf6). Round cap 3; Sol is read-only every round; the builder is the final arbiter and every finding is fixed or dispositioned in tasks/todo.md.

## Round 1 — Sol (verbatim)

The diff is not safe to ship. I found 13 blockers and 2 high-severity defects.

1. **BLOCKER** — Scheduler refusal bookkeeping writes beneath a deleting owner without reading the owner root. [dispositionTriggers.js:1727](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1727>), [dispositionTriggers.js:1735](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1735>), [dispositionTriggers.js:1766](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1766>), [dispositionTriggers.js:1821](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1821>). Contract: [C6.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:355>). Counterexample: `users/u.accountDeletion` is present and a due task is admitted; the reducer throws `ACCOUNT_DELETION_FENCED`, but `refusalReasonOf` converts that into `AUTHORITY_REFUSAL` and `recordRefusal` creates `users/u/schedulerRefusals/...`. Missing/changed candidates similarly delete refusal descendants without a root read. The tests at `dispositionTriggers.test.js:928`, `:1010`, and `:1149` explicitly require this wrong behavior. Fix: root-read and call `assertDeletionAbsent` before every refusal create/update/delete, and never translate a deletion-fence error into a descendant write.

2. **BLOCKER** — An invalid nested `events` collection-group path bypasses the deletion fence and is mutated. [dispositionTriggers.js:993](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:993>), [dispositionTriggers.js:1123](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1123>), [dispositionTriggers.js:1039](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1039>). Contract: [C6.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:355>). Counterexample: `users/u/foo/x/events/e` is pending while `users/u.accountDeletion` is present. `eventUserId` rejects the path, line 1132 skips the root read, then `REFERENCE_INVALID` updates the source and eventually creates quarantine. Fix: derive and fence any enclosing `users/{uid}` owner before every invalid-reference mutation, or make invalid paths a zero-write refusal.

3. **BLOCKER** — All three notification-intent shared writers are independently unfenced. [notificationIntents.js:124](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:124>), [notificationIntents.js:149](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:149>), [notificationIntents.js:159](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:159>). Contract: [C6.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:355>). Counterexample: with `users/u.accountDeletion` present, calling `produceWake`, `upgradeWakeUrgency`, or `cancelPendingIntent` in a transaction still mutates `users/u/tasks/...` and/or `users/u/notificationIntents/...`. Fix: make each shared child perform the owner-root read and fence check inside its transaction before any mutation.

4. **BLOCKER** — The scheduler never implements the policy-present wake transitions. [dispositionTriggers.js:1571](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1571>), [dispositionTriggers.js:1583](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1583>). Contract: [C9.3.11](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2017>). Counterexample: a due `InProgress + USER_ACTION_TRACKED`, `matching_in_progress + WAITING_ON_EXTERNAL`, or valid armed threshold task is returned as `{woke:false}` without inspecting its live policy. The lane is considered settled and the scheduler can report completion while no wake, intent, `ATTENTION_NOW`, urgency upgrade, or attended projection was written. Fix: implement the complete C9.3.11 transaction table; reserve the no-op only for the exact policy-absent or stale-projection branches.

5. **BLOCKER** — Scheduler and migration cross-fence validators accept semantically invalid v2 leases as valid. [dispositionTriggers.js:1280](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1280>), [migrateOversizeEvents.js:499](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:499>), [migrateOversizeEvents.js:520](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:520>). Contract: [C9.1.4](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:497>) and [C9.2.3](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1296>). Counterexample: an expired map with `runOrdinal:-1`, `ownerToken:""`, and reversed timestamps passes `isV2Lease`; scheduler acquisition replaces it, and MIG-EVENT can acquire past it, although a non-v2 shape must produce zero writes. Current tests even use `"t"` and `"other"` as supposedly valid v2 owner tokens. Fix: enforce lowercase UUID, nonnegative safe ordinal, and exact timestamp relationships before classifying a lease as v2.

6. **HIGH** — D8 charges automatic scalar indexes in addition to array-contains entries. [dispositionTriggers.js:681](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:681>), [phase0SizingOracle.js:108](</Users/adampowell/Desktop/Peezy 4.0/functions/tests/support/phase0SizingOracle.js:108>). Contract: [C9.1.23](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:955>). Counterexample: an unoverridden one-element array receives three entries—ascending, descending, and contains—instead of one contains entry. Near the 8 MiB limit this falsely produces `SOURCE_TOO_LARGE` or a post-cutoff invariant. The “independent” oracle repeats the production bug. Fix: make array and scalar automatic-index branches mutually exclusive and correct the oracle separately.

7. **BLOCKER** — MIG-EVENT can mutate production without the required external rollout/drain authorization gate. [migrateOversizeEvents.js:67](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:67>), [migrateOversizeEvents.js:879](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:879>), [migrateOversizeEvents.js:947](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:947>). Contract: [C9.2.2](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1260>). Counterexample: the literal arming triple with matching project, tuple, and pins proceeds directly to lease acquisition and archive writes with no platform evidence observation; `dispositionTriggers.test.js:2109` proves this path succeeds. The optional caller file is only read while constructing the report at line 914—after mutation—and is not authority. Fix: inject and validate trusted platform authorization before `runApply`, then bind the captured evidence into the report; a caller-supplied file must not serve as authority.

8. **BLOCKER** — The read-only MIG-EVENT audit exits successfully with known failing sources. [migrateOversizeEvents.js:421](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:421>), [migrateOversizeEvents.js:967](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:967>). Contract: [C9.2.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1215>). Counterexample: one oversized pending source appears identically in both scans; `stable` becomes true and the CLI exits 0 despite `preShipCriterion:false`. `dispositionTriggers.test.js:1870` explicitly expects a stable nonzero failing set. Fix: run the second audit pass only after a complete first pass with zero failing/out-of-scope rows, and base exit success on the complete pass criterion rather than `stable`.

9. **BLOCKER** — Historical-migration checkpoint validation omits the state-dependent ordinal/cursor grammar, and production transitions create forbidden checkpoints. [purgeLegacyDeletedAccounts.js:109](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:109>), [purgeLegacyDeletedAccounts.js:698](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:698>), [purgeLegacyDeletedAccounts.js:792](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:792>). Contract: [C9.4.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2103>). Counterexample: `waiting_guards → confirming` retains `source_ordinal:23`, although confirming requires `0...22`; a confirmation failure writes `status:"reducing", source_ordinal:0`, although reducing requires 23. Both are accepted and resumed. Fix: validate the complete status/ordinal/source-kind/cursor relation and write the required values on every state transition.

10. **BLOCKER** — Historical confirmation is unbounded, not durably pageable, does not nominate new UIDs, and commits without a fresh barrier check. [purgeLegacyDeletedAccounts.js:721](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:721>), [purgeLegacyDeletedAccounts.js:792](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:792>). Contract: [C9.4.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2103>). Counterexample: one million candidates are fetched into `all`/`known`, then all 23 sources are traversed in one invocation. If a new orphan UID appears, the function returns `NEW_UID` but never creates its candidate; reduction sees only the old exclusions and confirmation loops forever. A barrier change after the scan is also not re-read before the zero-pass checkpoint commit. Fix: make confirmation one durable page per step, recheck the barrier immediately before each page commit, and nominate newly discovered UIDs before returning to reduction.

11. **BLOCKER** — Intent pair creation and cancellation do not enforce exact retry or pointer identity. [notificationIntents.js:124](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:124>), [notificationIntents.js:159](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:159>). Contract: [C9.3.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1817>). Counterexample: replaying `produceWake` after the deterministic intent was consumed blindly overwrites it back to pending. Separately, if task A’s pointer is crossed to task B’s pending intent ID, `cancelPendingIntent` cancels B’s intent and clears A’s pointer without calling `validateWakePointer`. Fix: transactionally read and validate an existing deterministic intent for exact retry, use create-only for absence, and require complete pair identity before cancellation.

12. **BLOCKER** — Claim accepts intents whose live cause, policy state, direct-WAIT state, or PC linkage is missing or stale. [notificationIntents.js:212](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:212>), [notificationIntents.js:231](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:231>). Contract: [C9.3.2](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1850>). Counterexample: the existing happy-path test task has no `taskInteractionState` at all and still claims successfully. A caller can also change `wakeEvidence.cause` while retaining `wake_id`; no hash is recomputed, so the intent is consumed and a claim record is created. Fix: require and validate the complete live instance/epoch/fingerprint/state/cause plus route-specific handoff/WAIT/PC linkage before the fence and writes.

13. **BLOCKER** — `drive` has no direct singleflight and revalidates auth only once. [TaskPlanService.swift:1466](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskPlanService.swift:1466>). Contract: [C9.5.12](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2730>). Counterexample: user A starts a drive, an inspection suspends, then auth switches to B; the code continues invoking A’s protected cleanup callback and can finalize without another auth check. Two concurrent direct `drive` calls can also both dispatch and execute separate callback bundles because only `recoverEpoch` owns a slot. Fix: install a per-handle drive singleflight whose winner owns callbacks, and rejoin/revalidate auth, row, and migration state after every await and before every call, callback, or return.

14. **HIGH** — Callback errors are returned without the mandatory post-error inspection. [TaskPlanService.swift:1498](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskPlanService.swift:1498>). Contract: [C9.5.14](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2764>). Counterexample: another device commits the reset while a local callback finishes its side effect but throws; this invocation propagates the callback error and leaves the row awaiting cleanup rather than adopting the committed final receipt. The existing test only re-inspects on a later separate invocation. Fix: catch each callback error, immediately re-inspect, adopt committed state if present, retain on byte-identical pending, and block every other result.

15. **BLOCKER** — `resetForRetake` ignores malformed or newer-epoch local-dose cleanup results and removes legacy keys anyway. [DailyDoseEngine.swift:415](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/DailyDoseEngine.swift:415>). Contract: [C9.5.16](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2816>). Counterexample: malformed v2 bytes plus valid legacy keys cause `cleanup` to return `.malformed`; line 425 discards that result, line 426 deletes the legacy keys, and the callback returns success so finalization proceeds. Fix: switch exhaustively on cleanup result, throw/block for `.malformed` and `.drift`, and remove legacy keys only after an accepted cleanup transition.

### Coverage gaps

- No contract-correct deletion-fence test for refusal create/update/delete, invalid nested event paths, or the three notification-intent helpers. Existing refusal tests assert the opposite.
- No policy-present ordinary/threshold scheduler wake matrix or race test from C9.3.11.
- No semantic malformed-v2 lease cases or genuinely independent D8 array oracle.
- No trusted platform-gate test; no CLI assertion that a stable failing audit exits nonzero.
- No state-dependent checkpoint grammar, bounded confirmation/resume, new-UID convergence, or barrier-drift-at-confirmation-commit test.
- No producer collision/consumed replay or crossed-pointer cancellation test.
- No claim tests for absent live state, mutated cause, direct-WAIT state, or PC linkage.
- No direct-drive concurrency, auth-switch-at-each-suspension, same-invocation callback-error inspection, or `resetForRetake` malformed/drift propagation test.
- I found no diff-specific Firestore/Storage Rules regression; the added operation-denial cases cover get/list/create/update/delete.

VERDICT: ITERATE
## Round 1 — builder response

# Response to Sol round 1 (builder; every finding verified against PHASE2_CONTRACT.md by a fresh-context reader before disposition)

Head for round 2: `4a86f0f` (fix commits 722f599, e48bef2, a6facbf, 4a86f0f on top of the close-out db4da6d and the Swift-pass fix 1092ea7). Diff for round 2: `S3_DIFF_1a48e0f..4a86f0f.patch` (same boundary paths). Envelope at 4a86f0f: offline Node 369 tests / 364 pass / 5 emulator-gated skips / 0 fail; Swift 70 tests in 3 suites passed on the emulator; emulator Node and rules re-run in progress at the time of writing.

| # | Disposition | What changed / why not |
|---|---|---|
| 1 | FIXED (C6.1 L357, L1797; C9.1.7 L550) | `refusalReasonOf` no longer maps `ACCOUNT_DELETION_FENCED` to `AUTHORITY_REFUSAL`. `recordRefusal`, `admitFresh`, and `retryRefusal` read the enclosing owner root (`assertDeletionAbsent`) after their candidate reads and before any refusal delete/set or reducer write; a fenced candidate settles for the run with the new lane kind `fenced` (zero writes, no refusal record). The three tests that pinned the old behavior now prove zero writes beneath a deleting owner on the fresh, retry, and threshold paths; the backoff series is driven by a policy-present row instead. Contract gap recorded: C9.1.20 does not say whether a fenced candidate settles the cursor; the fix treats it as settled-without-write. |
| 2 | FIXED (C6.1 L357, L322) | `consumeEventEnvelopeInTransaction` fences the enclosing `users/{uid}` owner of a nested (invalid) events path before any `REFERENCE_INVALID` mutation; the transaction still throws FENCED (the registered-write rule) and the phase-0 lane settles the source without writes. New fixture: `users/u2/foo/x/events/e1` under a deleting owner stays pending with no quarantine row. Paths with no `users/` prefix have no owner root the contract names and proceed as before. |
| 3 | FIXED by convention (contract gap) | C6.1 places the read on "the transaction", not the shared child, and no production caller of the three helpers exists yet (S6's wake transaction). The sibling file's shared children self-fence, so `produceWake`, `upgradeWakeUrgency` (now takes `db`/`uid`), and `cancelPendingIntent` each call `assertDeletionAbsent` before any write; a new named assertion proves all three refuse under a deletion marker with zero writes. |
| 4 | PARTIAL (S6 seam) | The C9.3.11 intent-producing branches need members no server path writes today (`taskInteractionState`, deadline evidence, `activeHandoff`; the contract defines no live policy-state shape), so the complete table stays with S6 as the I10 ledger disclosed. What S3 can do with existing members is now done: a policy-present row reaching any ordinary or threshold reducer is a fail-closed, durable, backed-off `VALIDATION_REFUSAL` instead of a silent `{woke:false}` no-op; policy-absent rows keep the H55 behavior. Ownership note for the owner: C10.1 L3989 and C6.5 L389 still charge these branches to `dispositionTriggers.js` (S3) while the ledger records them as S6; a C10 row should say which. |
| 5 | FIXED (C9.1.3/C9.1.4 L499-511; C9.2.3 L1313-1316) | Both validators require a lowercase UUID owner token, a nonnegative safe ordinal, and `expiresAt == startedAt + 270 s`; a v2-keyed lease that breaks the grammar is a non-v2 shape: the scheduler blocks with `SCHEDULER_LEASE_INVARIANT` and zero writes, MIG-EVENT refuses `SCHEDULER_LEASE_SHAPE` with zero commits. Fixtures use real UUIDs; the migration refusal codes are asserted exactly (live → `SCHEDULER_LEASE_LIVE`, within grace → `SCHEDULER_LEASE_DRAINING`). |
| 6 | FIXED (C9.1.23 L984-985 "instead") | An unoverridden array leaf carries one array-contains entry per element and no ascending/descending pair, in production and, separately, in the oracle; the 40,001-entry bound fixture was rebuilt on the corrected count; a named assertion pins one entry for `{a:[1]}` and two for `{a:[1,2]}`. |
| 7 | REFUTED (C9.2.2 L1262-1273) | The write-mode predicate is the literal arming triple, no unknown argument, resolved project/database, emulator variable absent, rollout-tuple match, and the fenced lease; L1272 places the rollout/drain authorization gate outside the script ("script neither infers nor claims to prove them from a caller-supplied file"). An in-script platform-evidence gate would contradict L1272; `--drain-evidence` is report-only by design. No change. |
| 8 | CONTRACT GAP (owner) | C9.2.1/C9.2.2 define no audit-mode exit criterion; apply mode already exits 4 on the pre-ship criterion. L1256's "second pass after first complete zero-failing pass" is the one literal sequencing mismatch (the code runs the second read-only pass after any complete pass and, in apply mode, deliberately drives migration from its failing set). Recorded for the owner; no change. |
| 9 | FIXED (C9.4.1 L2136-2138, L2222) | `validateCheckpoint` enforces the status-dependent grammar (0...22 with a cursor matching the current row while discovering/confirming; exactly 23 with `{kind:"start"}` while reducing/waiting_guards); discovery's last page transitions to reducing in the same commit; waiting_guards → confirming resets the position to row 0; every failure/drift transition writes ordinal 23. Grammar cases are named assertions. |
| 10 | FIXED (a, c); CONTRACT GAP (b) | (a) Confirmation is one durable source page per step from row 0, advancing `source_ordinal`/`source_cursor` under the confirming grammar; the candidate-row walk at pass end is bounded to 100 rows per page; count +1 and restart at row 0. (c) The barrier is re-read before every confirmation-page commit (advance, failure, count); a drift blocks with zero confirmation writes. (b) The contract's only confirming failure row is "any failure → reducing"; nothing says a new UID is nominated, though `pass_ordinal`/`first_pass_ordinal` imply later passes. Candidate amendment for the owner: NEW_UID → discovering at row 0 with `pass_ordinal + 1`. Until then the code follows L2222. |
| 11 | FIXED (C9.3.1 L1841-1847; C9.3.11 L2034) | `produceWake` returns an existing pair with the same identities untouched (a consumed or cancelled intent is never rewritten to pending) and fails closed on an incompatible wake under the pointer; a fresh pair is `create`-only. `cancelPendingIntent` validates the exact pair (`validateWakePointer`) and rejects a missing, dangling, crossed, or unequal pointer with zero writes. |
| 12 | PARTIAL (S3 half fixed; rest contract gap) | The claim re-derives the wake id from the live `wakeEvidence.cause` (drift → `INTENT_STALE`) and requires the WAITING state (`matching_in_progress` + `WAITING_ON_EXTERNAL`) for a direct outcome route (C9.3.12 row 5). Requiring the live policy state's presence and PC linkage needs shapes the contract does not define (no member names for the live policy state; no PC referent on the intent document) — recorded as contract gaps for the owner rather than hard-coding a shape. |
| 13 | FIXED (C9.5.8 L2655-2666; C9.5.12 L2736-2742) | `drive` holds the per-handle `inflightResetOperation` slot (installed before the first suspension, cleared in `defer`); a caller with the same UID/auth epoch and revision ≥ baseline joins for the outcome only and rereads auth after the task; a foreign caller waits for the slot, discards, rereads, restarts. The winner revalidates signed auth after every suspension and before every call, callback, and return: a different UID/epoch or signed-out state preserves the durable phase and returns the frozen auth branch, a lower revision is corruption, a higher one rebases. The earlier ledger disposition that saw no such row is withdrawn. The migration-singleflight rejoin (L2738) is S4's C9.4.5. |
| 14 | FIXED (C9.5.14 L2785) | A callback error is followed by one post-error inspection in the same invocation: committed adopts the final receipt and skips forever; a byte-identical pending authority retains the phase and rethrows; anything else blocks. The resume test's committed inspection moved from ordinal 2 to 3 because the failed drive now consumes its post-error inspection. |
| 15 | FIXED (C9.5.16 L2829-2830) | `resetForRetake` switches on the cleanup result: malformed throws `localDoseMalformed` (v2 bytes and legacy keys preserved), a newer epoch throws `localDoseDrift(currentEpoch:)` (preserved and reported), and the legacy keys are removed only after `cleaned`/`preserved`. The legacy-key ordering on a later reset is still unstated by the contract; the code now matches the bridge's preservation semantics. |

Coverage gaps named in round 1 that now have named tests: fenced refusal/nested-event/intent-writer paths; malformed-v2 lease cases and an oracle-independent array assertion; checkpoint grammar, paged confirmation, barrier drift at the confirmation-page commit; consumed-replay and crossed-pointer cancellation; cause drift and direct-WAIT coherence; drive auth drift, join without callbacks, same-invocation post-error adoption; malformed/drift dose propagation. Still open by contract gap: trusted platform gate (refuted), audit exit rule, NEW_UID nomination, live policy-state presence and PC linkage.

## Round 2 — Sol (verbatim; resumed thread, model pinned; the first resume attempt without -m substituted the config default model and consumed no round)

1. ADDRESSED — Refusal create/update/delete paths now fence the derived owner and treat deletion as zero-write; I accept that [C9.1.20](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1130>) does not specify whether a fenced candidate advances the run cursor.

2. ADDRESSED — Invalid nested event paths now derive and fence their enclosing owner before mutation at [dispositionTriggers.js:1137](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1137>) ([C6.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:355>)).

3. STILL OPEN — [notificationIntents.js:124](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:124>) fences caller-supplied `uid`, without proving it owns `taskRef`. Counterexample: `uid=A`, open `users/A`, `taskRef=users/B/tasks/t1`, and `users/B.accountDeletion` present; `produceWake` fences A and writes B’s descendant. [C6.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:355>) requires every current/prospective owner root. Fix: validate the exact `users/{uid}/tasks/{id}` path and fence its derived owner in all three helpers.

4. STILL OPEN — [dispositionTriggers.js:1585](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1585>) converts every policy-present due task into `VALIDATION_REFUSAL`. Counterexample: a valid due `InProgress + USER_ACTION_TRACKED` row receives backoff instead of wake evidence, `ATTENTION_NOW`, and an intent. [C9.3.11](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2017>) mandates that transition, and [C10.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:3980>) assigns `dispositionTriggers.js` to S3; the S6 reading is contradicted. Fix: implement the complete C9.3.11 wake table.

5. ADDRESSED — Both lease validators now enforce nonnegative ordinals, lowercase UUID syntax, timestamps, and exact 270-second duration at [dispositionTriggers.js:1286](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:1286>) and [migrateOversizeEvents.js:502](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:502>).

6. ADDRESSED — Arrays now receive only per-element array-contains entries at [dispositionTriggers.js:699](</Users/adampowell/Desktop/Peezy 4.0/functions/dispositionTriggers.js:699>), with the oracle corrected separately ([C9.1.23](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:955>)).

7. ADDRESSED — I accept the REFUTED reading: [C9.2.2](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1260>) makes rollout/drain authorization external and explicitly says the script neither infers nor proves it from a caller file.

8. STILL OPEN — [migrateOversizeEvents.js:422](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:422>) still starts pass two after any complete pass. Counterexample: pass one contains one stable oversized source; pass two runs and audit exits successfully at [line 975](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:975>). Although audit exit status is unspecified, [C9.2.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1256>) explicitly requires the second pass only after a complete zero-failing first pass. Fix: gate pass two on that predicate.

9. ADDRESSED — Checkpoint validation and all relevant transitions now enforce the status-dependent ordinal/cursor grammar at [purgeLegacyDeletedAccounts.js:109](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:109>).

10. ACCEPT GAP — The originally cited confirmation traversal is now durably source-page-based and barrier-checked at [purgeLegacyDeletedAccounts.js:758](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:758>) and [line 823](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:823>); I accept that [C9.4.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2222>) specifies only “any failure → reducing,” not NEW_UID nomination.

11. STILL OPEN — Existing-pair validation at [notificationIntents.js:145](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:145>) compares only selected IDs. Counterexample: mutate the existing intent’s `policy_fingerprint` or add surplus content while retaining its IDs/pointers; `produceWake` returns it as an exact retry without `validateIntent` or identity recomputation. This violates exact retry and incompatible-wake rejection in [C9.3.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1817>) and [C9.3.11](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2017>). Fix: strictly validate and identity-check both existing documents before preserving them.

12. STILL OPEN — [notificationIntents.js:237](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:237>) still treats absent live policy state as valid. Counterexample: the current happy-path task has no `taskInteractionState`, yet claim consumes the intent and creates the operation record. [C9.3.2](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1850>) explicitly requires live epoch, fingerprint, state, and PC-linkage validation, contradicting the claimed state gap. Fix: require a complete validated live policy state and route-specific linkage before writing.

13. STILL OPEN — The singleflight remains incomplete at [TaskPlanService.swift:1473](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskPlanService.swift:1473>). Counterexample: a same-identity joiner awaits a task that throws while auth changes; `try await` at line 1478 propagates before the mandatory post-task auth reread. Separately, auth can change after entry and before the first server call at line 1523, which has no immediate revalidation. This contradicts [C9.5.8](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2635>) and [C9.5.12](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2730>). Fix: capture the task as `Result`, always reread auth, and revalidate immediately before every call and return.

14. ADDRESSED — Callback errors now trigger same-invocation reinspection and committed-state adoption at [TaskPlanService.swift:1553](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskPlanService.swift:1553>) ([C9.5.14](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2764>)).

15. ADDRESSED — Malformed and newer-epoch dose states are preserved and thrown, and legacy keys are removed only for accepted cleanup outcomes at [DailyDoseEngine.swift:427](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/DailyDoseEngine.swift:427>).

New findings

1. BLOCKER — [purgeLegacyDeletedAccounts.js:655](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:655>) and [line 707](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:707>) still issue unlimited candidate queries. Counterexample: one million candidates are materialized by `sweepComplete` or `waitingStep`, violating [C9.4.1](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2243>) (“at most one resident page; no unbounded RAM”) and potentially preventing migration progress. Fix: scan bounded pages sequentially or use bounded authoritative existence queries.

2. HIGH — Joiners inherit the winner’s `notify:true` at [TaskPlanService.swift:1478](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskPlanService.swift:1478>), and each coordinator posts at [RetakeAssessmentCoordinator.swift:68](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/RetakeAssessmentCoordinator.swift:68>). Counterexample: a second coordinator joins an in-flight drive that stores the fresh final receipt; both receive `notify:true` and post `.retakeAssessment`, violating [C9.5.17](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:2835>) at-most-once semantics. Fix: return `notify:false` to every joiner.

3. BLOCKER — [notificationIntents.js:223](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:223>) does not validate the stored response’s closed schema or cap. Counterexample: change `response.accountUid` to another UID, add a surplus member, and recompute `response_sha256`; `validateClaimRecord` accepts and replays it. This violates the closed typed response and altered-response rejection in [C9.3.3](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/PHASE2_CONTRACT.md:1875>). Fix: validate the complete response member/type/equality grammar and size before accepting its digest.

VERDICT: ITERATE
## Round 2 — builder response

# Response to Sol round 2 (builder)

Head for round 3: `4b6aaba` (fix commit on top of 4a86f0f). Fix delta: `S3_FIXES_4a86f0f..4b6aaba.patch` (429 lines). Full boundary diff: `S3_DIFF_1a48e0f..4b6aaba.patch`. Envelope at 4b6aaba: offline Node 369 / 364 / 5 skips / 0 fail; Swift 71 tests in 3 suites passed on the emulator; emulator Node and rules re-run in progress.

| # | Disposition | What changed |
|---|---|---|
| 3 | FIXED (C6.1) | `requireTaskOwner(uid, taskRef)` in all three writers: the task path must be `users/{uid}/tasks/{id}` beneath the fenced owner; a foreign task path rejects before any write (new named assertions). |
| 4 | OWNER (scope) | I do not dispute that C10.1 names `dispositionTriggers.js` as S3's file. The complete C9.3.11 table cannot be coded from the contract as written: the live policy state (`taskInteractionState`), deadline evidence, and `activeHandoff` have no defined shape or writer anywhere in the contract, and the fresh-context verification confirmed no server path produces them. The ledger disclosed this at I10 as an S6 seam; the owner decides whether to amend C10 (S6 owns the branches with the policy-state shape added to C9.3) or extend S3 after the shape is added. The fail-closed VALIDATION_REFUSAL for policy-present rows stays as the interim behavior. |
| 8 | FIXED (C9.2.1 L1256) | Audit: the second pass runs only after a complete first pass with zero failing, zero out-of-scope, zero drift; otherwise one pass, `stable:false`, exit 1. Apply: the confirmation under the lease is the complete zero-drift pass whose failing set is migrated (a zero-failing pass is confirmed by its second pass); the two-pass criterion remains the pre-ship gate after migration. |
| 11 | FIXED (C9.3.1) | The existing pair is preserved only when the stored intent validates (`validateIntent`), carries this pair's identity (`validateIntentIdentity`), and equals the intent to be written outside `state/created_at/expires_at/consumed_at/claim_operation_id`, and the stored wake equals the computed evidence outside `fired_at/urgency/urgency_basis`; drift or surplus content fails closed with zero writes (four named cases). |
| 12 | FIXED for the live policy state; PC linkage remains a gap | A claim requires `taskInteractionState` with the intent's epoch and fingerprint; absent or partial state is `INTENT_STALE`. The intent document carries no PC referent (C9.3.1 grammar), so "PC linkage no longer matches" has nothing to compare against; recorded for the owner. |
| 13 | FIXED (C9.5.8, C9.5.12) | A joiner awaits the task's `Result`, rereads signed auth, and only then exposes the outcome or error (drift → `authRequired`); the winner revalidates at the top of every reducer iteration, so every server call, callback, and return follows a fresh check (the task hop after entry is covered). |
| N1 | FIXED (C9.4.1 residency) | `sweepComplete` and the waiting-guards completion check walk the candidates through one bounded page helper (`candidatePage`, 100 per page); a source scan asserts every reader of the candidate collection bounds its page. |
| N2 | FIXED (C9.5.17) | A joiner's outcome carries `notify:false`; only the winner's invocation posts. |
| N3 | FIXED (C9.3.3) | `isClaimResponse` validates the stored `task_route` response as the closed typed wire (exact member set, types, the caller's account/operation/intent, route grammar, UTC wire, size cap) before the digest is trusted; recomputed-digest defects are `OPERATION_REUSED`. |

This is round 3 of a 3-round cap. Items 1, 2, 5, 6, 7, 9, 10, 14, 15 were accepted in round 2 and are unchanged.

## Round 3 — Sol (verbatim; final round of the 3-round cap)

1. ADDRESSED — Task ownership is now validated before all three writers at [notificationIntents.js:70](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:70>), satisfying C6.1.

2. 4. ACCEPT GAP — I accept the owner/scope reading: C9.3.11 defines required outcomes but does not define a complete live `taskInteractionState`, deadline-evidence, or `activeHandoff` schema that these branches could validate.

3. 8. ADDRESSED — [migrateOversizeEvents.js:426](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/migrateOversizeEvents.js:426>) now runs audit pass two only after a complete zero-failing first pass, as required by C9.2.1 §two-pass.

4. 11. STILL OPEN — [notificationIntents.js:159](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:159>) removes `fired_at`, `urgency`, and `urgency_basis` before comparison without validating them. Counterexample: an otherwise matching wake with `fired_at:"bad"`, `urgency:"bogus"`, and malformed `urgency_basis` is preserved and returned. C9.3.1 defines their grammar, and C9.3.11 requires malformed existing wakes to fail closed. Fix: strictly validate the complete existing wake before comparing permitted lifecycle differences.

5. 12. STILL OPEN — [notificationIntents.js:273](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:273>) checks policy epoch/fingerprint but does not validate live task status for row routes. Counterexample: change the task to `status:"Completed"` while retaining its pointer and policy state; claim still consumes the intent and returns a row route. C9.3.2 §Claim validation explicitly rejects when live state no longer matches. I accept that PC linkage itself remains a contract gap. Fix: reject terminal or route-incompatible task statuses using the states defined by C9.3.11/C9.3.12.

6. 13. STILL OPEN — [TaskPlanService.swift:1473](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskPlanService.swift:1473>) rejects `uid != handle.uid` before consulting the existing slot. Counterexample: A’s drive is in flight, auth changes to B, and another caller invokes A’s handle; it immediately throws `operationStale` instead of awaiting and discarding A’s result. C9.5.8 §Foreign caller and C9.5.12 §Join require wait/discard/reread/restart. Fix: after the fresh auth read, handle an existing foreign slot before applying the handle-UID rejection. The round-3 post-task reread and winner revalidation changes are otherwise correct.

7. N1. ADDRESSED — Candidate-wide checks now traverse bounded 100-row pages through [purgeLegacyDeletedAccounts.js:653](</Users/adampowell/Desktop/Peezy 4.0/functions/scripts/purgeLegacyDeletedAccounts.js:653>), satisfying C9.4.1 residency.

8. N2. ADDRESSED — Joiners now return `notify:false` at [TaskPlanService.swift:1485](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskPlanService.swift:1485>), satisfying C9.5.17.

9. N3. STILL OPEN — [notificationIntents.js:261](</Users/adampowell/Desktop/Peezy 4.0/functions/notificationIntents.js:261>) checks only the timestamp’s textual pattern. Counterexample reproduced: `expiresAt:"2026-99-99T99:99:99.999Z"` with a recomputed digest is accepted by `validateClaimRecord`. C9.3.2 requires an exact UTC projection of the stored timestamp, while C9.3.3 requires altered responses to fail. Fix: strictly parse the date and require a byte-identical UTC millisecond round-trip.

New findings: None beyond the carried items above. Targeted Node suites passed 155 tests with 5 emulator-only skips, but do not cover these counterexamples.

VERDICT: ITERATE
## Round 3 — builder disposition (post-cap; not re-reviewed by Sol)

Round 3 accepted 3, 4 (as owner scope), 8, N1, N2 and left 11, 12, 13, N3 open. All four were verified as contract-backed and fixed after the cap, each RED then GREEN:

| # | Fix | Anchor |
|---|---|---|
| 11 | `validateWakeEvidence` validates the complete existing wake (exact members, timestamp `fired_at`, urgency enum, basis exactly when urgent, cause grammar) before the exact-retry comparison; malformed `fired_at`/`urgency`/`urgency_basis` fail closed with zero writes. | C9.3.1, C9.3.11 "fail-closed, not overwritten" |
| 12 | A claim on a terminal task (`Completed`/`Dismissed`) is `INTENT_STALE` for every route. PC linkage remains a contract gap (no referent on the intent document). | C9.3.2 claim validation |
| 13 | `drive` consults an occupied slot before judging the handle: a foreign caller waits for slot retirement, discards, rereads auth, and restarts (then finds the handle stale); the immediate `operationStale` no longer preempts the wait. A new test observes that the foreign caller is not answered while the slot is occupied. | C9.5.8 foreign caller, C9.5.12 join |
| N3 | `expiresAt` must round-trip byte-identically through `Date.parse` → `toISOString()`; a non-calendar wire with a recomputed digest is `OPERATION_REUSED`. The record-reuse test loop was also made independent per case (it had carried the previous case's defect forward). | C9.3.2 UTC wire, C9.3.3 |

Envelope at the final head: offline Node 369 / 364 / 5 emulator-gated skips / 0 fail; Swift 72 tests in 3 suites on the emulator; emulator Node subset and rules recorded in the ledger. Whether to spend a fourth Sol round on these four fixes is the owner's budget decision.

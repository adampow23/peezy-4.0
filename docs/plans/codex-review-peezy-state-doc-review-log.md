# Plan Review Log: Generate PEEZY_STATE.md — canonical state document
MAX_ROUNDS=3, BUDGET=6 chain rounds, strict=false, reviewer model=gpt-5.6-sol, codex-cli 0.147.0.

## Round 1 — Codex (thread 01a02ed2-b2e1-7203-8f31-0a6d3280c6cc)

1. [CRITICAL] The plan asks static code to establish live facts—“every deployed callable,” current Firestore state/rules, and whether `resetInventory` remains deployed. [functions/index.js](</Users/adampowell/Desktop/Peezy 4.0/functions/index.js>) proves only local exports, which include HTTP and event triggers as well as callables; no local `resetInventory` export exists, but remote absence is unprovable. Fix — Require every operational claim to be classified `LOCAL_OBSERVED`, `DEPLOY_CONFIGURED`, `LIVE_VERIFIED`, or `UNKNOWN`, with live inventory allowed only through a separately authorized read-only check recording project and timestamp.

2. [CRITICAL] The audit boundary does not protect secrets or user data. “Env vars in use” plus broad repo inspection exposes an ignored `.env`, credential-bearing `functions/serviceAccountKey.json`, a literal gift code, and generated logs/snapshots; duplicating full output into Downloads compounds leakage. Fix — Forbid reading secret-value files and runtime/user data, derive variable names only from reachable source or sanitized templates, require output redaction/secret scanning, and rotate or revoke the exposed service-account key before this audit.

3. [CRITICAL] “Anything failing” does not define a safe test matrix. The default Firebase configuration targets a real project; [E2ETestBase.swift](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0UITests/E2ETestBase.swift>) contains test credentials, UI tests write tasks/support messages and invoke restore, and [ProcessInventoryDiagnostic.js](</Users/adampowell/Desktop/Peezy 4.0/Tests/ProcessInventoryDiagnostic.js>) has a live mode that uploads, invokes, and deletes data. “No live seeds/deploys” does not prohibit these operations. Fix — Permit only an explicit offline/emulator test allowlist with a hard non-production-project assertion, forbid network/UI/integration/purchase/restore scripts, and report each test as `PASS`, `FAIL`, `SKIPPED`, or `ENV_BLOCKED`.

4. [CRITICAL] “Stage-1 subscription hardening” overstates the security actually present. [validateSubscription.js](</Users/adampowell/Desktop/Peezy 4.0/functions/validateSubscription.js>) is an unauthenticated HTTP endpoint that accepts caller-supplied user and transaction evidence without Apple verification, so forged input can grant server entitlement. Fix — Require an entitlement threat model and a negative acceptance test proving forged requests cannot grant access; until authentication, App Check, and Apple server verification exist, label this path insecure/provisional rather than hardened.

5. [HIGH] The specification violates its own “only if evidenced” rule by seeding conclusions such as “split-brain,” Stage 1/Stage 2, “pending,” “orphan,” “manual-release posture,” and “one-card-one-idea.” Some originate only in prior documents the plan expressly forbids consulting; insider-content support exists in code, but the generic pacing and release claims do not. Fix — Convert every seeded topic into a hypothesis row with mandatory `SUPPORTED`, `CONTRADICTED`, or `UNKNOWN` disposition, and state that the plan itself is never evidence.

6. [HIGH] There is no reproducible snapshot or audit scope. The reviewed checkout is dirty/untracked and substantially ahead of its remote, while the repository also contains tooling, artifacts, prototypes, dependencies, and generated files that can produce contradictory evidence. Fix — Record root, branch, commit SHA, upstream divergence, tracked/untracked policy, start/end timestamps, and input hashes; derive scope from active Xcode targets and reachable Firebase exports, and restart if inputs change.

7. [HIGH] None of the three deliverables has complete binary acceptance criteria: “under ~250 lines” is approximate, citation coverage is unspecified, manifest completeness is subjective, and the Downloads copies may drift. Fix — Define exact heading/schema and line limits, require evidence or `UNKNOWN` for every factual row, mechanically compare the manifest against an explicit candidate set, and verify report copies by byte hash.

8. [HIGH] The flow structure can produce false reachability claims. Spawn/`skipIfKnown` exists in [FlowDefinition.swift](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/Tasks/FlowEngine/FlowDefinition.swift>), but two of the three data definitions using those features are shadowed by bespoke routes in [TaskFlowRouter.swift](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskFlowRouter.swift>); the post-flow fork applies to engine flows, not all flows, and scanner/packing/research have different client/server gates. Fix — Require a per-flow `definition → router → renderer → post-flow → paywall → enforcing side` reachability matrix before making “flows free, help paid” claims.

9. [HIGH] The spawn and nudge systems exist, but the proposed labels hide failure modes. [spawnTasks.js](</Users/adampowell/Desktop/Peezy 4.0/functions/spawnTasks.js>) checks an idempotency token outside a transaction, allowing concurrent duplicate spawning; nudge dismissal can remove the card locally while its Firestore write error is swallowed, so it may reappear. Fix — Specify concurrent same-token and persistence-failure tests, and describe spawn as sequential-replay-idempotent and nudge completion as non-durable until those cases are resolved.

10. [HIGH] “Catalog count” has no source-of-truth rule. The root catalog contains 61 task rows and 34 flows, while the active Functions seeding inputs contain 63 and 35; [seedTaskCatalog.js](</Users/adampowell/Desktop/Peezy 4.0/functions/seedTaskCatalog.js>) delete-rewrites both collections. Fix — Require a provenance table containing each source’s role, hash, counts, divergence, loader/seeder path, and target collection, and replace “reseedable” with “deterministically reproducible from checked-in input—deployment still requires backup, diff, target confirmation, and approval.”

11. [HIGH] The requested Firestore “carve-outs” risk documenting comments instead of effective authorization. In [firestore.rules](</Users/adampowell/Desktop/Peezy 4.0/firestore.rules:43>), the recursive owner-write rule also matches `moveAnswers`, so its narrower rule does not prevent writes because Firebase allows are additive. Fix — Require an effective permission matrix that evaluates every overlapping match by path, actor, and operation, backed by emulator rule tests.

12. [HIGH] Gift codes and Move Pass exist, but the requested pricing/lifecycle summary lacks essential qualifications. The StoreKit configuration proves only a local test price, restore updates local state without server synchronization, an expired pass can mask gift fallback, and redemption ignores the stored `termMonths` field in favor of a hard-coded duration. Fix — Require a source-of-truth table for local StoreKit, App Store production, server entitlement, and gift entitlement, plus acceptance cases for restore, expiry-plus-gift, redemption concurrency, and variable term length.

13. [HIGH] The three-task movers chain is real, but it is not the only movers behavior. [MoversChainCoordinator.swift](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/MoversChainCoordinator.swift>) implements the new chain while legacy inline quote/stage logic remains reachable for existing state. Fix — Document new-chain, legacy-resume, migration, and fallback paths separately, with the routing predicate and persisted-state prerequisites for each.

14. [HIGH] `ARCHIVE_MANIFEST.md` is underspecified and internally contradictory: the agent must identify every root document “replaced” while being forbidden to inspect prior documents, and “document,” “old,” and “replaces” are undefined. Root files include operational and code-referenced material that a filename-only pass could wrongly archive. Fix — Permit a narrow classification pass and define a deterministic candidate inventory with `KEEP`, `ARCHIVE`, or `UNKNOWN`, reason, tracked status, hash, and inbound-reference checks; archive only an explicit approved set.

15. [MEDIUM] “Two task loaders” and “terminal status handling” are pre-baked abstractions rather than established invariants. Active Home and Tasks consumers differ, an older stack view model appears unreachable, and statuses treated as completed or hidden vary by surface. Fix — Require reference/call-graph evidence for active loaders and a status-by-writer/by-consumer matrix instead of one global terminal-status list.

16. [MEDIUM] The audit is called read-only while requiring writes to the repository and Downloads, with no overwrite, permissions, atomicity, or partial-failure policy; “regenerate Sections 1–3 whenever anything changed” can also overwrite unrelated findings without re-audit. Fix — Declare the exact output paths as the sole write exceptions, preflight them, write atomically, hash-copy the report, and require a full snapshot/coverage rerun before regeneration.

17. [MEDIUM] The approximately 250-line narrative cap conflicts with exhaustive functions, collections, workflows, evidence, gaps, and citations. A stronger simpler alternative is a compact canonical state matrix—`topic | observed state | evidence | confidence`—with mechanically generated inventories/evidence ledgers outside the narrative cap. Fix — Adopt that matrix-plus-generated-appendices structure and cap only explanatory prose, not verifiable evidence.

VERDICT: ITERATE
### Response (rev 2)

Accepted with concrete plan changes: F1 (evidence classes LOCAL_OBSERVED/DEPLOY_CONFIGURED/UNKNOWN; no live claims), F2 (secrets boundary: no value files opened, names-only env vars, redaction pass; key-rotation note surfaced to human), F3 (stronger than requested: audit executes nothing — runtime test status uniformly UNKNOWN, static reporting only), F4 (doc must state validateSubscription's actual auth posture; "hardened" banned absent verified code; gap → section 3), F5+F15 ("spec is not evidence" — every seeded topic becomes a hypothesis with SUPPORTED/CONTRADICTED/UNKNOWN disposition; loaders verified by call graph; per-surface status view), F6 (snapshot header: date/branch/SHA/dirty-count), F7 (8 binary acceptance criteria incl. shasum-verified copies, 300-line hard cap, 100% manifest coverage), F8 (gating map scoped per-gate with enforcing side + router-shadowing noted; full matrix rejected as disproportionate), F9 (spawn idempotency + nudge durability described as-implemented, caveats → section 3), F10 (catalog provenance table, divergence stated, "reseedable-without-review" phrasing banned), F11 (effective-permission analysis of overlapping rules incl. moveAnswers additive-allow), F12 (StoreKit source-of-truth table + all four wiring gaps → section 3), F13 (movers legacy path + routing predicate documented), F14 (mechanical candidate set + narrow classification pass + KEEP/ARCHIVE/UNKNOWN with inbound-reference check), F16 (three declared output paths as sole write exceptions, git-status verification).

Rejected (rationale in plan's "Reviewer pushback" section): input-hash ledger/restart-on-change (F6, partial), threat model + negative entitlement tests (F4, partial — follow-up work, not doc generation), emulator rules tests (F11, partial), appendix-ledger architecture (F17 — recreates doc sprawl; compensated by 300-line cap + fact-table style), exhaustive per-flow matrix (F8, partial), per-test PASS/FAIL matrix (F3, partial — superseded by no-execution rule), live Firebase inventory (F1, partial — human-run per protocol).

## Round 2 — Codex (same thread, resumed)

1. [CRITICAL] Line 25 is factually wrong and misses a larger deployment hazard: `functions/serviceAccountKey.json` is ignored—not checked in—by [.gitignore:75](</Users/adampowell/Desktop/Peezy 4.0/.gitignore:75>), but the Functions ignore list omits it and `.env` ([firebase.json:13](</Users/adampowell/Desktop/Peezy 4.0/firebase.json:13>)); the installed Firebase packager archives files based on that list ([prepareFunctionsUpload.js:57](/opt/homebrew/lib/node_modules/firebase-tools/lib/deploy/functions/prepareFunctionsUpload.js:57)), making both local files eligible for upload. Fix — Add existence-only `LOCAL_METADATA` checks and require a CRITICAL deploy blocker covering removal/relocation, explicit upload exclusions, and human rotation/revocation before deployment.

2. [HIGH] The rejected snapshot rationale at line 75 is factually wrong: commit SHA identifies only HEAD, while a dirty-file count identifies neither paths nor contents and cannot detect same-count changes during the audit. The current baseline already includes modified `PHASE_MANIFEST` and two untracked plan files, so criterion 7 also fails as an absolute post-audit status check. Fix — Record the full initial porcelain state plus one combined digest of audited dirty/untracked inputs, then require the final state to equal that baseline plus the declared outputs, treating `PHASE_MANIFEST` as preserved baseline rather than a fourth exception.

3. [HIGH] Rev 2 still lacks a repository evidence boundary. Tracked tooling, generated artifacts, alternate implementation trees, tests, and stale source can all satisfy “code” unless the cold agent independently guesses which Xcode and Firebase paths are active. Fix — Define inclusion and precedence from active Xcode target/build-phase membership, `firebase.json`, `functions/index.js` reachability, and active loader/seeder references, classifying everything else as test, tooling, dead, or unscoped evidence.

4. [HIGH] Lines 29 and 65 use `file:line OR evidence tag OR UNKNOWN`, allowing a bare `LOCAL_OBSERVED` tag with no evidence and allowing `UNKNOWN` without showing that anything was investigated; file-line evidence also cannot prove derived counts. Fix — Require both evidence class and source evidence for every SUPPORTED/CONTRADICTED claim, require search scope and unavailable evidence for UNKNOWN, and define a deterministic-command evidence form for counts and set comparisons.

5. [HIGH] Acceptance criterion 3 is not binary because hypotheses are scattered through prose and an ellipsis, while no criterion reconciles documented functions, collections, data sources, or test targets against discovered sets; section 4’s required verbatim text is likewise unchecked. Fix — Add a finite hypothesis-ID register, set-equality/count reconciliations for every exhaustive inventory, and an exact block comparison for section 4.

6. [HIGH] The audit itself is safely non-executing, but the canonical future protocol only bans live seeds and deploys. Future agents following section 4 could still run production-connected UI/diagnostic tests, invoke callables, write Firestore/support data, or trigger StoreKit restore. Fix — Extend the verbatim protocol to forbid all live-service and billing operations unless a human authorizes the exact target, action, and non-production safeguards.

7. [HIGH] “Seeding itself is human-run” is not adequate mitigation for [seedTaskCatalog.js](</Users/adampowell/Desktop/Peezy 4.0/functions/seedTaskCatalog.js:47>), which delete-rewrites catalog and flow collections. Human execution alone does not provide target validation or recovery. Fix — Require project-ID confirmation, backup/export, dry-run count/hash diff, explicit approval, and post-seed verification before any live seed.

8. [HIGH] Section 3 has no severity or stop-action schema, so confirmed credential, entitlement, billing, or destructive-data hazards can appear as ordinary caveats without affecting release/deploy decisions. Fix — Require `severity | impact | evidence | safe next action | blocks` fields, with CRITICAL findings blocking seed, deploy, or release until recorded human sign-off.

9. [HIGH] Manifest construction remains sequencing-dependent: if candidates are gathered after output creation, `PEEZY_STATE.md` and `ARCHIVE_MANIFEST.md` enter the candidate set, and the manifest can become an inbound self-reference; “doc-like `*.txt` from ls” is not mechanical. Fix — Freeze a NUL-safe candidate list before any writes, define whether all `.txt` files qualify, exclude generated outputs from candidates and reference searches, and record the frozen count.

10. [HIGH] `STATE_GEN.md` has no deterministic container format. A composite report can embed exact payloads, but its own hash cannot prove that two undefined regions are byte-identical to two source files. Fix — Specify an unambiguous length-delimited envelope and verify each extracted payload’s byte count and SHA against its source.

11. [HIGH] The redaction procedure is not executable as written: it says to grep outputs before they are written, temporary staging is not an allowed write, “key-like patterns” is undefined, and matching secret filenames would flag legitimate path-only warnings without detecting leaked values. Fix — Authorize private temporary staging and report-directory creation, mandate value-redacting source inspection plus an exact zero-match pattern set, then atomically rename and hash the validated outputs.

12. [MEDIUM] Section 2 permits an unexecuted test to establish a product decision “in force.” Static test presence proves only an asserted expectation, especially when all runtime test status is explicitly UNKNOWN. Fix — Classify test-only evidence as `TEST_ASSERTED_UNVERIFIED` and require matching reachable implementation or active data before assigning SUPPORTED.

13. [MEDIUM] `TODO/H-markers` remains undefined and unscoped, allowing retired code, tests, generated logs, or tooling comments to become canonical “known gaps.” Fix — Define the exact marker syntax and search roots, and require reachable production-code corroboration before treating a marker as a current gap.

14. [MEDIUM] The future instruction to regenerate sections 1–3 whenever “anything changed” still permits a scoped agent to overwrite unrelated facts from partial knowledge and does not require refreshing the snapshot or manifest. Fix — Require the complete evidence and acceptance pass against a fresh snapshot before replacement, and refresh the manifest whenever its frozen root-document set changes.

VERDICT: ITERATE
### Response (rev 3)

Accepted: F1 (fixed my factual error — key is gitignored not checked in; adopted LOCAL_METADATA existence-only class; functions-upload hazard becomes a CRITICAL section-3 row with blocks:deploy + human rotation note), F2 (full porcelain baseline + combined dirty-input digest recorded first; final state must equal baseline + the two root outputs; PHASE_MANIFEST treated as preserved baseline), F3 (scope boundary: active = pbxproj app-target membership / index.js reachability / firebase.json / seeder-loader references; all else test/tooling/dead/unscoped), F4 (AND-logic: evidence class + file:line for every SUPPORTED/CONTRADICTED; UNKNOWN must state search scope; counts carry the deterministic command), F5 (closed hypothesis register H1–H40; set-equality reconciliation criteria; byte-identical check on section 4 block), F6+F14 (adopted as clearly-labeled protocol addendum pending owner sign-off — verbatim bullets preserved), F7 (safe-seed checklist embedded in the doc's seeding description), F8 (section-3 row schema severity|impact|evidence|safe next action|blocks; CRITICAL blocks until sign-off), F9 (candidate list frozen NUL-safe at baseline before any writes; all root *.md+*.txt; outputs excluded by construction), F10 partial (per-file byte count + SHA-256 verified against source files; formal envelope grammar rejected as redundant), F11 (staging dir authorized; exact zero-match pattern set defined; install-then-hash), F12 (TEST_ASSERTED_UNVERIFIED class), F13 (marker syntax TODO/FIXME/HACK/H[0-9]+, active scope only, corroboration required).

Rejected/partial (rationale in plan): envelope grammar (F10), rewriting owner's verbatim bullets (F6/F14 — addendum instead), wholesale restart-on-drift (carried from R1-F6 — detect + re-verify affected claims).

## Round 3 — Codex (same thread, resumed; final round under cap)

1. [HIGH] The redaction acceptance is internally impossible: [plan line 42](</Users/adampowell/Desktop/Peezy 4.0/docs/plans/codex-review-peezy-state-doc.md:42>) rejects any 40-plus-character Base64-alphabet run, while the required 40-character Git SHA and 64-character SHA-256 values satisfy that pattern, so criterion 8 cannot report zero matches. The listed patterns also miss alternate gift-code and hard-coded test-credential formats, and ordinary grep may print a discovered secret into logs. Fix — Use quiet, context-aware scanning that exempts only labeled checksum fields and detects credential assignments and both gift-code formats without printing matches.

2. [HIGH] The round-2 drift defect remains: a digest is calculated initially, but no execution step or acceptance criterion recomputes it; final porcelain equality cannot detect changed contents of an already-dirty file. Pushback line 95 therefore incorrectly claims the digest detects drift, and one combined digest cannot identify which claims are “affected.” Fix — Define deterministic path-framed per-file hashes and compare them at close, or require a full evidence recheck whenever a recomputed combined digest differs.

3. [HIGH] The deployment scope is incomplete because it equates backend relevance with `functions/index.js` runtime reachability. Firebase deploys additional surfaces from [firebase.json](</Users/adampowell/Desktop/Peezy 4.0/firebase.json:2>): the broader Functions source bundle, [storage.rules](</Users/adampowell/Desktop/Peezy 4.0/storage.rules:1>), and `public/**` Hosting content, including the admin surface; active inventory uploads use Storage through [InventoryStorageService.swift](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/Inventory/Services/InventoryStorageService.swift:48>). Fix — Add a transitive Firebase deployment-scope inventory covering packaged source, Hosting, Storage paths/rules, and runtime exports, with corresponding hypothesis IDs and set-reconciliation criteria.

4. [HIGH] The Functions-package hazard is too narrow: the upload ignore list also leaves seed, test, and diagnostic files bundle-eligible, including [seedAppReviewGiftCode.js](</Users/adampowell/Desktop/Peezy 4.0/functions/seedAppReviewGiftCode.js:16>) with literal entitlement codes. Removing only `.env` and the service-account file would leave other secret-like artifacts packaged. Fix — Require a non-printing inventory of every package-eligible sensitive file and block deployment until each is excluded or relocated and any exposed credential/code is assessed for rotation.

5. [HIGH] The mandatory upload hazard contradicts the closed hypothesis/evidence rules: every plan-named topic must be in H1–H40, but line 43 introduces an additional pre-decided conclusion, and repository files alone establish only that exclusions are absent—not Firebase CLI packaging behavior. Fix — Add an explicit packaging-risk hypothesis and either permit versioned `LOCAL_TOOLCHAIN_OBSERVED` evidence or conservatively report “not explicitly excluded; package inclusion unverified” while retaining the deploy block.

6. [HIGH] The future production safeguard is labeled “pending owner sign-off,” so its authority is ambiguous, while “Firestore writes outside owned dev data” implicitly permits writes to undefined dev data that could still reside in production. Critical-row sign-off is also required without a sign-off field or authoritative record. Fix — Make the addendum binding by default, allow remote writes only through an enumerated non-production target or exact human authorization, and add `sign_off: PENDING | owner/date/scope/reference` to blocker rows.

7. [HIGH] H17 can still be materially underclassified. If the audit confirms the current unauthenticated, caller-supplied entitlement path in [validateSubscription.js](</Users/adampowell/Desktop/Peezy 4.0/functions/validateSubscription.js:149>) and the tokenless client request in [SubscriptionAPIClient.swift](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/SubscriptionAPIClient.swift:14>), an executor may assign any severity despite forged paid access being possible. Fix — Require confirmed absence of caller authentication, App Check, and Apple verification to produce a CRITICAL H17 row with `blocks: deploy/release` and an urgent human live-deployment check.

8. [HIGH] The simplified `STATE_GEN.md` rejection rationale is factually wrong: matching hashes of the root source files authenticate those sources, not the report’s embedded blocks, which may be truncated or altered while criterion 7 passes. Fix — Using the already-required delimiters and byte counts, extract each embedded block and require its byte count and SHA-256 to match the corresponding root source.

9. [HIGH] Manifest acceptance checks only row count, so duplicating one filename while omitting another passes; excluding outputs merely because they “do not exist yet” also fails on reruns or partial prior attempts. Fix — Record the frozen filename set and require exact unique set equality with the manifest, explicitly excluding both generated root filenames regardless of prior existence.

10. [HIGH] The evidence-precedence rule incorrectly allows test, tooling, dead, or unscoped code to CONTRADICT active runtime architecture merely by existing as a shadow implementation. Stale code cannot establish that reachable behavior is false. Fix — Permit only active reachable evidence to SUPPORT or CONTRADICT runtime claims; out-of-scope material may support only test/tooling/dead-code annotations.

11. [MEDIUM] Several hypothesis IDs bundle independently answerable claims—such as support components, movers paths, function kinds, catalog behavior, and Move Pass sources—while criterion 2 permits only one disposition per ID. Fix — Require component-level sub-dispositions and prohibit overall SUPPORTED unless every enumerated component is supported.

12. [MEDIUM] The Xcode scope instruction does not explain this repository’s `PBXFileSystemSynchronizedRootGroup` structure, whose source build-phase lists are empty; a literal membership inspection can therefore discover zero app files. Fix — State that target membership is resolved from synchronized root directories minus membership exceptions, then apply call-graph reachability separately.

13. [MEDIUM] Final cleanup is not tested: criterion 10 omits simulators despite line 40 prohibiting them, and no criterion verifies staging removal or that `STATE_GEN.md` is the sole new report-directory entry. Fix — Add simulator/live-operation prohibitions to criterion 10 and require an empty/removed staging area plus an exact report-directory delta.

VERDICT: ITERATE
### Outcome

Round cap (MAX_ROUNDS=3) hit without APPROVE. Trajectory: R1 4C/10H/3M → R2 1C/10H/3M → R3 0C/10H/3M. No convergence stall (each round surfaced new, non-restated findings). Decision escalated to user: accept as-is / one more round / abandon. Chain budget spent: 3 of 6.

### User decision after round-cap stop

User selected "One more round": apply all 13 round-3 fixes as rev 4, then resume the same session for round 4 (chain rounds: 4 of 6).

### Response (rev 4)

All 13 round-3 findings accepted: F1 (quiet non-printing redaction scan; gift-code patterns derived from the redemption validator's own format check; checksum-labeled fields exempt from long-hex heuristics), F2 (path-framed per-file baseline hashes with mandatory close-time recomputation; drift → targeted re-verification, recorded), F3 (H41 deployment surfaces + H42 Storage usage/rules added; set-reconciliation extended), F4+F5 (packaging hazard converted to closed-register hypothesis H43 with LOCAL_TOOLCHAIN_OBSERVED class, "package inclusion unverified" honesty rule, non-printing sensitive-file inventory, deploy block retained), F6 (addendum binding-by-default with owner amendment rights; sign_off field schema on CRITICAL rows), F7 (confirmed-absent auth/App Check/Apple verification forces H17 CRITICAL with blocks: deploy/release), F8 (embedded-block extraction verification — prior rejection withdrawn as factually wrong), F9 (manifest = unique set equality; outputs excluded by name regardless of prior existence), F10 (only active reachable evidence may SUPPORT or CONTRADICT runtime claims), F11 (component sub-dispositions for bundled hypotheses), F12 (PBXFileSystemSynchronizedRootGroup membership resolution — literal build-phase inspection banned), F13 (criterion 10 extended to simulators/live ops; staging cleanup + report-directory delta criteria added).

No new rejections this round; one prior rejection withdrawn (R2-F10).

## Round 4 — Codex (same thread, resumed; user-authorized extra round)

All 13 round-3 findings are materially resolved. Remaining refinements are non-blocking:

1. [MEDIUM] The integrity hash covers only “in-scope” dirty/untracked files, potentially excluding pre-existing control state such as `PHASE_MANIFEST` and the untracked task specification; unchanged porcelain status would not detect their content changing. Fix — Hash every pre-existing porcelain path plus the controlling plan, while keeping evidence scope separate from integrity scope.

2. [MEDIUM] Criterion 4 still leaves the Firestore and test discovery universes executor-defined, especially for computed collection paths, `collectionGroup` calls, nested helpers, JavaScript suites, and Xcode targets. Fix — Define search roots and pattern families, require manual tracing for computed paths, and represent unresolved dynamic paths explicitly as UNKNOWN.

3. [MEDIUM] The active-config boundary omits two relevant inputs: root-level [Configuration.storekit](</Users/adampowell/Desktop/Peezy 4.0/Configuration.storekit>) is an explicit target/scheme resource outside the synchronized app root, and [.firebaserc](</Users/adampowell/Desktop/Peezy 4.0/.firebaserc:1>) selects the default Firebase project. Fix — Include explicit build-phase/scheme inputs and Firebase aliases/targets, adding an H41 sub-ID for local target selection while keeping live identity UNKNOWN.

4. [MEDIUM] H43’s “every sensitive file” set remains subjective, and validator-derived gift-code patterns may miss legacy or invalid literals still present in seed/tooling source. Fix — Define a quiet, filename-only assignment-aware candidate scan over the complete Functions bundle and require exact reconciliation of its resulting sensitive-file set.

5. [MEDIUM] H17 and H43 contain mandatory severity/blocking outcomes in prose, but the binary acceptance list does not explicitly enforce those conditions. Fix — Add conditional checks requiring confirmed H17 exposure to be CRITICAL with deploy/release blocks and H43 to retain its deploy block until cleared or signed off.

6. [MEDIUM] Criterion 11 refers to the report-directory “delta” without recording its initial contents, while “record each file’s SHA-256” could be misread as requiring `STATE_GEN.md` to contain its own hash. Fix — Snapshot the report directory initially and clarify that only the two embedded root files have hashes stored inside STATE_GEN, with any report hash recorded externally.

7. [LOW] The top-level “checked-in data only” wording conflicts superficially with the later, intentional `LOCAL_METADATA` and `LOCAL_TOOLCHAIN_OBSERVED` exceptions. Fix — State those two evidence classes as explicit narrow exceptions in the opening discipline rule.

8. [LOW] Manifest inbound-reference searching does not define its roots, so retired documents could keep one another or tooling references could be missed. Fix — Search active code plus tooling/config using fixed-string filenames while excluding candidate documents and generated outputs.

VERDICT: APPROVE
## Final summary

| Round | Verdict | Findings |
|---|---|---|
| 1 | ITERATE | 4 CRITICAL / 10 HIGH / 3 MEDIUM |
| 2 | ITERATE | 1 CRITICAL / 10 HIGH / 3 MEDIUM |
| 3 | ITERATE | 0 CRITICAL / 10 HIGH / 3 MEDIUM |
| 4 | APPROVE | 0 CRITICAL / 0 HIGH / 6 MEDIUM / 2 LOW (non-blocking) |

Consensus reached at rev 4. Chain rounds spent: 4 of 6 (2 remain for codex-execute). Round-4 non-blocking refinements folded into the plan's ADR Follow-ups as executor guidance.

## Execution + final deliverable review (reviewer thread resumed, read-only)

Executor: fresh Sol worker, thread 01a02f07-7f27-7a41-9713-049d8b031e14 — reported 12/12 PASS. Orchestrator independently verified hashes, git-status delta, structure, staging/tmp cleanup, redaction. Final review verdict below disputes 4 acceptance criteria and finds content defects; orchestrator spot-verified claims 3, 5, 6, 9 as TRUE.

1. **[HIGH]** Acceptance ledger: AC1 PASS; AC2 PASS (treating H41e as rev-4’s follow-up-defined sub-ID); AC3 FAIL; AC4 FAIL; AC5 PASS despite its broken self-check; AC6 FAIL/unverifiable; AC7 PASS; AC8 PASS for artifact contents; AC9 FAIL; AC10 PASS on recorded evidence; AC11 PASS literally although write discipline was breached; AC12 PASS. **Fix — Regenerate the deliverables and install them only after all 12 criteria independently report PASS.**

2. **[CRITICAL]** No secret value appears in the three outputs, but the audit exposed production-connected UI-test credentials to terminal/session logs and nevertheless recorded its safety check as PASS ([STATE_GEN.md](</Users/adampowell/Downloads/peezy-reports/STATE_GEN.md:313>)). **Fix — Rotate/remove those credentials, assess or purge retained logs, and rerun using staging-only, non-printing comparisons.**

3. **[HIGH]** STATE_GEN is not a valid verification record: the frozen candidate set, test inventory, and manifest comparison are replaced by shell parse errors; the protocol checker records failure; function output is blank; H43’s purported command is not shell-valid; and the porcelain baseline loses a significant status-column space ([STATE_GEN.md](</Users/adampowell/Downloads/peezy-reports/STATE_GEN.md:31>)). **Fix — Rerun shell-safe, fail-closed commands and capture complete sets, exact output, stderr, and exit status before regenerating hashes and embeddings.**

4. **[HIGH]** H20’s SUPPORTED collection-map disposition is false: it audits backend paths only, omits reachable `packingPlan` and `readiness`, labels `moveAnswers` write-only despite its client read, and labels `identity`/`user_assessments` read-only despite active writes ([PEEZY_STATE.md](</Users/adampowell/Desktop/Peezy 4.0/PEEZY_STATE.md:39>), [TaskActionService.swift](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0/MainInterface/Models/TaskActionService.swift:764>)). **Fix — Reconcile the union of reachable Swift, Functions, and configured-rules paths with per-actor read/write/delete operations.**

5. **[HIGH]** H23 answers a different local cleanup question, cites a nonexistent `Inventory/ViewModels` path, and masks the failed negative check with `|| true`; the deployed `resetInventory` orphan remains UNKNOWN ([PEEZY_STATE.md](</Users/adampowell/Desktop/Peezy 4.0/PEEZY_STATE.md:42>)). **Fix — Disposition the backend/deployed orphan separately as UNKNOWN and document the supported local cleanup bug using `Inventory/Models/InventorySessionManager.swift`.**

6. **[HIGH]** Two ARCHIVE classifications would break retained runbooks: `LAUNCH_CHECKLIST.md` requires `DEAD_CODE_REMOVAL_LIST.md`, while `MASTER_QA_CHECKLIST.md` schedules `THURSDAY_CALIBRATION.md` ([ARCHIVE_MANIFEST.md](</Users/adampowell/Desktop/Peezy 4.0/ARCHIVE_MANIFEST.md:21>), [LAUNCH_CHECKLIST.md](</Users/adampowell/Desktop/Peezy 4.0/LAUNCH_CHECKLIST.md:45>)). **Fix — Reclassify the former KEEP and the latter KEEP/UNKNOWN, then check references from KEEP and UNKNOWN candidates before any ARCHIVE decision.**

7. **[HIGH]** H42 materially understates account-deletion residue: the deletion function omits top-level subscription bindings, workflow submissions, support-thread content, and gift redemption identifiers in addition to using the wrong Storage prefix ([functions/index.js](</Users/adampowell/Desktop/Peezy 4.0/functions/index.js:220>)). **Fix — Add a per-collection delete/retain/anonymize matrix and retain the release block until every personal-data path has an intentional disposition.**

8. **[MEDIUM]** H12c incorrectly says the Hosting admin Firebase configuration is TODO-gated; concrete local configuration satisfies the readiness gate, although live Hosting remains UNKNOWN ([PEEZY_STATE.md](</Users/adampowell/Desktop/Peezy 4.0/PEEZY_STATE.md:25>), [public/admin/index.html](</Users/adampowell/Desktop/Peezy 4.0/public/admin/index.html:853>)). **Fix — Describe the admin surface as locally configured and keep only deployment/authentication behavior UNKNOWN.**

9. **[MEDIUM]** STATE_GEN falsely says the app Google service plist is absent; metadata-only inspection shows it is tracked, 1,227 bytes, and expected as an app resource ([STATE_GEN.md](</Users/adampowell/Downloads/peezy-reports/STATE_GEN.md:317>), [project.pbxproj](</Users/adampowell/Desktop/Peezy 4.0/Peezy 4.0.xcodeproj/project.pbxproj:294>)). **Fix — Correct the protected-file metadata inventory without opening or reproducing its contents.**

10. **[MEDIUM]** H18’s search proves only that Apple verification is unimplemented, not that roadmap work is “pending”; H27 likewise overstates exact determinism because the seeder writes server timestamps and omits the claimed reproducible divergence command. **Fix — Mark roadmap intent UNKNOWN, qualify determinism to source-declared fields, and include the literal divergence command.**

11. **[MEDIUM]** Severity/blocking is inconsistent: unauthenticated `submitWorkflowAnswers` accepts an arbitrary UID and performs cross-account Admin writes but is grouped as HIGH, while H35’s HIGH restore/server-entitlement defect has `blocks:none` ([getWorkflowQualifying.js](</Users/adampowell/Desktop/Peezy 4.0/functions/getWorkflowQualifying.js:137>), [PEEZY_STATE.md](</Users/adampowell/Desktop/Peezy 4.0/PEEZY_STATE.md:87>)). **Fix — Split the arbitrary-user writer into a CRITICAL deploy/release blocker and make H35 block release or document an explicit risk acceptance.**

12. **[LOW]** Requested spot-checks otherwise reproduce: H17, H21, H43’s conservative ignore-list/deploy-block premise, catalog divergence, spawn race, nudge durability, H1/H13/H22/H28/H35/H36, embedded byte equality, and the current 54-row unique manifest set; independent scans found no output secret values. **Fix — Preserve these supported findings while correcting and regenerating the affected artifacts.**

VERDICT: FIX
## Fix round (user-authorized, +1 beyond budget)

Executor thread resumed with all 12 final-review findings verbatim. Result: 12/12 acceptance criteria re-verified fail-closed; PEEZY_STATE.md regenerated (108 lines, 68-ID register incl. H18a/b and H23a/b splits, 37-name collection union, three CRITICAL rows incl. submitWorkflowAnswers); ARCHIVE_MANIFEST transitively re-closed (28 KEEP / 23 ARCHIVE / 3 UNKNOWN); STATE_GEN.md regenerated shell-valid (132,779 bytes). Orchestrator independently confirmed: hashes match, zero parse errors, corrected H23 path, packingPlan/readiness in H20, plist metadata corrected, redaction zero-match incl. credential prefix, git delta = baseline + two outputs. Disclosed safety deviations (credential literals emitted to session logs twice, never to artifacts) stand as recorded; human remediation items: rotate test credentials, three CRITICAL sign_offs PENDING. Total Sol rounds: 7 (4 review + 1 execute + 1 final review + 1 authorized fix).

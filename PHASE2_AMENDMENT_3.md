# PHASE2 AMENDMENT 3 — Owner adjudication of third-pass P1 findings 1–9

Apply to the spec, update §8.6 (one mapping per finding), next read-only pass. No worktree until zero P0/P1 and hash adoption.

1. **Event starvation — adopt.** Phase-0 gains a persisted cursor and a quarantine rule: an envelope failing validation deterministically N times (freeze N=3) moves to a bounded `quarantinedEvents` record with reason and original bytes digest; quarantined events never block the scan window; the 100-poison regression is mandatory. Quarantine is surfaced to the regeneration doc as an UNKNOWN-consumer row, never silently dropped.
2. **`REPLACE_QUOTES` domain — adopt.** Bind it to `TaskCanonicalV1` (same as `COMPLETE_BOOKING` per Amendment 2 #2); fractional parity/replay fixtures.
3. **Cross-task evidence — adopt with the copying rule.** Evidence stays task-instance-bound. `AMENDMENT_CONFIRMATION` on the original writes a **new original-instance evidence record** whose bounded facts embed the replacement's `(task_instance_id, evidence_id, digest, observed_at)` by value. No cross-task resolver exists; the reference is data, verified at write time inside the confirming transaction (both docs read in one transaction). Fixture: confirmation with a concurrently-reset replacement fails closed.
4. **Original terminal state — frozen by higher authority.** `confirmAmendment` terminalizes the original as `Dismissed + SUPERSEDED` carrying the replacement task-instance ID (the master's external-supersede row requires "replaced," and A12's register already decided it). `RETIRED` remains the internal-supersede projection only. Validator and fixtures align.
5. **Restoration triggers — adopt the typed descriptor.** Cancel/undo/reopen gain a read-only `preflight` variant returning `{task_instance_id, branch, requires_trigger: bool, trigger_policy_id, prior_trigger_summary {kind, at_or_event, fired}}`; the mutating call's `TRIGGER_INVALID` error carries the same descriptor. The client sources the confirmed trigger from the descriptor's policy exactly as any fresh command; nothing is guessed from denied snapshots.
6. **Reset aliasing — adopt tombstone.** The canonical UID operation record is never deleted; finalization converts it to a bounded terminal tombstone `{state: finalized, final_receipt_digest, finalized_at, aliases[]}` retained permanently (one doc per reset epoch; growth is bounded by resets, which are rare and epoch-numbered). A losing scene's retained suggestion resolves against the tombstone and adopts the final receipt instead of starting a second reset. Crash-after-first-final-receipt fixture mandatory.
7. **`AUTH_REQUIRED` wedge — adopt lease.** Replace the persisted boolean with a refresh lease `{attempt_id, started_at, expires_at}` written in the same durable record; a crash recovers when the lease expires (freeze 60s) or a credential-revision change lands, whichever first; a live lease still forbids a second refresh. Both crash-before-invocation and crash-before-persistence fixtures.
8. **`task_route.expiresAt` wire type — adopt.** UTC ISO-8601 with exactly milliseconds on the response wire (the Amendment 2 #3 rule); the inbox parses, persists, and compares in epoch milliseconds; Swift/JS parity fixture.
9. **Receipt `disposition?` — adopt the frozen union.** Value union = the six §A dispositions exactly as persisted; present iff the command's transaction wrote a disposition (absent on pure state/handoff transitions); equality projection = string equality on the persisted value. The workflow consumer's WAIT-vs-USER_ACTION branch keys on it; fixture covers the Amendment 2 #4 fallback (WAIT-ineligible → receipt shows USER_ACTION_TRACKED).

Process unchanged: apply, re-register, one read-only pass with all findings together; zero → present the hash for adoption.
